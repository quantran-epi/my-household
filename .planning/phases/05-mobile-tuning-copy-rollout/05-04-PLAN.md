---
phase: 05-mobile-tuning-copy-rollout
plan: 04
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - src/Common/Copy/AppCopy.ts
  - src/Modules/ShoppingList/Screens/ShoppingListDetail.widget.tsx
  - src/Modules/ShoppingList/Screens/ShoppingList.screen.tsx
autonomous: true
requirements: [COPY-03, MOB-03, MOB-01, MOB-02]
must_haves:
  truths:
    - "ShoppingList detail + list user-facing strings read from AppCopy, not inline literals"
    - "ShoppingList pickers and the delete-confirmation render as bottom sheets"
    - "ShoppingList primary CTAs touched by this plan are full-width thumb-zone and >= 44px"
  artifacts:
    - path: "src/Common/Copy/AppCopy.ts"
      provides: "shoppingList namespace covering ShoppingList strings"
      contains: "shoppingList:"
    - path: "src/Modules/ShoppingList/Screens/ShoppingList.screen.tsx"
      provides: "ShoppingList screen reading copy from AppCopy + Sheet confirm"
      contains: "@common/Copy"
  key_links:
    - from: "src/Modules/ShoppingList/Screens/ShoppingListDetail.widget.tsx"
      to: "src/Common/Copy/AppCopy.ts"
      via: "import { AppCopy } from '@common/Copy'"
      pattern: "AppCopy\\.shoppingList\\."
    - from: "src/Modules/ShoppingList/Screens/ShoppingList.screen.tsx"
      to: "@components/Sheet"
      via: "delete-confirm Modal -> Sheet"
      pattern: "@components/Sheet"
---

<objective>
Migrate the ShoppingList high-traffic surfaces — `ShoppingListDetail.widget.tsx` (1546 lines, ~36 strings, the most-used "Đi chợ" detail screen) and `ShoppingList.screen.tsx` (655 lines, ~22 strings, with a delete-confirm Modal) — to read copy from a new `shoppingList` namespace in `AppCopy`, swap their pickers/confirmations onto `@components/Sheet` (MOB-03 including the imperative `modal.confirm` state-lift in the detail widget), and bring this plan's primary CTAs to full-width thumb-zone >= 44px.

This cluster owns `AppCopy.ts` for its wave (additive `shoppingList` namespace) and its two ShoppingList files exclusively. It runs after 05-03 because both edit the single-source `AppCopy.ts`.

Purpose: COPY-03 (high-traffic scope, D-01), MOB-03 (D-06), MOB-01/MOB-02 (D-04).
Output: `shoppingList` namespace in `AppCopy.ts`; both ShoppingList files reading copy via `@common/Copy`, pickers/confirmations as sheets, CTAs touch-sized.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-tuning-copy-rollout/05-CONTEXT.md
@.planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md
@src/Common/Copy/AppCopy.ts
@src/Common/Copy/Glossary.ts
@src/Components/Sheet/index.ts
@src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
---
NOTE on tooling: `rg` here is real ripgrep 14.1.1 (shell wrapper), so the `rg --pcre2` diacritic recipe in `src/Common/Copy/index.ts` works. If `--pcre2` ever errors, substitute `grep -REn` with the same Vietnamese-diacritic class. Run the diacritic grep before AND after migration on each file.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ShoppingListDetail.widget — copy migration + picker/confirm Sheet swap + CTA touch sizing</name>
  <files>src/Common/Copy/AppCopy.ts, src/Modules/ShoppingList/Screens/ShoppingListDetail.widget.tsx</files>
  <read_first>
    - src/Common/Copy/AppCopy.ts (current shape after 05-03; add a sibling `shoppingList:` namespace)
    - src/Common/Copy/Glossary.ts (canonical terms; "Thêm" not "Tạo mới")
    - src/Modules/ShoppingList/Screens/ShoppingListDetail.widget.tsx (full file — ~36 strings; the imperative `modal.confirm` "Tải lại danh sách nguyên liệu?" ~616-619 needs a state-lift per PATTERNS case c)
    - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx (Sheet analog + body-CTA confirm shape)
    - .planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md (bottom-sheet cases a/b/c; layout tuning — full-width Button size="large" thumb-zone, ActionButton defaults NOT touch-sized)
  </read_first>
  <action>
    Run the diacritic recipe on `ShoppingListDetail.widget.tsx` plus a visual scan for JSX text nodes. Add a `shoppingList:` namespace to `AppCopy.ts` (additive sibling, PascalCase const, `as const`, interpolation as named-arg arrow functions) with one leaf per enumerated detail-widget string; name by role (e.g. `shoppingList.detailTitle`, `shoppingList.regenerateConfirm`, `shoppingList.itemCount(fn)`, etc. — derive exact set from the file). Reuse `common.*` where matching. Use glossary canonical terms for any concept that has one. Keep CURRENT phrasing verbatim (migrate only; rewording is 05-07).
    Add `import { AppCopy } from "@common/Copy";` and swap every inline user-facing literal to its `AppCopy.shoppingList.*` / `common.*` reference.
    Sheet swap (MOB-03): convert single-step content `Modal`s/pickers to `<Sheet>` (onCancel->onClose, drop footer={null}, body CTA replaces onOk). For the imperative `modal.confirm({ content: "Tải lại danh sách nguyên liệu?", okText: "Đồng ý", ... onOk: _onGenerate })` — lift to a `useState(regenOpen)` + `<Sheet>` with the message + a primary Button running `_onGenerate` then closing (cheap single confirm, convert per D-06). If any confirm is entangled in multi-branch async, leave as-is with a `// MOB-03 left-as-is (multi-step)` comment and record in SUMMARY.
    Layout (MOB-01/02): bring this widget's primary action buttons to full-width `Button size="large"` thumb-zone shape; bump any sub-44px tappable control touched here to >= 44px via explicit sizing. No new responsive framework (D-05). Preserve all `data-testid`.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `AppCopy.ts` contains `shoppingList:` namespace; `ShoppingListDetail.widget.tsx` imports `@common/Copy` and `@components/Sheet`
    - Post-migration diacritic grep on the file returns 0 user-facing literals outside comments (intentional exceptions recorded in SUMMARY)
    - The `modal.confirm` regenerate site is converted to a `<Sheet>` OR carries the `// MOB-03 left-as-is` comment with rationale in SUMMARY: `rg -n "modal\.confirm" src/Modules/ShoppingList/Screens/ShoppingListDetail.widget.tsx` returns 0 unannotated matches
    - No glossary `avoid` term added: `rg -n 'Tạo mới|Thêm mới' src/Common/Copy/AppCopy.ts` returns 0
    - `yarn build` green
  </acceptance_criteria>
  <done>ShoppingListDetail renders copy from AppCopy; pickers/regenerate confirm are sheets; CTAs touch-sized; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ShoppingList.screen — copy migration + delete-confirm Sheet swap + CTA touch sizing</name>
  <files>src/Common/Copy/AppCopy.ts, src/Modules/ShoppingList/Screens/ShoppingList.screen.tsx</files>
  <read_first>
    - src/Common/Copy/AppCopy.ts (extend the `shoppingList` namespace from Task 1)
    - src/Modules/ShoppingList/Screens/ShoppingList.screen.tsx (full file — ~22 strings; delete-confirm `Modal` ~640-650 with okButtonProps danger)
    - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx (Sheet body-CTA analog)
    - .planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md (confirm-Modal swap case b: danger CTA in Sheet body)
  </read_first>
  <action>
    Run the diacritic recipe on `ShoppingList.screen.tsx` + JSX-text scan. Extend the `shoppingList` namespace in `AppCopy.ts` with this screen's strings (e.g. `shoppingList.deleteConfirmTitle`, `shoppingList.deleteConfirmBody(fn)`, `shoppingList.deleteAction`, etc.). Add `import { AppCopy } from "@common/Copy";` and swap every inline literal.
    Sheet swap (MOB-03, case b): the delete-confirm `Modal` (`onOk`/`okText="Xóa"`/`okButtonProps={{ danger: true }}`) -> `<Sheet>` whose body renders the confirmation message + a `Button type="primary" danger` that runs the original `onDelete` then closes, plus a secondary cancel button running the old `onCancel`. Carry over the `data-testid`.
    Layout (MOB-01/02): primary CTAs to full-width `size="large"` thumb-zone; touched controls >= 44px. No responsive framework (D-05). Preserve all `data-testid`.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `ShoppingList.screen.tsx` imports `@common/Copy` and `@components/Sheet`
    - Delete confirm no longer an antd `Modal` with `onOk`/`okButtonProps`: `rg -n "onOk=|okButtonProps" src/Modules/ShoppingList/Screens/ShoppingList.screen.tsx` returns 0
    - Destructive confirm keeps a `danger` affordance: `rg -n "danger" src/Modules/ShoppingList/Screens/ShoppingList.screen.tsx` returns >= 1
    - Post-migration diacritic grep returns 0 user-facing literals outside comments
    - `yarn build` green
  </acceptance_criteria>
  <done>ShoppingList.screen renders copy from AppCopy; delete confirm is a danger bottom sheet; CTAs touch-sized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user action -> destructive op | Delete/regenerate confirms mutate persisted shopping-list data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | Delete/regenerate confirm moved from Modal onOk to Sheet body Button | mitigate | Preserve exact original handler behind an explicit primary-CTA tap; keep `danger` styling on the delete so the destructive action stays visually deliberate; no implicit confirm. |
| T-05-09 | Denial of Service | Count interpolation (`itemCount`, `deleteConfirmBody`) renders list-derived numbers/names | accept | Values rendered as React text (auto-escaped); no new sink, no `dangerouslySetInnerHTML`. |

No package installs, no network, no data-shape changes — copy + sheet + layout only.
</threat_model>

<verification>
- `yarn build` green (CopyKey union compiles)
- e2e: `tests/e2e/shopping-list.spec.ts` still passes (delete + list flows not regressed)
</verification>

<success_criteria>
- Both ShoppingList files render all user-facing copy from AppCopy (zero inline literals)
- Pickers + delete/regenerate confirms are bottom sheets; delete keeps a danger CTA
- This plan's primary CTAs full-width >= 44px; no responsive framework introduced
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 04)

New AppCopy namespace: `shoppingList` (leaves derived from ShoppingListDetail.widget + ShoppingList.screen — e.g. `shoppingList.detailTitle`, `shoppingList.regenerateConfirm`, `shoppingList.deleteConfirmTitle`, `shoppingList.deleteAction`, `shoppingList.itemCount(fn)`; exact set enumerated by executor).
New component usages: `AppCopy` import in both ShoppingList files; `@components/Sheet` in both; lifted `regenOpen` state + sheet in the detail widget; carried-over delete-confirm `data-testid`.
New file paths: none.
</artifacts>

<output>
Create `.planning/phases/05-mobile-tuning-copy-rollout/05-04-SUMMARY.md` when done.
</output>
