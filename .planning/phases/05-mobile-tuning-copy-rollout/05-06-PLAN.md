---
phase: 05-mobile-tuning-copy-rollout
plan: 06
type: execute
wave: 5
depends_on: [05-05]
files_modified:
  - src/Common/Copy/AppCopy.ts
  - src/Modules/DishSuggester/Screens/DishSuggester.screen.tsx
  - src/Modules/Dishes/Screens/DishesList.screen.tsx
  - src/Modules/Ingredient/Screens/IngredientList.screen.tsx
autonomous: true
requirements: [COPY-03, MOB-03, MOB-01, MOB-02]
must_haves:
  truths:
    - "DishSuggester, DishesList, IngredientList user-facing strings read from AppCopy"
    - "Single-step pickers/confirmations on these list screens render as bottom sheets"
    - "Primary list-screen actions meet >= 44px touch targets within the antd-token idiom"
  artifacts:
    - path: "src/Common/Copy/AppCopy.ts"
      provides: "dishSuggester / dishes / ingredient namespaces"
      contains: "dishes:"
  key_links:
    - from: "src/Modules/Dishes/Screens/DishesList.screen.tsx"
      to: "src/Common/Copy/AppCopy.ts"
      via: "import { AppCopy } from '@common/Copy'"
      pattern: "AppCopy\\.(dishes|dishSuggester|ingredient)\\."
    - from: "src/Modules/DishSuggester/Screens/DishSuggester.screen.tsx"
      to: "@components/Sheet"
      via: "single-step pickers -> Sheet"
      pattern: "@components/Sheet"
---

<objective>
Migrate the three primary list/suggester screens — `DishSuggester.screen.tsx` (1280 lines, ~23 strings; the suggester that stays reachable per NAV-02 and shares `IngredientPickerWidget` with the wizard), `DishesList.screen.tsx` (783 lines, ~21 strings, `Modal` with `okButtonProps danger`), `IngredientList.screen.tsx` (635 lines, ~22 strings) — to read copy from new `dishSuggester` / `dishes` / `ingredient` namespaces, swap single-step pickers/confirmations onto `@components/Sheet`, and bring primary actions to >= 44px touch targets within the existing antd-token idiom.

This is the final copy-migration cluster; it owns `AppCopy.ts` for its wave (additive namespaces) and its three screen files exclusively. Runs after 05-05 (shared `AppCopy.ts`).

Purpose: COPY-03 (high-traffic, D-01), MOB-03 (D-06), MOB-01/MOB-02 (D-04).
Output: three new namespaces; three screens reading copy via `@common/Copy`, single-step pickers as sheets, primary CTAs touch-sized.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-tuning-copy-rollout/05-CONTEXT.md
@.planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md
@src/Common/Copy/AppCopy.ts
@src/Common/Copy/Glossary.ts
@src/Components/Sheet/index.ts
@src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
@src/Modules/MealPlanning/Screens/WizardIngredientStep.widget.tsx
@.planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md
---
NOTE: `rg` here is real ripgrep 14.1.1 — the `--pcre2` diacritic recipe in `src/Common/Copy/index.ts` works. Fallback: `grep -REn` with the same diacritic class. Run before AND after migration per file. NAV-02: do not remove the DishSuggester route's entry path — it must stay reachable.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DishSuggester.screen — copy migration + single-step picker Sheet swap</name>
  <files>src/Common/Copy/AppCopy.ts, src/Modules/DishSuggester/Screens/DishSuggester.screen.tsx</files>
  <read_first>
    - src/Common/Copy/AppCopy.ts (add sibling `dishSuggester:` namespace)
    - src/Common/Copy/Glossary.ts (canonical: dish "Món", addAction "Thêm")
    - src/Modules/DishSuggester/Screens/DishSuggester.screen.tsx (full file — ~23 strings; pickers/modals; shares IngredientPickerWidget; back-button idiom ~432-439/543 already 40px circle)
    - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx (Sheet analog)
    - .planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md (bottom-sheet cases; ActionButton 28px caveat — do NOT use defaults for primary CTAs)
  </read_first>
  <action>
    Run the diacritic recipe + JSX-text scan. Add a `dishSuggester:` namespace to `AppCopy.ts` (additive, `as const`, named-arg fn interpolation) with one leaf per enumerated string. Reuse `common.*` and the `wizard`/`dishes` keys where the literal already matches. Add `import { AppCopy } from "@common/Copy";` and swap every inline literal.
    Sheet swap (MOB-03): convert single-step pickers/confirmations (`rg -n "Modal|Popconfirm|modal\.confirm"`) to `<Sheet>` per the case a/b/c protocol; the existing `IngredientPickerWidget` host should sit inside a `<Sheet>` mirroring the wizard's ingredient sheet. Leave any complex multi-step modal as-is with the `// MOB-03 left-as-is (multi-step)` annotation + SUMMARY note.
    Layout (MOB-01/02): the back-circle is already 40px — bump to >= 44px only if it is a primary nav affordance on this screen; do not convert compact 28px `ActionButton` rows into primary CTAs. Touch-bump only the screen's primary action(s). No new responsive framework (D-05). Preserve all `data-testid` and the route entry path (NAV-02).
  </action>
  <verify>
    <automated>yarn build && yarn test:e2e --grep 'dish-suggester' || yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `AppCopy.ts` contains `dishSuggester:`; `DishSuggester.screen.tsx` imports `@common/Copy` (and `@components/Sheet` if it had a convertible picker)
    - Post-migration diacritic grep returns 0 user-facing literals outside comments
    - DishSuggester route still present in entry path (spot-check against ROUTE-INVENTORY.md); `dish-suggester` e2e passes (or build-only fallback recorded)
    - `yarn build` green
  </acceptance_criteria>
  <done>DishSuggester renders copy from AppCopy; single-step pickers are sheets; route still reachable.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DishesList + IngredientList — copy migration + Sheet swap + touch targets</name>
  <files>src/Common/Copy/AppCopy.ts, src/Modules/Dishes/Screens/DishesList.screen.tsx, src/Modules/Ingredient/Screens/IngredientList.screen.tsx</files>
  <read_first>
    - src/Common/Copy/AppCopy.ts (add `dishes:` and `ingredient:` sibling namespaces)
    - src/Common/Copy/Glossary.ts (dish "Món" short form in lists; addAction "Thêm")
    - src/Modules/Dishes/Screens/DishesList.screen.tsx (full file — ~21 strings incl. 305 `useScreenTitle({ value: "Món ăn" })`, 572 "Đã lưu thời gian món ăn", 630 "Chưa có", 648 "Hoàn thiện"/"Cần cập nhật"; Modal with okButtonProps danger)
    - src/Modules/Ingredient/Screens/IngredientList.screen.tsx (full file — ~22 strings; picker/confirm Modals)
    - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx (Sheet analog)
    - .planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md (bottom-sheet b: danger confirm -> Sheet body danger CTA; ActionButton caveat)
  </read_first>
  <action>
    For each file: run diacritic recipe + JSX-text scan, add namespace leaves to `AppCopy.ts` (`dishes:` for DishesList, `ingredient:` for IngredientList), `import { AppCopy } from "@common/Copy";`, swap every inline literal. Use glossary short form "Món" in list contexts where a new choice arises; keep phrasing verbatim otherwise (reword in 05-08).
    Sheet swap (MOB-03): convert single-step pickers and confirmations to `<Sheet>`. The DishesList delete confirm (`Modal` + `okButtonProps={{ danger: true }}`) -> Sheet body with a `Button type="primary" danger` running the original `onOk`, secondary cancel running `onCancel`. Same for IngredientList confirms. Complex multi-step modals left-as-is + annotated + SUMMARY.
    Layout (MOB-01/02): touch-bump the primary action(s) on each list screen to >= 44px (full-width `Button size="large"` for a footer/primary add, or explicit `height={44}` on a circle action) — do NOT restyle the compact in-row 28px `ActionButton`s into primary CTAs. No new responsive framework (D-05). Preserve all `data-testid`.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `AppCopy.ts` contains `dishes:` and `ingredient:`; both files import `@common/Copy`
    - Post-migration diacritic grep on each file returns 0 user-facing literals outside comments (including `useScreenTitle({ value: ... })` and `message.success(...)` toasts)
    - DishesList delete confirm renders a `danger` CTA inside a Sheet: `rg -n "danger" src/Modules/Dishes/Screens/DishesList.screen.tsx` returns >= 1 and `rg -n "okButtonProps" src/Modules/Dishes/Screens/DishesList.screen.tsx` returns 0 on the converted confirm
    - At least one primary CTA per screen carries `size="large"` or explicit `height={44}`
    - `yarn build` green
  </acceptance_criteria>
  <done>DishesList + IngredientList render copy from AppCopy; single-step pickers/confirms are sheets; primary CTAs touch-sized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user action -> dish/ingredient mutation | Delete/edit confirmations mutate persisted catalog data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | DishesList delete confirm moved from Modal danger onOk to Sheet body | mitigate | Preserve handler behind an explicit `danger` CTA tap; no auto-confirm; destructive affordance stays visually distinct. |
| T-05-13 | Information Disclosure | NAV-02 — DishSuggester must stay reachable | mitigate | Verify route entry path against ROUTE-INVENTORY.md after edit; do not remove nav entry. |

No package installs, no network, no data-shape changes — copy + sheet + layout only.
</threat_model>

<verification>
- `yarn build` green
- DishSuggester route reachable (ROUTE-INVENTORY.md spot-check; dish-suggester e2e)
- e2e/manual: DishesList delete still requires explicit danger tap and removes the right dish
</verification>

<success_criteria>
- All three screens render user-facing copy from AppCopy
- Single-step pickers/confirmations are bottom sheets; destructive confirms keep danger CTA; complex ones recorded
- Primary CTAs touch-sized; no responsive framework introduced; DishSuggester stays reachable
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 06)

New AppCopy namespaces: `dishSuggester`, `dishes`, `ingredient` (leaves enumerated by executor from the three screens — e.g. `dishes.screenTitle` = "Món ăn", `dishes.savedCookTimeToast`, `dishes.statusComplete`/`statusNeedsUpdate`, `ingredient.*`, `dishSuggester.*`).
New component usages: `AppCopy` import in all three screens; `@components/Sheet` where a single-step picker existed; touch-sized primary CTAs.
New file paths: none.
</artifacts>

<output>
Create `.planning/phases/05-mobile-tuning-copy-rollout/05-06-SUMMARY.md` when done.
</output>
