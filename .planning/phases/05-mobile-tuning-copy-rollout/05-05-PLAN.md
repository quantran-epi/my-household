---
phase: 05-mobile-tuning-copy-rollout
plan: 05
type: execute
wave: 4
depends_on: [05-04]
files_modified:
  - src/Common/Copy/AppCopy.ts
  - src/Modules/ScheduledMeal/Screens/ScheduledMealList.screen.tsx
  - src/Modules/ScheduledMeal/Screens/ScheduledMealAdd.widget.tsx
autonomous: true
requirements: [COPY-03, MOB-03, MOB-01, MOB-02]
must_haves:
  truths:
    - "ScheduledMeal list + add user-facing strings read from AppCopy, not inline literals"
    - "ScheduledMeal list pickers/confirmations render as bottom sheets"
    - "ScheduledMealAdd footer CTA is full-width thumb-zone (>= 44px), not a flex-end default button"
  artifacts:
    - path: "src/Common/Copy/AppCopy.ts"
      provides: "scheduledMeal namespace covering ScheduledMeal strings"
      contains: "scheduledMeal:"
    - path: "src/Modules/ScheduledMeal/Screens/ScheduledMealAdd.widget.tsx"
      provides: "Add widget reading copy from AppCopy with thumb-zone CTA"
      contains: "@common/Copy"
  key_links:
    - from: "src/Modules/ScheduledMeal/Screens/ScheduledMealList.screen.tsx"
      to: "src/Common/Copy/AppCopy.ts"
      via: "import { AppCopy } from '@common/Copy'"
      pattern: "AppCopy\\.scheduledMeal\\."
    - from: "src/Modules/ScheduledMeal/Screens/ScheduledMealList.screen.tsx"
      to: "@components/Sheet"
      via: "the 5 list Modals -> Sheet where single-step"
      pattern: "@components/Sheet"
---

<objective>
Migrate the ScheduledMeal journey-adjacent surfaces — `ScheduledMealList.screen.tsx` (804 lines, ~32 strings, 5 antd `Modal`s) and `ScheduledMealAdd.widget.tsx` (181 lines, ~13 strings, the worst-offender footer CTA at `justify="flex-end"` with a default-size Button) — to read copy from a new `scheduledMeal` namespace, swap single-step list pickers/confirmations onto `@components/Sheet`, and retune the Add footer CTA to the full-width thumb-zone idiom (MOB-01/02). The result step (05-01) adds meals here, so this is journey-adjacent.

This cluster owns `AppCopy.ts` for its wave (additive `scheduledMeal` namespace) and its two ScheduledMeal files exclusively. Runs after 05-04 (shared `AppCopy.ts`).

Purpose: COPY-03 (high-traffic, D-01), MOB-03 (D-06), MOB-01/MOB-02 (D-04).
Output: `scheduledMeal` namespace; both files reading copy via `@common/Copy`, pickers as sheets, Add CTA thumb-zoned.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-tuning-copy-rollout/05-CONTEXT.md
@.planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md
@src/Common/Copy/AppCopy.ts
@src/Common/Copy/Glossary.ts
@src/Components/Sheet/index.ts
@src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
@src/Modules/MealPlanning/Screens/WizardIngredientStep.widget.tsx
---
NOTE: `rg` here is real ripgrep 14.1.1 — the `--pcre2` diacritic recipe in `src/Common/Copy/index.ts` works. Fallback: `grep -REn` with the same diacritic class. Run before AND after migration per file.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ScheduledMealList.screen — copy migration + list-Modal Sheet swap</name>
  <files>src/Common/Copy/AppCopy.ts, src/Modules/ScheduledMeal/Screens/ScheduledMealList.screen.tsx</files>
  <read_first>
    - src/Common/Copy/AppCopy.ts (add a sibling `scheduledMeal:` namespace)
    - src/Common/Copy/Glossary.ts (canonical: addAction "Thêm", todaysMeal "Bữa hôm nay"; avoid "Tạo mới")
    - src/Modules/ScheduledMeal/Screens/ScheduledMealList.screen.tsx (full file — ~32 strings; 5 Modals incl. the content Modal "Thêm thực đơn" ~396-409 with okText="Tạo"/cancelText="Huỷ")
    - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx (Sheet analog)
    - .planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md (bottom-sheet cases a/b/c; content-Modal swap example uses THIS file ~396-409)
  </read_first>
  <action>
    Run the diacritic recipe + JSX-text scan on `ScheduledMealList.screen.tsx`. Add a `scheduledMeal:` namespace to `AppCopy.ts` (additive sibling const, `as const`, named-arg fn interpolation) with one leaf per enumerated string (e.g. `scheduledMeal.addMenuLabel` = "Thêm thực đơn", `scheduledMeal.createAction`, `scheduledMeal.cancelAction`, etc.). Reuse `common.*` (e.g. `common.cancel` = "Hủy" — note the file uses "Huỷ" with a different diacritic; migrate to the canonical `common.cancel` value and record the normalization). Use glossary canonical terms; keep phrasing verbatim otherwise (reword later in 05-08).
    Add `import { AppCopy } from "@common/Copy";` and swap every inline literal to `AppCopy.scheduledMeal.*` / `common.*`.
    Sheet swap (MOB-03): the content `Modal` ("Thêm thực đơn", `footer={null}`, `destroyOnClose`, hosts `ScheduledMealAddWidget` via `DeferredModalContent`) -> `<Sheet open title onClose>`, keep `DeferredModalContent` inside, map `onCancel`->`onClose`, drop `footer={null}`. Convert the other single-step Modals the same way (content -> Sheet; confirm -> Sheet body + explicit CTA, danger where destructive). Any multi-step/complex modal among the 5 -> leave as-is with `// MOB-03 left-as-is (multi-step)` and record in SUMMARY. Preserve all `data-testid`.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `AppCopy.ts` contains `scheduledMeal:`; `ScheduledMealList.screen.tsx` imports `@common/Copy` and `@components/Sheet`
    - Post-migration diacritic grep on the file returns 0 user-facing literals outside comments
    - Content "Thêm thực đơn" Modal converted to Sheet: `rg -n "footer={null}" src/Modules/ScheduledMeal/Screens/ScheduledMealList.screen.tsx` returns only lines on intentionally-retained complex modals (annotated)
    - No glossary avoid term: `rg -n 'Tạo mới|Thêm mới' src/Common/Copy/AppCopy.ts` returns 0
    - `yarn build` green
  </acceptance_criteria>
  <done>ScheduledMealList renders copy from AppCopy; single-step list Modals are sheets; complex ones recorded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ScheduledMealAdd.widget — copy migration + worst-offender footer CTA retune</name>
  <files>src/Common/Copy/AppCopy.ts, src/Modules/ScheduledMeal/Screens/ScheduledMealAdd.widget.tsx</files>
  <read_first>
    - src/Common/Copy/AppCopy.ts (extend `scheduledMeal` namespace from Task 1)
    - src/Modules/ScheduledMeal/Screens/ScheduledMealAdd.widget.tsx (full file — ~13 strings incl. line 67 "Thực đơn chưa đặt tên", 77 "Đã tạo thực đơn", 84 "Tên gợi nhớ", 142 "Nhập tên", 151 "Cho ai ăn?", 178 `<Button>Lưu</Button>`; footer `justify="flex-end"` ~177-179)
    - src/Modules/MealPlanning/Screens/WizardIngredientStep.widget.tsx (thumb-zone CTA analog lines 61-80: full-width Button size="large" in a column Stack)
    - .planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md (layout tuning — this file's footer is the named worst-offender CTA to retune)
  </read_first>
  <action>
    Run the diacritic recipe + JSX-text scan. Extend the `scheduledMeal` namespace with this widget's strings (e.g. `scheduledMeal.defaultMenuName` = "Thực đơn chưa đặt tên", `scheduledMeal.createdToast` = "Đã tạo thực đơn", `scheduledMeal.nameLabel` = "Tên gợi nhớ", `scheduledMeal.namePlaceholder` = "Nhập tên", `scheduledMeal.forWhom` = "Cho ai ăn?", reuse `common.save` = "Lưu"). Add `import { AppCopy } from "@common/Copy";` and swap every inline literal (including the `message.success("Đã tạo thực đơn")` toast -> `AppCopy.scheduledMeal.createdToast`).
    Layout (MOB-01/02): retune the footer from `Stack justify="flex-end"` + default-size `Button` to the wizard thumb-zone idiom — full-width `Button type="primary" size="large"` (>= 40px, antd large) at the bottom in a `Stack direction="column" fullwidth`. This is the named worst-offender CTA in PATTERNS. No new responsive framework (D-05). Preserve all `data-testid`.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `ScheduledMealAdd.widget.tsx` imports `@common/Copy`; the save toast reads `AppCopy.scheduledMeal.createdToast`
    - Footer CTA is full-width large, not flex-end default: `rg -n 'justify="flex-end"' src/Modules/ScheduledMeal/Screens/ScheduledMealAdd.widget.tsx` returns 0 for the primary save footer; `rg -n 'size="large"' src/Modules/ScheduledMeal/Screens/ScheduledMealAdd.widget.tsx` returns >= 1
    - Post-migration diacritic grep returns 0 user-facing literals outside comments
    - `yarn build` green
  </acceptance_criteria>
  <done>ScheduledMealAdd renders copy from AppCopy; footer save CTA is full-width thumb-zone large.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user action -> meal mutation | Add/create + list confirmations mutate persisted scheduled-meal data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | List confirm actions moved from Modal onOk to Sheet body CTA | mitigate | Preserve original handlers behind explicit CTA tap; keep danger styling on destructive confirms. |
| T-05-11 | Denial of Service | Name interpolation renders user-entered menu names | accept | Rendered as React text (auto-escaped); no new sink. |

No package installs, no network, no data-shape changes — copy + sheet + layout only.
</threat_model>

<verification>
- `yarn build` green
- e2e/manual: add-menu flow opens as a Sheet and still creates a menu; list confirmations still act on the right item
</verification>

<success_criteria>
- Both ScheduledMeal files render all user-facing copy from AppCopy
- Single-step list pickers/confirmations are bottom sheets; complex ones recorded
- Add footer CTA full-width >= 40px large; no responsive framework introduced
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 05)

New AppCopy namespace: `scheduledMeal` (leaves derived from ScheduledMealList.screen + ScheduledMealAdd.widget — e.g. `scheduledMeal.addMenuLabel`, `scheduledMeal.createAction`, `scheduledMeal.defaultMenuName`, `scheduledMeal.createdToast`, `scheduledMeal.nameLabel`, `scheduledMeal.namePlaceholder`, `scheduledMeal.forWhom`; exact set enumerated by executor). Note: "Huỷ" normalized to `common.cancel` value "Hủy".
New component usages: `AppCopy` import in both files; `@components/Sheet` in ScheduledMealList; thumb-zone footer CTA in ScheduledMealAdd.
New file paths: none.
</artifacts>

<output>
Create `.planning/phases/05-mobile-tuning-copy-rollout/05-05-SUMMARY.md` when done.
</output>
