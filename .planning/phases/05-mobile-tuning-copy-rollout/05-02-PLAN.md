---
phase: 05-mobile-tuning-copy-rollout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Modules/ShoppingList/Screens/ShoppingListExport.widget.tsx
  - src/Modules/ShoppingList/Screens/ShoppingListMealDetail.widget.tsx
  - src/Modules/ScheduledMeal/Screens/LeftoverManagement.screen.tsx
  - src/Modules/ScheduledMeal/Screens/MealSlotTimesModal.tsx
  - src/Modules/ScheduledMeal/Screens/ScheduledMealToolkit.widget.tsx
  - src/Modules/Dishes/Screens/DishDetail.widget.tsx
  - src/Modules/Dishes/Screens/FinishCooking.widget.tsx
  - src/Modules/Dishes/Screens/DishesExport.widget.tsx
  - src/Modules/Ingredient/Screens/IngredientInventory.widget.tsx
autonomous: true
requirements: [MOB-03]
must_haves:
  truths:
    - "Simple picker and confirmation surfaces across the app open as bottom sheets, not centered antd Modals"
    - "Multi-step / complex flow modals are deliberately left as-is and recorded, not silently broken"
    - "The full app-wide MOB-03 picker/confirmation inventory has a per-file disposition (converted-by-plan / deferred-with-reason) that is auditable at the verification gate"
  artifacts:
    - path: "src/Modules/Ingredient/Screens/IngredientInventory.widget.tsx"
      provides: "Inventory picker hosted in @components/Sheet"
      contains: "Sheet"
  key_links:
    - from: "swept picker files"
      to: "@components/Sheet"
      via: "import { Sheet } from '@components/Sheet'"
      pattern: "@components/Sheet"
---

<objective>
Sweep simple, single-step pickers and confirmations across the app onto the `@components/Sheet` bottom-sheet pattern (MOB-03, D-06 app-wide breadth). This plan covers the long-tail picker/confirmation files that are NOT owned by the copy-migration cluster plans (03-07), so it runs in parallel on a disjoint file set and touches no `AppCopy.ts` or copy strings.

This plan is also the **owner of the full MOB-03 inventory disposition**. The 05-PATTERNS.md app-wide picker/confirmation inventory lists every `Modal`/`Popconfirm`/`modal.confirm` site in the app. D-06 grants subset discretion (the planner picks which sites convert and may leave complex multi-step modals as-is), but the "app-wide" intent means the disposition must be **auditable** — every inventoried file is either converted (by this plan or a named cluster plan) or deliberately deferred with a recorded reason. Task 3 records that full disposition so MOB-03 coverage is traceable at the verification gate (no silent omissions).

D-06 makes the sheet sweep intentionally broader than layout tuning because the swap is mechanical and low-risk. Per D-06 the planner inventories sites and may leave ambiguous/complex multi-step modals as-is. This plan converts the cheap cases this plan owns, and explicitly records the disposition of the entire inventory.

Purpose: MOB-03 (consistent mobile ergonomics for pickers/confirmations app-wide).
Output: The listed long-tail files render `<Sheet>` for their single-step pickers/confirmations; complex flow modals documented as deliberately retained; full inventory disposition recorded in SUMMARY.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-tuning-copy-rollout/05-CONTEXT.md
@.planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md
@src/Components/Sheet/index.ts
@src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Inventory + convert content/picker Modals to Sheet (ShoppingList + ScheduledMeal long-tail)</name>
  <files>src/Modules/ShoppingList/Screens/ShoppingListExport.widget.tsx, src/Modules/ShoppingList/Screens/ShoppingListMealDetail.widget.tsx, src/Modules/ScheduledMeal/Screens/LeftoverManagement.screen.tsx, src/Modules/ScheduledMeal/Screens/MealSlotTimesModal.tsx, src/Modules/ScheduledMeal/Screens/ScheduledMealToolkit.widget.tsx</files>
  <read_first>
    - src/Components/Sheet/index.ts (the @components/Sheet barrel; SheetProps = open/title/onClose/data-testid, no footer/onOk slot)
    - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx (Sheet usage analog lines 197-219: title + body + primary CTA inside; confirm action modeled as a primary Button in the body)
    - .planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md (Bottom-sheet section: content-Modal swap (a), confirm-Modal swap (b), imperative modal.confirm (c) needs state-lift)
    - Each target file in this task (read fully before editing — confirm each Modal is single-step)
  </read_first>
  <action>
    For each file, locate antd `Modal` / `Popconfirm` / `modal.confirm` picker and confirmation surfaces (`rg -n "Modal|Popconfirm|modal\.confirm" <file>`). Convert SINGLE-STEP pickers/confirmations to `<Sheet>`:
    - Add `import { Sheet } from "@components/Sheet";`. Remove the now-unused `Modal` import only if no Modal usage remains in the file.
    - Content Modal (form/picker host): `<Modal open title footer={null} onCancel destroyOnClose>` -> `<Sheet open title onClose data-testid={...}>`; keep the inner content (and any `DeferredModalContent`) unchanged; map `onCancel`->`onClose`; drop `footer={null}` (Sheet has no footer slot).
    - Confirm Modal (`onOk`/`okText`/`okButtonProps danger`): render the message + an explicit CTA stack inside the Sheet body — model the confirm action as a `Button type="primary"` (add `danger` when the original used `okButtonProps={{ danger: true }}`) that runs the old `onOk` then closes; a secondary text/cancel button runs the old `onCancel`. Mirror the WizardResult body-CTA shape.
    - Imperative `modal.confirm(...)`: per D-06 (PATTERNS case c) this needs lifting to `useState(open)` + `<Sheet>`. Convert if the call is a simple single confirm (cheap); if it is entangled with multi-branch async flow, LEAVE AS-IS and add a one-line `// MOB-03: imperative confirm left as-is (multi-step), see 05-02 SUMMARY` comment and record it in the SUMMARY deferred list.
    Preserve every `data-testid`. Do NOT touch copy strings (those migrate in their owning cluster plan) — leave inline literals exactly as found.
    Add a `data-testid` to each new Sheet if the source Modal had one (carry it over) or derive a stable one (`<feature>-sheet`).
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - Each edited file imports `@components/Sheet`: `rg -l "@components/Sheet" src/Modules/ShoppingList/Screens/ShoppingListExport.widget.tsx src/Modules/ShoppingList/Screens/ShoppingListMealDetail.widget.tsx src/Modules/ScheduledMeal/Screens/LeftoverManagement.screen.tsx src/Modules/ScheduledMeal/Screens/MealSlotTimesModal.tsx src/Modules/ScheduledMeal/Screens/ScheduledMealToolkit.widget.tsx` lists every file that had a convertible picker
    - For each converted surface, no `footer={null}` / `onOk=` remains on the swapped element (`rg -n "onOk=|footer={null}"` on each file returns only lines belonging to intentionally-retained complex modals, which carry the MOB-03 left-as-is comment)
    - `yarn build` succeeds
    - SUMMARY lists every converted surface and every deliberately-retained complex modal with rationale
  </acceptance_criteria>
  <done>ShoppingList/ScheduledMeal long-tail single-step pickers/confirmations render as bottom sheets; complex ones recorded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert content/picker Modals to Sheet (Dishes + Ingredient long-tail)</name>
  <files>src/Modules/Dishes/Screens/DishDetail.widget.tsx, src/Modules/Dishes/Screens/FinishCooking.widget.tsx, src/Modules/Dishes/Screens/DishesExport.widget.tsx, src/Modules/Ingredient/Screens/IngredientInventory.widget.tsx</files>
  <read_first>
    - src/Components/Sheet/index.ts (Sheet barrel + props)
    - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx (Sheet analog)
    - .planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md (Bottom-sheet swap cases a/b/c)
    - Each target file in this task (read fully before editing)
  </read_first>
  <action>
    Same conversion protocol as Task 1, applied to the Dishes/Ingredient long-tail files. For each: `rg -n "Modal|Popconfirm|modal\.confirm" <file>`, then convert single-step pickers (content Modal -> Sheet, map onCancel->onClose, drop footer) and single-step confirms (Sheet body with explicit primary/danger CTA replacing onOk). Add `import { Sheet } from "@components/Sheet";`. Leave any multi-step/cooking-flow modal (e.g. a multi-stage FinishCooking flow) as-is with the `// MOB-03: left as-is (multi-step)` comment and record in SUMMARY. Do NOT touch copy strings. Preserve all `data-testid`s.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -l "@components/Sheet" src/Modules/Dishes/Screens/DishDetail.widget.tsx src/Modules/Dishes/Screens/DishesExport.widget.tsx src/Modules/Ingredient/Screens/IngredientInventory.widget.tsx` lists each file that had a convertible picker
    - No swapped surface retains `footer={null}` / `onOk=` (except intentionally-retained complex modals carrying the left-as-is comment)
    - `yarn build` succeeds
    - SUMMARY lists converted vs deliberately-retained surfaces with rationale
  </acceptance_criteria>
  <done>Dishes/Ingredient long-tail single-step pickers render as bottom sheets; complex flows recorded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record the full app-wide MOB-03 inventory disposition</name>
  <files>(SUMMARY only — no source edits)</files>
  <read_first>
    - .planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md (the "Picker/confirmation inventory (app-wide, D-06 broad sweep)" list, lines ~166-175 — the authoritative file list)
    - The "MOB-03 Inventory Disposition" reference table in this plan (below)
  </read_first>
  <action>
    Reproduce the full app-wide MOB-03 picker/confirmation inventory disposition in this plan's SUMMARY so MOB-03 coverage is auditable end-to-end. Cross-check the 05-PATTERNS.md inventory list against the reference table below: for EVERY inventoried file, record exactly one disposition — `converted (this plan / 05-NN)`, `converted (cluster plan 05-NN)`, or `deferred (reason)`. After Tasks 1-2 run, update the "converted by 05-02" rows to reflect what was ACTUALLY converted vs left-as-is (a file that turned out fully complex moves to the deferred set with its reason).
    Do NOT convert the deferred files in this plan — recording them with a reason is the D-06-sanctioned disposition (the checker confirmed this is acceptable). The deferred reasons must be honest scope/complexity statements (out-of-phase-scope per D-01, or genuine multi-step flow), never "too hard".
    No source files are edited in this task — it is a documentation/audit deliverable written into `05-02-SUMMARY.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-mobile-tuning-copy-rollout/05-02-SUMMARY.md &amp;&amp; grep -qi "inventory disposition" .planning/phases/05-mobile-tuning-copy-rollout/05-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains an "MOB-03 Inventory Disposition" section
    - EVERY file in the 05-PATTERNS.md app-wide inventory appears exactly once with a disposition (converted-by-plan or deferred-with-reason) — no inventoried file is unaccounted for
    - Every deferred file carries a concrete reason (out-of-scope per D-01, or multi-step flow); no "too hard" rationale
    - The converted-by-05-02 rows match what Tasks 1-2 actually did (left-as-is reclassifications reflected)
  </acceptance_criteria>
  <done>The full app-wide MOB-03 inventory has an auditable per-file disposition recorded in SUMMARY; no inventoried file is silently omitted.</done>
</task>

</tasks>

<mob03_inventory_disposition>
## MOB-03 Inventory Disposition (reference for Task 3)

Authoritative source list: 05-PATTERNS.md "Picker/confirmation inventory (app-wide, D-06 broad sweep)". Each inventoried file gets exactly one disposition. Task 3 reproduces this in SUMMARY and reconciles the 05-02 rows against actual conversions.

**Converted — this plan (05-02), single-step pickers/confirmations:**
- ShoppingList: `ShoppingListExport.widget`, `ShoppingListMealDetail.widget`
- ScheduledMeal: `LeftoverManagement.screen`, `MealSlotTimesModal`, `ScheduledMealToolkit.widget`
- Dishes: `DishDetail.widget`, `DishesExport.widget`, `FinishCooking.widget` (single-step portions only)
- Ingredient: `IngredientInventory.widget`

**Converted — by named cluster plan (copy + sheet swap together):**
- `ShoppingList.screen`, `ShoppingListDetail.widget` → 05-04
- `ScheduledMealList.screen` → 05-05
- `DishesList.screen`, `IngredientList.screen` → 05-06
- `DishSuggester.screen` → 05-06
- `SidebarDrawer.tsx` (PIN/backup Modals) → 05-03

**Deferred — complex multi-step flow (D-06 discretion; genuine multi-stage modal):**
- `ScheduledMealFinishFlow.modal` — multi-step finish flow, no single declarative Sheet equivalent
- `CookingSession.widget` — multi-step cooking session
- `FinishCooking.widget` multi-stage portions (if entangled) — single-step parts converted in Task 2; any multi-stage flow left-as-is + annotated

**Deferred — out of phase scope (D-01 prioritizes journey + high-traffic; D-06 subset discretion; lower-traffic long-tail, candidate for a later sweep):**
- Home module: `HouseholdHealth.widget`, `NutritionGoals.screen`, `Dashboard.screen`, `Templates.screen`, `HouseholdProfiles.screen`, `SyncBackupHealth.screen`, `NutritionCalculator.widget`
- ScheduledMeal long-tail: `ScheduledMealCooking.widget`, `SmartPlannerTemplates.screen`, `ScheduledMealSlotDetail.modal`, `ScheduledMealEstimateSummary.widget`, `PrepTasks.screen`, `ScheduledMealScopePrompt.modal`, `SmartMealPlanner.screen`
- Dishes long-tail: `DishesNutritionUsageModal.widget`, `DishStepList.widget`, `DishIngredientList.widget`, `CookingHistory.widget`, `DishesDetail.screen`
- ShoppingList: `ShoppingListDetail.screen` (the screen wrapper, distinct from the converted `.widget`)

Note: these deferrals are scope/complexity statements, not difficulty judgments. The Home-module pickers are simple but lower-traffic and outside D-01's journey/high-traffic scope; D-06 explicitly permits a subset sweep with the remainder recorded. If a later phase widens the sweep, this disposition is the starting checklist.
</mob03_inventory_disposition>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> picker action | Confirm/picker sheets trigger deletes/exports/edits on local persisted data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | Confirm action moved from Modal `onOk` to a Sheet body Button | mitigate | Preserve exact original handler + add explicit `danger` styling on destructive confirms so the user still sees a deliberate destructive affordance; no auto-confirm. |
| T-05-04 | Repudiation | A converted confirm fires without the user's intent | accept | Sheet requires explicit CTA tap (no implicit onOk); same intent gate as before. |

No package installs, no network, no AppCopy/auth/data-shape changes — sheet-wrapper swap only.
</threat_model>

<verification>
- `yarn build` green
- Manual/e2e spot-check: a converted destructive confirm (e.g. delete) still requires an explicit tap and still performs the original action
- The full MOB-03 inventory disposition (Task 3) accounts for every inventoried file (converted-by-plan or deferred-with-reason)
</verification>

<success_criteria>
- All single-step pickers/confirmations in the listed long-tail files render via `@components/Sheet`
- Destructive confirms keep an explicit `danger` CTA
- Complex multi-step modals deliberately retained and recorded (D-06 discretion)
- The full app-wide MOB-03 inventory has an auditable per-file disposition (no silent omissions)
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 02)

New component usages: `import { Sheet } from "@components/Sheet"` added to the listed long-tail picker files; new `data-testid` `<feature>-sheet` hooks on converted surfaces.
New AppCopy keys: none (this plan touches no copy).
New file paths: none.
Deliberately retained: complex multi-step modals + out-of-scope long-tail pickers (recorded with reasons in SUMMARY) — not a gap, a D-06 discretion call enumerated in the inventory disposition.
</artifacts>

<output>
Create `.planning/phases/05-mobile-tuning-copy-rollout/05-02-SUMMARY.md` when done.
</output>
