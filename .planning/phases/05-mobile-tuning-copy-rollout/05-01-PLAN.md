---
phase: 05-mobile-tuning-copy-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Common/Copy/AppCopy.ts
  - src/Modules/MealPlanning/Screens/WizardIngredientStep.widget.tsx
  - src/Modules/MealPlanning/Screens/WizardPreferenceStep.widget.tsx
  - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
  - src/Modules/MealPlanning/Components/WizardProgress.tsx
  - src/Modules/MealPlanning/Screens/Wizard.screen.tsx
autonomous: true
requirements: [COPY-03, COPY-05, MOB-01, MOB-02]
must_haves:
  truths:
    - "Every user-facing string on the wizard journey screens is read from AppCopy, not an inline literal"
    - "The wizard empty-states (empty catalog, no preferences) render friendly AppCopy.emptyStates copy, never a blank or technical message"
    - "Primary journey CTAs are full-width in the bottom thumb zone and interactive controls are >= 44px tall"
  artifacts:
    - path: "src/Common/Copy/AppCopy.ts"
      provides: "Extended wizard namespace + emptyStates keys covering all journey strings"
      contains: "wizard:"
    - path: "src/Modules/MealPlanning/Screens/WizardResult.widget.tsx"
      provides: "Result step reading copy + empty-states from AppCopy"
      contains: "AppCopy"
  key_links:
    - from: "src/Modules/MealPlanning/Screens/WizardIngredientStep.widget.tsx"
      to: "src/Common/Copy/AppCopy.ts"
      via: "import { AppCopy } from '@common/Copy'"
      pattern: "AppCopy\\.wizard\\."
    - from: "src/Modules/MealPlanning/Screens/WizardResult.widget.tsx"
      to: "AppCopy.emptyStates"
      via: "empty-catalog block reads emptyStates copy"
      pattern: "AppCopy\\.emptyStates\\."
---

<objective>
Migrate every inline Vietnamese string on the guided meal-planning journey (the `src/Modules/MealPlanning/*` wizard screens) to reference the typed `AppCopy` source of truth, wire friendly empty-states from `AppCopy.emptyStates`, and finish the phone-first layout polish that Phase 4 deferred (thumb-zone CTAs already exist; this plan guarantees >= 44px touch targets on the interactive controls).

The journey screens are the mandatory copy-migration target (CONTEXT D-01) and the layout analog for the rest of the phase. Migrating them first establishes the `wizard`/`emptyStates` key surface that the voice-refinement plan (05-07) later rewords in place (D-03 order: migrate strings into AppCopy first, refine phrasing second).

Purpose: COPY-03 (journey scope), COPY-05 (friendly empty-states), MOB-01/MOB-02 (journey layout) — the milestone's "smooth on mobile + reads natural" metric, on the funnel a first-timer actually walks.
Output: Extended `wizard` + `emptyStates` namespaces in `AppCopy.ts`; 5 journey files reading copy via `@common/Copy` with no inline user-facing literals and >= 44px controls.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-tuning-copy-rollout/05-CONTEXT.md
@.planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md
@.planning/phases/04-wizard-ui-hero-entry/04-UI-SPEC.md
@src/Common/Copy/AppCopy.ts
@src/Common/Copy/Glossary.ts
@src/Common/Copy/index.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend AppCopy with the journey namespace surface</name>
  <files>src/Common/Copy/AppCopy.ts</files>
  <read_first>
    - src/Common/Copy/AppCopy.ts (current shape: `common`, `wizard`, `emptyStates`; conventions header lines 9-19)
    - src/Common/Copy/Glossary.ts (canonical terms: addAction.term="Thêm", skip.term="Tùy bạn", dish.term="Món", todaysMeal.term="Bữa hôm nay")
    - src/Modules/MealPlanning/Screens/WizardIngredientStep.widget.tsx (strings to cover)
    - src/Modules/MealPlanning/Screens/WizardPreferenceStep.widget.tsx (strings to cover)
    - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx (strings to cover)
    - src/Modules/MealPlanning/Components/WizardProgress.tsx (strings to cover)
  </read_first>
  <action>
    Extend the existing `wizard` namespace (do NOT create a parallel namespace) inside `AppCopy.ts` so every journey string has a key. Required leaf keys, named exactly, following the locked conventions (PascalCase const, `as const`, interpolation as single named-arg arrow functions):
    - `wizard.ingredientStepTitle` = "Bạn có sẵn nguyên liệu gì?"
    - `wizard.ingredientPickerTrigger` = "Chọn nguyên liệu"
    - `wizard.ingredientSheetTitle` = "Chọn nguyên liệu"
    - `wizard.selectedIngredients` = `(args: { count: number }) => \`Đã chọn ${args.count} nguyên liệu\``
    - `wizard.preferenceStepTitle` = "Bạn thích món kiểu nào?"
    - `wizard.preferencePickerTrigger` = "Chọn sở thích"
    - `wizard.preferenceSheetTitle` = "Sở thích"
    - `wizard.selectedPreferences` = `(args: { count: number }) => \`Đã chọn ${args.count} sở thích\``
    - `wizard.editAction` = "Sửa"  ·  `wizard.chooseAction` = "Chọn"
    - `wizard.continueAction` = "Tiếp tục"
    - `wizard.pickOtherDay` = "Chọn ngày khác"  ·  `wizard.daySheetTitle` = "Chọn ngày khác"  ·  `wizard.addToDay` = "Thêm vào ngày này"
    - `wizard.finishAction` = "Xong"
    - `wizard.matchFull` = "Đủ đồ"  ·  `wizard.matchPartial` = "Gần đủ"  ·  `wizard.matchLow` = "Cần mua"
    - `wizard.ingredientMatchSummary` = `(args: { matched: number; missing: number }) => \`${args.matched} đủ · ${args.missing} thiếu\``
    - `wizard.fullCatalogFallback` = "Chưa có món khớp — đây là vài gợi ý từ toàn bộ món của bạn"
    - `wizard.addedToTodayToast` = "Đã thêm vào thực đơn"
    - `wizard.progressStep` = `(args: { current: number; total: number }) => \`Bước ${args.current}/${args.total}\``
    - `wizard.backLabel` = "Quay lại" (or reuse `common.back` which already = "Quay lại" — prefer reusing `common.back`; only add `wizard.backLabel` if call sites need a distinct value)
    Reuse existing keys where they already match: `wizard.addToToday` ("Thêm vào hôm nai"→ already "Thêm vào hôm nay"), `common.skip` ("Tùy bạn"), `common.back` ("Quay lại").
    Extend `emptyStates` with the journey-specific empties: keep existing `noDishes`; add `emptyStates.emptyCatalogTitle` = "Chưa có món nào", `emptyStates.emptyCatalogBody` = "Thêm món đầu tiên để bắt đầu lên thực đơn.", `emptyStates.emptyCatalogCta` = "Thêm món đầu tiên", `emptyStates.noPreferences` = "Chưa có sở thích để chọn. Bạn có thể bỏ qua bước này."
    Keep phrasing as the CURRENT literal values verbatim (this plan migrates, it does not reword — rewording is 05-07). Use canonical glossary terms only where a NEW choice is required; do not swap "Thêm" for "Tạo".
    Do NOT inline any code fence in narrative — edit the const object directly.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'ingredientStepTitle|preferenceStepTitle|fullCatalogFallback|progressStep|emptyCatalogTitle' src/Common/Copy/AppCopy.ts` returns >= 5 matches
    - `yarn build` succeeds (the derived `CopyKey` union compiles — proves no malformed key)
    - All count/interpolation keys (`selectedIngredients`, `selectedPreferences`, `ingredientMatchSummary`, `progressStep`) are arrow functions taking a single named-arg object
    - No glossary `avoid` term introduced: `rg -n 'Tạo mới|Thêm mới|Bỏ qua' src/Common/Copy/AppCopy.ts` returns 0
  </acceptance_criteria>
  <done>AppCopy.ts contains an extended wizard + emptyStates surface covering every journey string, build-gate green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Swap journey inline literals to AppCopy + wire friendly empty-states</name>
  <files>src/Modules/MealPlanning/Screens/WizardIngredientStep.widget.tsx, src/Modules/MealPlanning/Screens/WizardPreferenceStep.widget.tsx, src/Modules/MealPlanning/Screens/WizardResult.widget.tsx, src/Modules/MealPlanning/Components/WizardProgress.tsx</files>
  <read_first>
    - src/Common/Copy/AppCopy.ts (the keys added in Task 1 — read after Task 1 lands)
    - src/Modules/MealPlanning/Screens/WizardIngredientStep.widget.tsx (full file — every literal migrates)
    - src/Modules/MealPlanning/Screens/WizardPreferenceStep.widget.tsx (full file)
    - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx (full file — empty-catalog block lines 124-149, fallback 170-174, toast 109)
    - src/Modules/MealPlanning/Components/WizardProgress.tsx (full file — "Bước N/M" line 73, "Quay lại" aria line 49)
  </read_first>
  <action>
    Add `import { AppCopy } from "@common/Copy";` to each of the 4 files. Replace every user-facing Vietnamese literal — both double-quoted attribute/string literals AND JSX text nodes between tags (the quoted-literal grep does NOT catch JSX text like `Tiếp tục` between `<Button>` tags; scan visually) — with the matching `AppCopy.*` reference from Task 1. Concrete swaps:
    - WizardIngredientStep: title -> `AppCopy.wizard.ingredientStepTitle`; the `Đã chọn N nguyên liệu` ternary -> `AppCopy.wizard.selectedIngredients({ count: selectedIds.length })` / `AppCopy.wizard.ingredientPickerTrigger`; "Sửa"/"Chọn" -> `AppCopy.wizard.editAction`/`AppCopy.wizard.chooseAction`; "Tiếp tục" -> `AppCopy.wizard.continueAction`; "Tùy bạn" -> `AppCopy.common.skip`; Sheet title "Chọn nguyên liệu" -> `AppCopy.wizard.ingredientSheetTitle`.
    - WizardPreferenceStep: "Quay lại" -> `AppCopy.common.back`; title -> `AppCopy.wizard.preferenceStepTitle`; the no-tags empty text -> `AppCopy.emptyStates.noPreferences` (COPY-05); `Đã chọn N sở thích` -> `AppCopy.wizard.selectedPreferences({ count: selectedTags.length })` / `AppCopy.wizard.preferencePickerTrigger`; "Sửa"/"Chọn", "Tiếp tục", "Tùy bạn"; Sheet title "Sở thích" -> `AppCopy.wizard.preferenceSheetTitle`.
    - WizardResult: empty-catalog title/body/cta -> `AppCopy.emptyStates.emptyCatalogTitle`/`emptyCatalogBody`/`emptyCatalogCta` (COPY-05 — keep the centered icon-stack, friendly, never a red error); the `${matched} đủ · ${missing} thiếu` -> `AppCopy.wizard.ingredientMatchSummary({...})`; "Đủ đồ"/"Gần đủ"/"Cần mua" -> `matchFull`/`matchPartial`/`matchLow`; "Thêm vào hôm nay" -> `AppCopy.wizard.addToToday`; "Chọn ngày khác" -> `AppCopy.wizard.pickOtherDay` (button) + `AppCopy.wizard.daySheetTitle` (Sheet title); fallback text -> `AppCopy.wizard.fullCatalogFallback`; "Xong" -> `AppCopy.wizard.finishAction`; "Thêm vào ngày này" -> `AppCopy.wizard.addToDay`; `message.success("Đã thêm vào thực đơn")` -> `AppCopy.wizard.addedToTodayToast`.
    - WizardProgress: `Bước ${current+1}/${total}` -> `AppCopy.wizard.progressStep({ current: current + 1, total })`; aria-label "Quay lại" -> `AppCopy.common.back`.
    Preserve all `data-testid` attributes byte-for-byte (e2e specs depend on them).
  </action>
  <verify>
    <automated>yarn build && yarn test --watchAll=false --testPathPattern='MealPlanning'</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 4 files imports from `@common/Copy`: `rg -l "from \"@common/Copy\"" src/Modules/MealPlanning/Screens/WizardIngredientStep.widget.tsx src/Modules/MealPlanning/Screens/WizardPreferenceStep.widget.tsx src/Modules/MealPlanning/Screens/WizardResult.widget.tsx src/Modules/MealPlanning/Components/WizardProgress.tsx` lists all 4
    - No leftover known literals: `rg -n 'Tiếp tục|Chọn nguyên liệu|Bạn thích món kiểu nào|Chưa có món nào|Đã thêm vào thực đơn|Bước ' src/Modules/MealPlanning/Screens src/Modules/MealPlanning/Components` returns 0 matches outside of comments
    - Empty-states wired: `rg -n 'AppCopy\.emptyStates\.' src/Modules/MealPlanning/Screens/WizardResult.widget.tsx src/Modules/MealPlanning/Screens/WizardPreferenceStep.widget.tsx` returns >= 3 matches
    - `data-testid="wizard-empty-catalog"` still present in WizardResult.widget.tsx
    - `yarn build` green and the existing `Wizard.screen.test.tsx` still passes
  </acceptance_criteria>
  <done>All journey screens render copy from AppCopy; empty-catalog and no-preferences states show friendly AppCopy copy.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phone-first touch-target polish on journey controls (MOB-01/MOB-02)</name>
  <files>src/Modules/MealPlanning/Components/WizardProgress.tsx, src/Modules/MealPlanning/Screens/WizardResult.widget.tsx</files>
  <read_first>
    - src/Modules/MealPlanning/Components/WizardProgress.tsx (backIconButtonStyle currently 40px, lines 20-27)
    - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx (ResultRow CTA stack lines 56-71; finish button 185-195)
    - .planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md (Layout tuning section — `ActionButton` defaults are 28px, NOT touch-sized; bump explicit width/height for >= 44px; thumb-zone idiom = full-width `Button size="large"` in bottom column Stack)
    - .planning/phases/04-wizard-ui-hero-entry/04-UI-SPEC.md (token/inline-style idiom; do not introduce a new framework — D-05)
  </read_first>
  <action>
    Bring journey interactive controls to >= 44px touch targets within the existing inline-style + antd-token idiom (no new responsive framework, D-05). The step screens (WizardIngredientStep/PreferenceStep) already use full-width `Button size="large"` bottom-stack CTAs (the thumb-zone analog) and `padding: "14px 16px"` (~46px) trigger rows — leave those, they already satisfy MOB-01/02. Concrete bumps:
    - WizardProgress `backIconButtonStyle`: change `width`/`height`/`minWidth` from 40 to 44 and pass `height={44}` to the `ActionButton` (keep `shape="circle"`, neutral `#595959`). This is the only sub-44px control on the journey chrome.
    - WizardResult `ResultRow` "Thêm vào hôm nay" / "Chọn ngày khác" buttons: these use `@components/Button` `Button` at default size (~32px). Add `size="large"` so they reach antd's ~40px, and keep `width: "100%"` (already full-width). Apply the same to the "Xong" finish button and the "Thêm vào ngày này" Sheet CTA.
    Do NOT restyle the compact 28px `ActionButton` rows elsewhere into primary CTAs — only the circle back-button is touch-bumped. No copy changes in this task (copy already migrated in Task 2).
  </action>
  <verify>
    <automated>yarn build && yarn test:e2e --grep 'cold-start|wizard'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'height: 44|height={44}' src/Modules/MealPlanning/Components/WizardProgress.tsx` returns >= 1 match; no remaining `height: 40` / `height={40}` in that file
    - WizardResult result-row + finish + day-sheet primary buttons carry `size="large"`: `rg -c 'size="large"' src/Modules/MealPlanning/Screens/WizardResult.widget.tsx` returns >= 3
    - No new import of any responsive/breakpoint library added (D-05): `rg -n 'react-responsive|useMediaQuery|breakpoint' src/Modules/MealPlanning` returns 0
    - `yarn build` green; cold-start wizard e2e still passes. A failing e2e run is NOT acceptable; only an absent e2e environment may be skipped, and the skip + reason must be recorded in SUMMARY
  </acceptance_criteria>
  <done>The journey back control is 44px and result-step CTAs are large/full-width; no responsive framework introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted state -> render | Wizard step/answers from the `personal` persisted root render into journey screens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Copy interpolation (`selectedIngredients`, `progressStep`) renders user/derived counts | accept | Values are numeric counts rendered as React text (auto-escaped); no `dangerouslySetInnerHTML`. No new sink introduced. |
| T-05-02 | Denial of Service | Corrupted persisted wizard step | accept | Already mitigated upstream (Wizard.screen clamps unknown step keys, T-04-06); this plan does not change the state path. |

No package installs, no network calls, no auth/data-handling changes in this plan — threat surface is copy/layout only.
</threat_model>

<verification>
- `yarn build` green (derived CopyKey union compiles — build-time copy-key safety holds)
- `yarn test --watchAll=false --testPathPattern='MealPlanning'` passes
- Journey cold-start e2e (`tests/e2e/`) still reaches a scheduled meal (NAV/journey not regressed)
</verification>

<success_criteria>
- Zero inline user-facing Vietnamese literals remain on the 5 journey files (quoted + JSX-text)
- Empty catalog + no-preferences render friendly AppCopy.emptyStates copy
- Journey back control >= 44px; result CTAs full-width large; no new responsive framework
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 01)

New AppCopy keys (under existing namespaces):
- `wizard.ingredientStepTitle`, `wizard.ingredientPickerTrigger`, `wizard.ingredientSheetTitle`, `wizard.selectedIngredients(fn)`, `wizard.preferenceStepTitle`, `wizard.preferencePickerTrigger`, `wizard.preferenceSheetTitle`, `wizard.selectedPreferences(fn)`, `wizard.editAction`, `wizard.chooseAction`, `wizard.continueAction`, `wizard.pickOtherDay`, `wizard.daySheetTitle`, `wizard.addToDay`, `wizard.finishAction`, `wizard.matchFull`, `wizard.matchPartial`, `wizard.matchLow`, `wizard.ingredientMatchSummary(fn)`, `wizard.fullCatalogFallback`, `wizard.addedToTodayToast`, `wizard.progressStep(fn)`
- `emptyStates.emptyCatalogTitle`, `emptyStates.emptyCatalogBody`, `emptyStates.emptyCatalogCta`, `emptyStates.noPreferences`

New component usages: `AppCopy` import added to WizardIngredientStep, WizardPreferenceStep, WizardResult, WizardProgress.
New file paths: none (all edits in place).
</artifacts>

<output>
Create `.planning/phases/05-mobile-tuning-copy-rollout/05-01-SUMMARY.md` when done.
</output>
