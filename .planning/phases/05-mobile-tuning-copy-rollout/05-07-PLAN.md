---
phase: 05-mobile-tuning-copy-rollout
plan: 07
type: execute
wave: 6
depends_on: [05-01, 05-03, 05-04, 05-05, 05-06]
files_modified:
  - src/Common/Copy/AppCopy.ts
autonomous: true
requirements: [COPY-04]
must_haves:
  truths:
    - "Every [ASSUMED] placeholder phrase in AppCopy is replaced with phrasing that reads natural to a local Vietnamese user"
    - "No English or technical-jargon leftovers remain in user-facing AppCopy values"
    - "Terminology obeys the glossary: one canonical term per concept, no synonym drift"
    - "Phrasing changes ripple to every consumer screen without re-touching screen files (single-file edit, D-03)"
  artifacts:
    - path: "src/Common/Copy/AppCopy.ts"
      provides: "Voice-refined Vietnamese copy across all migrated namespaces"
      contains: "AppCopy"
  key_links:
    - from: "src/Common/Copy/AppCopy.ts"
      to: "src/Common/Copy/Glossary.ts"
      via: "phrasing obeys COPY_GLOSSARY canonical terms (review-only, not imported)"
      pattern: "Bữa hôm nay|Tùy bạn|Hôm nay ăn gì"
---

<objective>
Refine the Vietnamese voice across the now-migrated `AppCopy` surface (COPY-04). Phase 1 seeded phrasing as `[ASSUMED]` placeholders, and plans 05-01/03/04/05/06 migrated all journey + high-traffic screens to read through `AppCopy`. Because copy is centralized (Phase 1 D-07), voice refinement is a single-file edit to `AppCopy.ts` that ripples to every consumer without re-touching screens (D-03). This plan runs LAST in the copy chain — after every string is migrated into the file (D-03 order: migrate first, reword second).

Per D-02 this is an iterative user-review loop: the executor proposes natural Vietnamese phrasing for the full `AppCopy` surface, the user reviews it, and the executor revises per feedback. The glossary (`Glossary.ts`) governs one term per concept; consult it to surface synonym drift before rewording.

Purpose: COPY-04 (all labels/descriptions read natural to a local Vietnamese user; no English/jargon leftovers).
Output: a voice-refined `AppCopy.ts` — same key surface, refined values — validated by user review at the phase verification gate.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-tuning-copy-rollout/05-CONTEXT.md
@.planning/phases/05-mobile-tuning-copy-rollout/05-PATTERNS.md
@src/Common/Copy/AppCopy.ts
@src/Common/Copy/Glossary.ts
@src/Common/Copy/index.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Glossary reconciliation + propose natural Vietnamese phrasing across AppCopy</name>
  <files>src/Common/Copy/AppCopy.ts</files>
  <read_first>
    - src/Common/Copy/AppCopy.ts (the FULL migrated surface after 05-01..05-06 — every namespace added by the cluster plans)
    - src/Common/Copy/Glossary.ts (canonical terms: todaysMeal "Bữa hôm nay" avoid "Bữa ăn"; dish "Món" avoid "Món ăn (in lists)"; addAction "Thêm" avoid "Tạo mới"/"Thêm mới"; skip "Tùy bạn" avoid "Bỏ qua"; heroPrompt "Hôm nay ăn gì?")
    - .planning/phases/05-mobile-tuning-copy-rollout/05-CONTEXT.md (D-02 review loop, D-03 reword-in-AppCopy)
  </read_first>
  <action>
    Edit ONLY `AppCopy.ts` values (never keys — keys are locked by the migration plans and the `CopyKey` build gate). For every leaf string and every interpolation arrow function, refine the value so it reads natural to a local Vietnamese household user:
    - Remove any English or technical-jargon leftovers (e.g. raw "Modal", "ID", "tags" surfaced to users; status strings like "Cần cập nhật" if they read as admin jargon — prefer warm household phrasing).
    - Enforce glossary canonical terms: use "Bữa hôm nay" (not "Bữa ăn"), "Món" in list contexts (not "Món ăn"), "Thêm" for all add/create verbs (not "Tạo"/"Tạo mới"/"Thêm mới"), "Tùy bạn" for skip (not "Bỏ qua"), "Hôm nay ăn gì?" for the hero. Reconcile any concept that drifted across namespaces during migration onto its single canonical term.
    - Keep interpolation arrow-function signatures byte-identical (same named args) so no dynamic value is dropped — reword only the template text around `${args.x}`.
    Do NOT import `Glossary.ts` at runtime (it is review-only, D-04) — consult it as a reference only. Do NOT add/remove/rename keys. Do NOT touch any screen file. Record in the SUMMARY a before/after table of the notable phrasing changes for the user's review.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - Key surface unchanged: `rg -c '^\s*\w+:' src/Common/Copy/AppCopy.ts` key count equals the count after 05-06 (values changed, keys not) — confirm `yarn build` still compiles the `CopyKey` union (proves no key added/removed/renamed)
    - No glossary `avoid` term present in user-facing values: `rg -n 'Tạo mới|Thêm mới|Bỏ qua|Bữa ăn hôm nay' src/Common/Copy/AppCopy.ts` returns 0
    - No obvious English/jargon leftover in values: `rg -ni 'modal|null|undefined|tag\b|id\b' src/Common/Copy/AppCopy.ts` returns only matches inside comments/keys, none inside user-facing string VALUES
    - All interpolation leaves remain arrow functions with unchanged arg names
    - `yarn build` green
  </acceptance_criteria>
  <done>AppCopy.ts values refined to natural Vietnamese, glossary-consistent, keys untouched, build green; before/after table in SUMMARY.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: User-review loop — revise phrasing per feedback</name>
  <files>src/Common/Copy/AppCopy.ts</files>
  <read_first>
    - src/Common/Copy/AppCopy.ts (the proposed phrasing from Task 1)
    - src/Common/Copy/Glossary.ts (re-check terms against any user-requested change)
  </read_first>
  <action>
    Present the Task 1 before/after phrasing table to the user for review (D-02: user reviews, Claude adjusts). Apply the user's feedback by editing the corresponding `AppCopy.ts` values. If the user introduces a new preferred term for a concept, update EVERY occurrence of that concept across all namespaces in one pass (single-file ripple, D-03) so terminology stays consistent, and note the new canonical term in the SUMMARY (the glossary is review-only and is not edited at runtime, but record the decision for a future glossary update). Repeat until the user signs off. Keep keys and interpolation signatures unchanged.
  </action>
  <verify>
    <human-check>
      User reviews the refined Vietnamese copy (before/after table from Task 1 + the rendered strings on the journey + high-traffic screens) and confirms it reads natural with no English/jargon leftovers and consistent terminology. Executor revises AppCopy.ts values per feedback and re-presents until the user approves. This is the COPY-04 acceptance gate, collected at the end-of-phase verification (human_verify_mode=end-of-phase).
    </human-check>
  </verify>
  <acceptance_criteria>
    - Every user-requested phrasing change is reflected in `AppCopy.ts` values (cross-check against the feedback list recorded in SUMMARY)
    - Terminology remains one-term-per-concept after the revision (no `avoid` terms reintroduced): `rg -n 'Tạo mới|Thêm mới|Bỏ qua' src/Common/Copy/AppCopy.ts` returns 0
    - `yarn build` green (keys + interpolation signatures intact)
    - SUMMARY records the user-approved final phrasing and any new canonical-term decisions
  </acceptance_criteria>
  <done>User has reviewed and approved the refined Vietnamese copy; AppCopy.ts reflects final phrasing; terminology consistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy author -> AppCopy.ts | Phrasing edits land in the single copy source consumed app-wide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Information Disclosure | A credential/secret accidentally typed into a copy value | mitigate | Glossary security note (Glossary.ts header) — never put a PAT/PIN in a term; reviewer scans values; grep for token-like strings before commit. |
| T-05-15 | Tampering | A reword silently changes an interpolation arg, dropping a dynamic value | mitigate | Acceptance criterion locks arrow-function signatures; `yarn build` TS2345 gate catches a dropped named arg. |

No package installs, no network, no auth/data-shape changes — single-file copy reword only.
</threat_model>

<verification>
- `yarn build` green (CopyKey union + interpolation signatures intact)
- Human review (end-of-phase) confirms natural Vietnamese, no English/jargon, consistent terminology (COPY-04 gate)
- No screen file touched (single-file ripple, D-03) — `git diff --name-only` for this plan shows only `src/Common/Copy/AppCopy.ts`
</verification>

<success_criteria>
- All `[ASSUMED]` placeholder phrasing replaced with user-approved natural Vietnamese
- No English/technical-jargon leftovers; glossary terms consistent across namespaces
- Keys + interpolation signatures unchanged; change is a single-file ripple
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 07)

New AppCopy keys: none (values refined, keys locked).
New component usages: none (single-file copy edit; ripples to existing consumers).
New file paths: none.
Decisions recorded: any new user-preferred canonical term (for a future Glossary.ts update — review-only artifact, not runtime-wired).
</artifacts>

<output>
Create `.planning/phases/05-mobile-tuning-copy-rollout/05-07-SUMMARY.md` when done.
</output>
