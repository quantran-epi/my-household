---
phase: 09-ios-visual-baseline-safe-area-shell
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/Components/Layout/Content/Content.tsx
  - src/Components/Layout/Content/Content.test.tsx
  - src/Routing/Shell/CookingPill.tsx
  - src/Routing/Shell/BottomTabNavigator.tsx
  - tests/e2e/cooking-pill.spec.ts
autonomous: true
requirements: [IOS-02]

must_haves:
  truths:
    - "The scrollable content box subtracts both safe-area insets and uses one unit family, so its last row is never clipped under the iOS toolbar or home indicator"
    - "The cooking pill clears the safe-area-raised bottom nav and never rides into the home indicator on a notched device"
    - "All sticky bottom chrome (nav, pill, content math) routes through one safe-area convention (safeAreaInset + layout tokens)"
    - "The BottomTabNavigator reference values are read from tokens so the convention and the tokens stay equal"
  artifacts:
    - path: "src/Components/Layout/Content/Content.tsx"
      provides: "Token-driven dvh-primary height subtracting env(safe-area-inset-top/bottom) exactly once (D-05)"
      contains: "env(safe-area-inset-bottom)"
    - path: "src/Routing/Shell/CookingPill.tsx"
      provides: "Pill bottom rebased on bottomNavHeight + env(safe-area-inset-bottom) (D-06)"
      contains: "safeAreaInset.bottom"
    - path: "src/Components/Layout/Content/Content.test.tsx"
      provides: "Regression guard: height subtracts both insets, single unit family"
  key_links:
    - from: "src/Routing/Shell/CookingPill.tsx"
      to: "iosTokens.layout.bottomNavHeight + safeAreaInset.bottom"
      via: "bottom offset reads token + helper (was raw 76)"
      pattern: "safeAreaInset\\.bottom"
    - from: "src/Components/Layout/Content/Content.tsx"
      to: "iosTokens.layout"
      via: "chrome = headerHeight + bottomNavHeight token sum"
      pattern: "iosTokens\\.layout"
---

<objective>
Fix the two safe-area shell bugs that keep sticky bottom chrome from clearing the home indicator (IOS-02): the Content.tsx height-math mismatch (D-05) and the CookingPill missing-inset bug (D-06), and route the working BottomTabNavigator reference (D-07) through the same token + safeAreaInset convention so all three agree from one definition.

Purpose: With `viewport-fit=cover` already shipped, `100dvh` spans the full display including behind the notch/home-indicator. The content box must subtract each inset exactly once, and the pill must rebase its offset on the safe-area-raised nav height — otherwise content clips and the pill collides with the nav on notched devices. Delivers IOS-02.
Output: refactored Content.tsx, CookingPill.tsx, BottomTabNavigator.tsx; new Content.test.tsx; extended cooking-pill.spec.ts.
</objective>

<execution_context>
@/Users/admin/personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-ios-visual-baseline-safe-area-shell/09-CONTEXT.md
@.planning/phases/09-ios-visual-baseline-safe-area-shell/09-UI-SPEC.md
@.planning/phases/09-ios-visual-baseline-safe-area-shell/09-PATTERNS.md
@.planning/phases/09-ios-visual-baseline-safe-area-shell/09-RESEARCH.md
@.planning/phases/09-ios-visual-baseline-safe-area-shell/09-01-SUMMARY.md
@src/Components/Layout/Content/Content.tsx
@src/Routing/Shell/CookingPill.tsx
@src/Routing/Shell/BottomTabNavigator.tsx
@src/Components/FastOverlay/FastOverlay.tsx
@src/Components/SheetPicker/SheetActionMenu/SheetActionMenu.test.tsx
@tests/e2e/cooking-pill.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Content.tsx height math — subtract both insets, one unit family (D-05)</name>
  <read_first>
    - src/Components/Layout/Content/Content.tsx (full file — the HEADER_HEIGHT=76 / BOTTOM_NAV_HEIGHT=80 / CONTENT_PADDING=12 consts and the bug at lines 21-22: height calc(100vh - 156px) AND maxHeight calc(100dvh - 156px) — units disagree, neither subtracts insets)
    - src/Components/FastOverlay/FastOverlay.tsx (lines 113-142 — the `<style>` block cascade precedent: two same-key declarations, vh fallback first then dvh; and the padding-bottom calc(16px + env(safe-area-inset-bottom)) line)
    - src/Components/SheetPicker/SheetActionMenu/SheetActionMenu.test.tsx (lines 24-35 style-assertion idiom)
    - src/Theme/iosTokens.ts (layout.headerHeight/bottomNavHeight, surface.contentGradient, spacing.md — from Plan 01)
  </read_first>
  <behavior>
    - The content box height expression subtracts BOTH env(safe-area-inset-top) AND env(safe-area-inset-bottom), each exactly once (Pitfall 1 — no double-count, no extra padding on the same box)
    - The height and maxHeight (if both kept) use the SAME unit family — no 100vh-vs-100dvh mismatch (Pitfall 2)
    - dvh is the primary unit; vh appears only as a first-declared fallback (FastOverlay cascade), OR dvh-only inline is used (acceptable per iOS 15.4+ floor)
    - chrome subtracted === iosTokens.layout.headerHeight + iosTokens.layout.bottomNavHeight (76+80=156)
    - background still reads iosTokens.surface.contentGradient; padding still reads iosTokens.spacing.md (12, was CONTENT_PADDING)
  </behavior>
  <action>
    Refactor `src/Components/Layout/Content/Content.tsx` per D-05. Remove the local `HEADER_HEIGHT`/`BOTTOM_NAV_HEIGHT`/`CONTENT_PADDING` consts and import `{ iosTokens }` from `@theme`. Compute `chrome = iosTokens.layout.headerHeight + iosTokens.layout.bottomNavHeight`. The height MUST subtract both insets exactly once: `calc(100dvh - ${chrome}px - env(safe-area-inset-top) - env(safe-area-inset-bottom))` (Pitfall 1). Resolve the vh→dvh cascade using the FastOverlay `<style>`-block precedent (RESEARCH Pattern 3 recommended option a): emit a scoped `<style>` block targeting `#app-content` (the existing id) with a `100vh`-first fallback line then the `100dvh` winner — a single inline style object CANNOT carry two `height` keys (JS overwrites). Keep `data-testid="app-content"`. Keep `background: iosTokens.surface.contentGradient` and `padding: iosTokens.spacing.md`. Do NOT also pad the box with the inset (Pitfall 1 — subtract once, don't double-correct). Do NOT introduce CSS variables (D-01). If you choose dvh-only inline instead of the `<style>` block, document the tradeoff in the SUMMARY (loses vh fallback; acceptable per RESEARCH A1).
  </action>
  <verify>
    <automated>CI=true npx react-scripts test src/Components/Layout/Content --watchAll=false</automated>
  </verify>
  <done>Content height subtracts both insets once, single unit family, chrome from tokens; Content.test.tsx (Task authored alongside) green.</done>
</task>

<task type="auto">
  <name>Task 2: Author Content.test.tsx regression guard (IOS-02)</name>
  <read_first>
    - src/Components/Layout/Content/Content.tsx (the refactored output of Task 1)
    - src/Components/SheetPicker/SheetActionMenu/SheetActionMenu.test.tsx (lines 24-35 — render + style assertion idiom)
  </read_first>
  <action>
    Create `src/Components/Layout/Content/Content.test.tsx`. Render `<Content>child</Content>`, query the box (by `data-testid="app-content"` or the emitted scoped style rule), and assert: (1) the height/max-height expression contains BOTH `env(safe-area-inset-top)` AND `env(safe-area-inset-bottom)` (regression guard for Pitfall 1's missing insets), and (2) it does NOT mix `100vh` and `100dvh` in a single same-key inline declaration — i.e. the inline `style.height` uses one unit family (regression guard for Pitfall 2's mismatch). If the cascade is emitted via a `<style>` block, assert against the injected stylesheet text (querySelector('style') textContent contains the dvh expression with both insets). Mirror the SheetActionMenu jsdom style-read idiom.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test src/Components/Layout/Content --watchAll=false</automated>
  </verify>
  <done>Test asserts both insets present and single unit family; suite green; guards the exact Content.tsx bug this phase fixes.</done>
</task>

<task type="auto">
  <name>Task 3: Rebase CookingPill inset (D-06) + route BottomTabNavigator through tokens (D-07)</name>
  <read_first>
    - src/Routing/Shell/CookingPill.tsx (lines 55-75 — the floating pill style object: bottom:76 raw no-inset bug at :57, minHeight:52, borderRadius:999, zIndex:1000; note the decorative gradient/shadow/orange chrome and 700/750 weights are out-of-ramp by intent)
    - src/Routing/Shell/BottomTabNavigator.tsx (line 37 safe-area reference literal; lines 36/59/61/79/81/82/83/124 the layout/radius/shadow/color literals; note the verbatim-keep exceptions: fontSize:10 :172, fontWeight:500 :171, 5px border :154, 6px 8px dock padding :60, zIndex:900 :40)
    - src/Theme/iosTokens.ts + src/Theme/safeArea.ts (tokens + helper from Plan 01)
    - tests/e2e/cooking-pill.spec.ts (existing structure + testids: active-cooking-floating-button, bottom-tab-navigator)
    - playwright.config.ts (lines 38-39 mobile-safari project)
    - .planning/phases/09-ios-visual-baseline-safe-area-shell/09-RESEARCH.md (the cooking-pill boundingBox clearance example under "Code Examples")
  </read_first>
  <action>
    CookingPill.tsx (D-06): import `{ iosTokens, safeAreaInset }` from `@theme`. Replace the raw `bottom: 76` (:57) with `bottom: safeAreaInset.bottom(iosTokens.layout.bottomNavHeight)` so the pill clears the safe-area-raised nav + home indicator (Pitfall 3, RESEARCH A4). Replace `minHeight: 52` → `iosTokens.touchTarget.comfortable`; `borderRadius: 999` → `iosTokens.radius.pill`. LEAVE `zIndex: 1000` verbatim (documented z-ref, do not fork — D-03) and LEAVE the decorative orange gradient/shadow (:60,69) and 700/750 font weights (:91,94) verbatim (UI-SPEC Typography exceptions). Do NOT touch the two Modal blocks.
    BottomTabNavigator.tsx (D-07, self-reference): swap its own magic numbers to read tokens so the reference and tokens stay equal — `:37` `calc(8px + env(safe-area-inset-bottom))` → `safeAreaInset.bottom(iosTokens.spacing.sm)`; `:36` `minHeight:88` → `iosTokens.layout.bottomNavContainerMinHeight`; `:79` `height:52` → `iosTokens.touchTarget.comfortable`; radii `:59`(20)/`:81`(14)/`:124`(10) → `iosTokens.radius.xxl/lg/md`; `:61` shadow → `iosTokens.surface.shadowNav`; colors `:82,169`(#2f2545)/`:83,169`(#6b6478)/`:82`(rgba(116,54,220,0.10)) → `iosTokens.color.text/textMuted/accentFill`. LEAVE verbatim the exceptions listed in read_first (fontSize:10, fontWeight:500, 5px border, 6px 8px dock padding, zIndex:900).
    Extend tests/e2e/cooking-pill.spec.ts (IOS-02): add a test runnable under the `mobile-safari` project asserting the pill's bottom edge sits at or above the nav's top edge (no overlap) via `boundingBox()` on `active-cooking-floating-button` and `bottom-tab-navigator` — the RESEARCH "Code Examples" clearance pattern. Note in SUMMARY this runs via `--project=mobile-safari` and the real home-indicator value is non-zero only on notched hardware (manual-only per VALIDATION).
  </action>
  <verify>
    <automated>CI=true npx react-scripts test src/Routing --watchAll=false 2>&1 | tail -5; npx tsc --noEmit 2>&1 | grep -iE "CookingPill|BottomTabNavigator" || echo "no shell type errors"</automated>
  </verify>
  <done>Pill bottom reads safeAreaInset.bottom(bottomNavHeight); nav reference reads tokens with string-equal safe-area output; e2e clearance test authored; jsdom + tsc clean.</done>
</task>

</tasks>

<artifacts_produced>
This plan consumes Plan 01's `iosTokens`/`safeAreaInset` and produces:
- Modified `src/Components/Layout/Content/Content.tsx` — dvh-primary height subtracting both insets, reads `iosTokens.layout`, `iosTokens.surface.contentGradient`, `iosTokens.spacing.md`
- New test `src/Components/Layout/Content/Content.test.tsx` — both-insets + single-unit-family regression guard
- Modified `src/Routing/Shell/CookingPill.tsx` — `bottom: safeAreaInset.bottom(iosTokens.layout.bottomNavHeight)`, `iosTokens.touchTarget.comfortable`, `iosTokens.radius.pill`
- Modified `src/Routing/Shell/BottomTabNavigator.tsx` — reads `safeAreaInset.bottom(iosTokens.spacing.sm)`, `iosTokens.layout.bottomNavContainerMinHeight`, `iosTokens.touchTarget.comfortable`, `iosTokens.radius.*`, `iosTokens.surface.shadowNav`, `iosTokens.color.*`
- Extended e2e `tests/e2e/cooking-pill.spec.ts` — pill-clears-nav boundingBox assertion on mobile-safari
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure client-side CSS layout (env()/dvh/calc) + token wiring + test additions. No auth, network, storage, input-handling, or crypto surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | safe-area/height style values | accept | All values are hardcoded compile-time constants and UA-resolved CSS env() insets; none derive from user input or network (RESEARCH Security Domain). No ASVS L1 control applies to static CSS layout. |

Security note: ASVS L1 reviewed — V2/V3/V4/V5/V6 all non-applicable (no auth/session/access/input/crypto surface). Threats none/low, stated explicitly per quality gate.
</threat_model>

<verification>
- `CI=true npx react-scripts test src/Components/Layout/Content src/Routing --watchAll=false` green.
- `npx tsc --noEmit` shows no new errors in Content/CookingPill/BottomTabNavigator.
- `npx playwright test tests/e2e/cooking-pill.spec.ts --project=mobile-safari` passes (or documented WebKit-binary fallback; real-device home-indicator check is manual-only per VALIDATION).
- Grep confirms `bottom: 76` no longer present in CookingPill.tsx and Content.tsx has no bare `100vh - 156px` height without insets.
</verification>

<success_criteria>
- Content height subtracts both insets once with one unit family (IOS-02 core fix).
- Cooking pill clears the raised nav + home indicator via the safe-area convention (IOS-02).
- Nav reference routes through the same tokens/helper so the convention is single-sourced (D-07).
</success_criteria>

<output>
Create `.planning/phases/09-ios-visual-baseline-safe-area-shell/09-03-SUMMARY.md` when done.
</output>
