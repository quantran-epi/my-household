---
phase: 09-ios-visual-baseline-safe-area-shell
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/App.tsx
  - src/Components/FastOverlay/FastOverlay.tsx
  - src/Components/SheetPicker/shared/SheetTrigger.tsx
  - src/Components/SheetPicker/SheetSelect/SheetSelect.tsx
  - src/Components/SheetPicker/shared/SheetTrigger.test.tsx
  - tests/e2e/sheet-picker.spec.ts
autonomous: true
requirements: [IOS-01, IOS-03]

must_haves:
  truths:
    - "The AntD ConfigProvider reads its primary color set, base font, and radius from the @theme token module (one definition feeds AntD surfaces incl. SheetDatePicker calendar)"
    - "The sheet surface (gradient, 18px top radius, ease curve) renders from tokens, not duplicated literals"
    - "The picker trigger and every sheet selectable row meet the 44px touch-target token, asserted in jsdom"
    - "No visual regression: every promoted value still equals its Phase 7/8 literal (11px trigger padding and 6px trigger radius preserved verbatim)"
  artifacts:
    - path: "src/App.tsx"
      provides: "ConfigProvider theme.token fed from iosTokens (D-04)"
      contains: "iosTokens.color.primary"
    - path: "src/Components/FastOverlay/FastOverlay.tsx"
      provides: "Sheet gradient/radius/ease read from iosTokens.surface/radius/motion (D-02)"
      contains: "iosTokens.surface.sheetGradient"
    - path: "src/Components/SheetPicker/shared/SheetTrigger.tsx"
      provides: "Trigger min-height/radius/type/colors from tokens (D-08, IOS-03)"
      contains: "iosTokens.touchTarget.min"
    - path: "src/Components/SheetPicker/SheetSelect/SheetSelect.tsx"
      provides: "Row min-height + control type from tokens (IOS-03)"
      contains: "iosTokens.touchTarget.min"
  key_links:
    - from: "src/App.tsx"
      to: "@theme"
      via: "import { iosTokens } feeding ConfigProvider token slots"
      pattern: "iosTokens\\.color\\.primary"
    - from: "src/Components/SheetPicker/shared/SheetTrigger.tsx"
      to: "iosTokens.touchTarget.min"
      via: "baseStyle.minHeight reads token (was literal 44)"
      pattern: "touchTarget\\.min"
---

<objective>
Wire the Wave 1 token module into the AntD ConfigProvider (D-04) and the Phase 7/8 sheet + sheet-picker layer (D-02), and pin the 44px thumb-zone bar as a token across the picker trigger and sheet rows with jsdom + WebKit assertions (D-08/D-09, IOS-03). This is a promotion + verification pass — no new components, no new visual language.

Purpose: Make the token module the single source of truth that AntD-rendered surfaces and hand-built sheet chrome both read from, and lock the already-met 44px bar so it cannot silently regress. Delivers IOS-01 (token application to the sheet-picker layer) and IOS-03 (≥44px enforcement on triggers + rows).
Output: refactored App.tsx, FastOverlay.tsx, SheetTrigger.tsx, SheetSelect.tsx; extended SheetTrigger.test.tsx + sheet-picker.spec.ts.
</objective>

<execution_context>
@/Users/admin/personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-ios-visual-baseline-safe-area-shell/09-CONTEXT.md
@.planning/phases/09-ios-visual-baseline-safe-area-shell/09-UI-SPEC.md
@.planning/phases/09-ios-visual-baseline-safe-area-shell/09-PATTERNS.md
@.planning/phases/09-ios-visual-baseline-safe-area-shell/09-RESEARCH.md
@.planning/phases/09-ios-visual-baseline-safe-area-shell/09-01-SUMMARY.md
@src/App.tsx
@src/Components/FastOverlay/FastOverlay.tsx
@src/Components/SheetPicker/shared/SheetTrigger.tsx
@src/Components/SheetPicker/SheetSelect/SheetSelect.tsx
@src/Components/SheetPicker/SheetActionMenu/SheetActionMenu.test.tsx
@tests/e2e/sheet-picker.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feed AntD ConfigProvider scalars from @theme (D-04)</name>
  <read_first>
    - src/App.tsx (lines 22-40 — the token + components blocks; note line 31 zIndexPopupBase:4000 and lines 33-40 the per-component zIndexPopup:4200 block that MUST stay untouched)
    - src/Theme/iosTokens.ts (the token names to read — created by Plan 01)
    - .planning/phases/09-ios-visual-baseline-safe-area-shell/09-PATTERNS.md (the App.tsx MODIFY section — exact slot→token mapping)
  </read_first>
  <action>
    In `src/App.tsx`, add `import { iosTokens } from '@theme';` and replace the hardcoded `theme.token` scalar literals with token reads per D-04: `colorPrimary: iosTokens.color.primary` (was "#7436dc"), `colorPrimaryHover: iosTokens.color.primaryHover`, `colorPrimaryActive: iosTokens.color.primaryActive`, `colorLink: iosTokens.color.primary`, `colorBorderSecondary: iosTokens.color.borderIdle`, `fontFamily: iosTokens.type.fontFamily`, `fontSize: iosTokens.type.body.fontSize` (18), and add `borderRadius: iosTokens.radius.md` (10 — D-04 discretion, RESEARCH A2/Open-Q2 recommend 10 to align AntD inputs/popups with the 6px trigger / 10px control family). LEAVE `locale={viVN}`, `zIndexPopupBase: 4000`, and the entire `components` per-component `zIndexPopup: 4200` block UNCHANGED (Phase 11 owns that cleanup — RESEARCH anti-pattern). Do NOT add a CSS-variable layer (D-01).
  </action>
  <verify>
    <automated>CI=true npx react-scripts test src/App --watchAll=false 2>&1 | tail -5; npx tsc --noEmit 2>&1 | grep -i "App.tsx" || echo "no App.tsx type errors"</automated>
  </verify>
  <done>App.tsx imports iosTokens; the six scalar slots + borderRadius read from tokens; zIndex blocks unchanged; tsc clean for App.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote sheet-picker + FastOverlay literals to tokens; pin the 44px bar (IOS-01/IOS-03)</name>
  <read_first>
    - src/Components/FastOverlay/FastOverlay.tsx (line 48 overlayMotionEase; lines 330-331 + 586-587 the duplicated 18px radius + sheet gradient; line 151 closeButton radius 10 — note lines 113-142 the `<style>` blocks must stay)
    - src/Components/SheetPicker/shared/SheetTrigger.tsx (lines 20-43 — PURPLE/BORDER_IDLE/ERROR consts + baseStyle: minHeight 44, padding '0 11px', borderRadius 6, fontSize 16/400/'22px')
    - src/Components/SheetPicker/SheetSelect/SheetSelect.tsx (lines 43-59 — rowBaseStyle: minHeight 44, padding '11px 16px', fontSize 16/400/'22px')
    - src/Components/SheetPicker/SheetActionMenu/SheetActionMenu.test.tsx (lines 24-35 min-height idiom; 46-48 hex/rgb dual-accept)
    - src/Theme/iosTokens.ts (token names from Plan 01)
  </read_first>
  <behavior>
    - SheetTrigger renders a button whose `style.minHeight` === `${iosTokens.touchTarget.min}px` (44px)
    - SheetTrigger button `style.borderRadius` === `${iosTokens.radius.sm}px` (6px) and padding stays '0 11px' verbatim
    - SheetSelect option rows render with `style.minHeight` === `44px` and padding stays '11px 16px' verbatim
    - FastOverlay sheet still renders the gradient and 18px top radius (value-identical post-refactor)
  </behavior>
  <action>
    Promote-what-exists (D-02, Pitfall 4 — values MUST stay identical, do NOT normalize):
    - FastOverlay.tsx: replace `const overlayMotionEase = "cubic-bezier(0.16, 1, 0.3, 1)"` with `iosTokens.motion.ease`; replace the sheet `background: "linear-gradient(180deg, #f5f0ff 0%, #ffffff 42%)"` (lines 331, 587) with `iosTokens.surface.sheetGradient`; replace the sheet top radius `"18px 18px 0 0"` (line 586) and drawer `"0 18px 18px 0"` (line 330) using `iosTokens.radius.xl` template strings; replace closeButton `borderRadius: 10` (line 151) with `iosTokens.radius.md`. KEEP the `<style>` blocks (lines 113-142) and the reduced-motion clamp verbatim — tokens do not remove them.
    - SheetTrigger.tsx: replace `PURPLE`/`BORDER_IDLE`/`ERROR` consts with `iosTokens.color.primary`/`.borderIdle`/`.destructive`; `minHeight: 44` → `iosTokens.touchTarget.min` (IOS-03); `borderRadius: 6` → `iosTokens.radius.sm`; `fontSize: 16, fontWeight: 400, lineHeight: '22px'` → spread from `iosTokens.type.control`. KEEP `padding: '0 11px'` verbatim (AntD-input parity exception, UI-SPEC Spacing exceptions).
    - SheetSelect.tsx: `rowBaseStyle.minHeight: 44` → `iosTokens.touchTarget.min`; the control type triple → `iosTokens.type.control`. KEEP `padding: '11px 16px'` verbatim.
    - Extend SheetTrigger.test.tsx: add a test asserting `btn.style.minHeight` === `` `${iosTokens.touchTarget.min}px` `` and `btn.style.borderRadius` === `` `${iosTokens.radius.sm}px` `` (mirror the SheetActionMenu.test.tsx min-height idiom; use hex/rgb dual-accept for any color assertion).
    Do NOT touch SheetActionMenu.tsx behavior (its 44 already tested) and do NOT introduce CSS variables (D-01).
  </action>
  <verify>
    <automated>CI=true npx react-scripts test src/Components/SheetPicker src/Components/FastOverlay --watchAll=false</automated>
  </verify>
  <done>Trigger + row min-heights read the token and assert at 44px; gradient/radius/ease read from tokens; 11px/16px paddings + 6px radius preserved; SheetPicker + FastOverlay suites green.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the mobile-safari e2e to assert ≥44px rendered touch targets (IOS-03)</name>
  <read_first>
    - tests/e2e/sheet-picker.spec.ts (existing structure, how it opens a picker and the testids/selectors it uses)
    - playwright.config.ts (lines 38-39 — the `mobile-safari` project, devices['iPhone 13'])
    - .planning/phases/09-ios-visual-baseline-safe-area-shell/09-RESEARCH.md (the Playwright boundingBox example under "Code Examples")
  </read_first>
  <action>
    Extend `tests/e2e/sheet-picker.spec.ts` with a test (runnable under the existing `mobile-safari` project) that opens a sheet picker, grabs the trigger and a sheet row via `boundingBox()`, and asserts each rendered `height` is `>= 44` (the touchTarget.min bar on a real WebKit/iPhone descriptor — IOS-03 D-09 scope: shared picker layer only). Reuse the existing spec's open/selector helpers; do NOT add a new picker call-site (Phases 10-11 own feature screens). If the `mobile-safari` WebKit binary is unavailable, the test must still be authored — note in the SUMMARY that it runs via `npx playwright test tests/e2e/sheet-picker.spec.ts --project=mobile-safari` and may require `npx playwright install webkit`.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/sheet-picker.spec.ts --project=mobile-safari 2>&1 | tail -15 || echo "WebKit binary may need: npx playwright install webkit"</automated>
  </verify>
  <done>Spec asserts trigger + row boundingBox height >= 44 on mobile-safari; passes when the WebKit binary is present (manual-fallback noted otherwise per VALIDATION).</done>
</task>

</tasks>

<artifacts_produced>
This plan consumes Plan 01's `@theme` exports and produces:
- Modified `src/App.tsx` — ConfigProvider `theme.token` fed from `iosTokens` (D-04)
- Modified `src/Components/FastOverlay/FastOverlay.tsx` — `iosTokens.surface.sheetGradient`, `iosTokens.radius.xl/md`, `iosTokens.motion.ease`
- Modified `src/Components/SheetPicker/shared/SheetTrigger.tsx` — reads `iosTokens.touchTarget.min`, `iosTokens.radius.sm`, `iosTokens.type.control`, `iosTokens.color.*`
- Modified `src/Components/SheetPicker/SheetSelect/SheetSelect.tsx` — reads `iosTokens.touchTarget.min`, `iosTokens.type.control`
- Extended test `src/Components/SheetPicker/shared/SheetTrigger.test.tsx` — 44px + 6px-radius assertions
- Extended e2e `tests/e2e/sheet-picker.spec.ts` — boundingBox ≥44 on mobile-safari
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure client-side inline-style token wiring + test additions. No auth, network, storage, input-handling, or crypto surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Tampering | ConfigProvider/sheet style values | accept | All values are hardcoded compile-time constants read from @theme; none derive from user input or network (RESEARCH Security Domain). No ASVS L1 control applies to static CSS literals. |

Security note: ASVS L1 reviewed — V2/V3/V4/V5/V6 all non-applicable (no auth/session/access/input/crypto surface). Threats none/low, stated explicitly per quality gate.
</threat_model>

<verification>
- `CI=true npx react-scripts test src/App src/Components/SheetPicker src/Components/FastOverlay --watchAll=false` green.
- `npx tsc --noEmit` shows no new errors in the modified files.
- `npx playwright test tests/e2e/sheet-picker.spec.ts --project=mobile-safari` passes (or documented WebKit-binary fallback).
- Grep confirms no orphan literals remain: the sheet gradient and `minHeight: 44` literals in the modified files now read tokens.
</verification>

<success_criteria>
- AntD ConfigProvider + sheet/picker layer render from the token module (IOS-01).
- Trigger + sheet rows assert at the 44px token in jsdom and ≥44px on WebKit (IOS-03).
- No visual regression: promoted values equal Phase 7/8 literals; 11px/16px paddings + 6px radius preserved.
</success_criteria>

<output>
Create `.planning/phases/09-ios-visual-baseline-safe-area-shell/09-02-SUMMARY.md` when done.
</output>
