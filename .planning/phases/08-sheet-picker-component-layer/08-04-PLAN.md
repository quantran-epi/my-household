---
phase: 08-sheet-picker-component-layer
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/Components/SheetPicker/SheetMultiSelect/SheetMultiSelect.tsx
  - src/Components/SheetPicker/SheetMultiSelect/SheetMultiSelect.test.tsx
  - src/Components/SheetPicker/SheetMultiSelect/SheetMultiSelect.formbind.test.tsx
  - src/Components/SheetPicker/SheetMultiSelect/index.ts
autonomous: true
requirements: [PICK-03, PICK-04, PICK-08]
must_haves:
  truths:
    - "Multi-select rows are checkboxes; the sheet stays open while toggling"
    - "The 'Xong (N)' primary button commits the draft via onChange and dismisses, showing the selected count"
    - "Cancelling (Hủy) reverts to the values the sheet had when opened — the draft is discarded"
    - "While the draft is dirty the sheet is maskClosable=false so an accidental drag can't lose edits"
    - "A SheetMultiSelect inside an AntD Form submits the committed array unchanged"
  artifacts:
    - path: "src/Components/SheetPicker/SheetMultiSelect/SheetMultiSelect.tsx"
      provides: "multi-select sheet picker (checkbox draft + Xong/Hủy commit-revert)"
      exports: ["SheetMultiSelect", "SheetMultiSelectProps"]
  key_links:
    - from: "src/Components/SheetPicker/SheetMultiSelect/SheetMultiSelect.tsx"
      to: "useSheetPickerField draft + dirty"
      via: "draft state machine + maskClosable={!dirty}"
      pattern: "useSheetPickerField|maskClosable"
    - from: "src/Components/SheetPicker/SheetMultiSelect/SheetMultiSelect.tsx"
      to: "@components/Sheet SheetActions"
      via: "sticky Xong/Hủy footer"
      pattern: "SheetActions"
---

<objective>
Build `SheetMultiSelect` (PICK-03/04, D-06): checkbox rows, the sheet STAYS open, edits a local draft seeded from `value` on open. A sticky "Xong (N)" primary button commits the draft via `onChange` and dismisses; "Hủy" (or drag/backdrop dismiss) reverts to the values-on-open by discarding the draft. While the draft is dirty, `maskClosable={false}` so an accidental drag-dismiss springs back instead of losing edits (Phase 7 D-04). Controlled and AntD-`Form`-bindable (PICK-08). Composes the Wave-1 shared primitives + `SheetActions`.

Purpose: Deliver the multi-select/tags parity surface with the iOS settings-style commit/revert semantics.
Output: `src/Components/SheetPicker/SheetMultiSelect/` with component, behavior test, form-binding test, and folder barrel.
</objective>

<execution_context>
@/Users/admin/personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-sheet-picker-component-layer/08-RESEARCH.md
@.planning/phases/08-sheet-picker-component-layer/08-PATTERNS.md
@.planning/phases/08-sheet-picker-component-layer/08-UI-SPEC.md
@.planning/phases/08-sheet-picker-component-layer/08-01-SUMMARY.md
@src/Components/Form/Select/Select.tsx
@src/Components/Sheet/Sheet.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SheetMultiSelect component (checkbox draft + Xong/Hủy commit-revert)</name>
  <read_first>
    - src/Components/SheetPicker/shared/useSheetPickerField.ts (draft/dirty/commit/cancel from Plan 01)
    - src/Components/SheetPicker/shared/SheetTrigger.tsx + optionLabel.ts (trigger + multi summary from Plan 01)
    - src/Components/FastOverlay/FastOverlay.tsx (SheetActions equal-flex sticky row ~636-650; maskClosable→dragDecision spring-back)
    - src/Components/Form/Select/Select.tsx (multi mode parity, _isMultiSelectMode, "Đã chọn (N)" panel wording)
    - .planning/phases/08-sheet-picker-component-layer/08-UI-SPEC.md (§SheetMultiSelect — checkbox rows #7436dc, Xong (N), Đã chọn (N) strip, maskClosable rule)
  </read_first>
  <behavior>
    - Toggling a checkbox row mutates the local draft only; the sheet stays open (no onChange yet)
    - The primary button label reflects the draft count: "Xong (3)" for 3, "Xong" for 0
    - Clicking "Xong (N)" calls onChange(draft array) once and closes
    - Clicking "Hủy" closes and never calls onChange; reopening shows the original values
    - While draft differs from value-on-open, the Sheet receives maskClosable={false}
    - The closed trigger shows "first +N" / "N đã chọn" summary
  </behavior>
  <action>
    Create `SheetMultiSelect.tsx` exporting `SheetMultiSelect` and `SheetMultiSelectProps`. Props mirror the AntD multi/tags wrapper (D-01): `value` (array), `onChange`, `options`, `showSearch`, `allowClear`, `placeholder`, `disabled`, plus injected `id`/`status` (D-02). Use `useSheetPickerField` for `open` + `draft`/`dirty`/`commit`/`cancel` (RESEARCH §Pattern 3 — draft seeded from `value` on each open, never persisted). Render `SheetTrigger` with `getTriggerSummary({ value, options, mode:'multiple' })` summary (first label `+N` / "N đã chọn", D-04) and `onClear={() => onChange?.(undefined)}` when `allowClear`. On open → `<Sheet open onClose={cancel} title={placeholder or 'Chọn'} maskClosable={!dirty}>` from `@components/Sheet` — pass `maskClosable={!dirty}` so an accidental drag-dismiss on a dirty draft springs back (Pitfall 2 / Phase 7 D-04). NEVER pass explicit `zIndex` (RESEARCH anti-pattern). In the sheet body: optional `Đã chọn (N)` summary strip at top (`#f8fafc`, 14px/600 `#64748b`, mirror Select.tsx wording); checkbox rows using AntD `<Checkbox>` (free `aria-checked` — Pitfall 6) + label, `min-height:44`, `padding:'11px 16px'`, checked fill `#7436dc`; toggling mutates `draft` via `setDraft` only (sheet stays open). Same search rule/threshold as SheetSelect (`options.length >= 8` OR `showSearch`, diacritic filter from `normalizeDiacritics`). Sticky footer via Phase-7 `SheetActions`: primary `Xong (N)` (`#7436dc` fill, white 16px/600, N = draft count, plain "Xong" when 0) → `commit()` (calls `onChange(draft)` + close); secondary `Hủy` → `cancel()` (close, draft discarded, never calls `onChange`). per PICK-03/04, D-06. Single root element so Form.Item child-clone binds (PICK-08).
    Add folder barrel `SheetMultiSelect/index.ts` re-exporting component + props type.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker/SheetMultiSelect/SheetMultiSelect.test</automated>
  </verify>
  <acceptance_criteria>
    - Toggling rows does NOT call onChange; sheet stays mounted (RTL: onChange spy not called until Xong).
    - "Xong (N)" label reflects draft count; clicking it calls onChange with the full draft array once and closes.
    - "Hủy" closes without onChange; reopening re-seeds the original value (draft discarded).
    - The Sheet receives `maskClosable={false}` while dirty (assert the prop or a data attribute reflecting it).
    - SheetMultiSelect.test.tsx passes under jsdom.
  </acceptance_criteria>
  <done>SheetMultiSelect edits a draft with checkbox rows, commits on "Xong (N)" with a count, reverts on "Hủy", and protects a dirty draft from drag-dismiss; behavior test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SheetMultiSelect Form-binding test (PICK-08)</name>
  <read_first>
    - src/Components/Sheet/Sheet.test.tsx (jsdom RTL setup + Vietnamese-label queries)
    - src/Components/SmartForm/SmartFormItem/SmartFormItem.tsx (Form.Item child-clone)
    - .planning/phases/08-sheet-picker-component-layer/08-RESEARCH.md (§Validation Architecture; Pitfall 1)
  </read_first>
  <behavior>
    - A SheetMultiSelect inside <Form onFinish={spy} initialValues={{ tags: ['a'] }}> renders the bound count on the trigger
    - Opening, toggling two more options, clicking "Xong", then submitting calls onFinish with the committed array unchanged
    - Cancelling before submit leaves the form value at its initial array
  </behavior>
  <action>
    Create `SheetMultiSelect.formbind.test.tsx` (jsdom RTL): wrap `<SheetMultiSelect>` in a real AntD `<Form onFinish={spy}>` under `<Form.Item name="tags">`. Drive via `fireEvent.click` (jsdom-safe plain clicks per RESEARCH A3): open, toggle option checkboxes, click `Xong`, submit form, assert `onFinish` received `{ tags: [<committed array>] }` unchanged (array order/contents preserved). Add a second case: open, toggle, click `Hủy`, submit — assert `onFinish` received the original `initialValues` array (revert proven through the form, not just internal state). Guards Pitfall 1 (`strict:false` won't catch a lost binding). per PICK-08, D-12.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker/SheetMultiSelect</automated>
  </verify>
  <acceptance_criteria>
    - onFinish receives the committed array unchanged after "Xong" (RTL spy payload assertion).
    - After "Hủy", the form value remains the original array (revert visible through onFinish).
    - Both SheetMultiSelect test files pass.
  </acceptance_criteria>
  <done>SheetMultiSelect proven Form-bindable: submit collects the committed array unchanged and cancel preserves the original value (PICK-08, PICK-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user search input → in-memory filter | Query normalized + `includes`-matched; never eval'd or rendered as raw HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering (XSS) | option label / search echo render | mitigate | Labels/query render through JSX (auto-escape); no `dangerouslySetInnerHTML` |
| T-08-08 | DoS (ReDoS) | search filter | mitigate | `normalizeDiacritics` + `String.includes`; no user-controlled regex |
| T-08-SC | Tampering | dependencies | accept | Zero new runtime deps (locked) — no install task |
</threat_model>

<verification>
- `CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker/SheetMultiSelect` green (behavior + form-binding).
- Component passes `maskClosable={!dirty}` and no explicit `zIndex` to `<Sheet>`; commits via `SheetActions` "Xong".
</verification>

<success_criteria>
- SheetMultiSelect (PICK-03/04) edits a draft with checkbox rows, commits on "Xong (N)" with count, reverts on "Hủy", drag-protects a dirty draft, and binds to AntD Form (PICK-08) — proven by two co-located jsdom tests.
</success_criteria>

## Artifacts this phase produces (this plan)

New symbols/files created by this plan (exclude from drift verification — they are net-new):
- `SheetMultiSelect`, `SheetMultiSelectProps` — `src/Components/SheetPicker/SheetMultiSelect/SheetMultiSelect.tsx`
- Folder barrel `src/Components/SheetPicker/SheetMultiSelect/index.ts`
- Co-located tests `SheetMultiSelect.test.tsx`, `SheetMultiSelect.formbind.test.tsx`

<output>
Create `.planning/phases/08-sheet-picker-component-layer/08-04-SUMMARY.md` when done.
</output>
