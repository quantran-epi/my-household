---
phase: 08-sheet-picker-component-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Components/SheetPicker/shared/normalizeDiacritics.ts
  - src/Components/SheetPicker/shared/normalizeDiacritics.test.ts
  - src/Components/SheetPicker/shared/optionLabel.ts
  - src/Components/SheetPicker/shared/optionLabel.test.ts
  - src/Components/SheetPicker/shared/useSheetPickerField.ts
  - src/Components/SheetPicker/shared/SheetTrigger.tsx
  - src/Components/SheetPicker/shared/SheetTrigger.test.tsx
autonomous: true
requirements: [PICK-02, PICK-08]
must_haves:
  truths:
    - "A closed trigger renders the selected label (single), a count summary (multi), or the placeholder when empty"
    - "A trigger bound by Form.Item forwards its injected id and shows an error ring when status==='error'"
    - "Diacritic-insensitive filter matches Vietnamese labels (searching 'do' matches 'đỏ', 'ca' matches 'cà')"
    - "The picker field hook seeds a draft from value on each open and reports dirty for maskClosable gating"
  artifacts:
    - path: "src/Components/SheetPicker/shared/normalizeDiacritics.ts"
      provides: "NFD-strip + đ/Đ replace VN-insensitive normalize"
      exports: ["normalizeDiacritics"]
    - path: "src/Components/SheetPicker/shared/optionLabel.ts"
      provides: "value→label resolution + closed-trigger summary (D-04)"
      exports: ["getTriggerSummary", "getSelectedOptionDisplays"]
    - path: "src/Components/SheetPicker/shared/useSheetPickerField.ts"
      provides: "normalize injected value/onChange/id/status + open/draft/dirty/commit/cancel"
      exports: ["useSheetPickerField"]
    - path: "src/Components/SheetPicker/shared/SheetTrigger.tsx"
      provides: "AntD-input-styled closed trigger forwarding id/status/disabled + clear ×"
      exports: ["SheetTrigger"]
  key_links:
    - from: "src/Components/SheetPicker/shared/SheetTrigger.tsx"
      to: "injected id / status"
      via: "props forwarded to root button (id, aria-invalid)"
      pattern: "aria-invalid|status"
    - from: "src/Components/SheetPicker/shared/optionLabel.ts"
      to: "Form/Select.tsx helpers"
      via: "lifted _getSelectedOptionDisplays shape"
      pattern: "getSelectedOptionDisplays"
---

<objective>
Build the four shared primitives every picker composes on: the diacritic-insensitive filter, the value→label/summary resolver, the field-normalization hook (value/onChange/id/status + open/draft/dirty), and the dedicated AntD-input-styled trigger. These encode the PICK-08 form-binding mechanism (D-02) and the closed-trigger contract (D-03/D-04) once, so the four pickers in Waves 2-3 are thin compositions.

Purpose: Centralize the load-bearing wiring (Form.Item child-clone consumption, draft state machine, trigger styling) so each picker stays small and parity is uniform.
Output: `src/Components/SheetPicker/shared/` with normalizeDiacritics, optionLabel, useSheetPickerField, SheetTrigger and co-located tests.
</objective>

<execution_context>
@/Users/admin/personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-sheet-picker-component-layer/08-RESEARCH.md
@.planning/phases/08-sheet-picker-component-layer/08-PATTERNS.md
@.planning/phases/08-sheet-picker-component-layer/08-UI-SPEC.md
@src/Components/Form/Select/Select.tsx
@src/Components/SmartForm/SmartFormItem/SmartFormItem.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Diacritic filter + label/summary resolver utilities</name>
  <read_first>
    - src/Components/Form/Select/Select.tsx (lines 50-111 — lift the label-resolution helpers near-verbatim)
    - .planning/phases/08-sheet-picker-component-layer/08-RESEARCH.md (§Pattern 4 normalizeDiacritics, §Pattern 1 trigger summary)
    - .planning/phases/08-sheet-picker-component-layer/08-UI-SPEC.md (§Copywriting Contract — trigger summary forms)
  </read_first>
  <behavior>
    - normalizeDiacritics('Đỏ') === 'do'; normalizeDiacritics('Cà chua').includes(normalizeDiacritics('ca')) === true
    - getTriggerSummary for a single value returns the matching option's label
    - getTriggerSummary for a multi value of 1 returns that label; of >1 returns "first +N" or "N đã chọn"
    - getTriggerSummary for empty/undefined value returns undefined (caller renders placeholder)
  </behavior>
  <action>
    Create `normalizeDiacritics.ts` exporting `normalizeDiacritics(s: string): string` exactly per RESEARCH §Pattern 4 — `s.normalize('NFD')`, strip combining marks with the `/[̀-ͯ]/g` range, then explicit `.replace(/đ/g,'d').replace(/Đ/g,'D')` (NFD alone misses đ/Đ — mandatory), then `.toLowerCase()`. No regex built from user input (ReDoS-safe; filter uses `String.includes`).
    Create `optionLabel.ts` by lifting `_renderOptionLabel`, `_isMultiSelectMode`, `_getOptionValue`, `_normalizeSelectedValues`, `_optionValueKey`, `_flattenOptions`, and `_getSelectedOptionDisplays` from `Form/Select.tsx` lines 50-111 near-verbatim (re-export `_getSelectedOptionDisplays` as `getSelectedOptionDisplays`, and the `SelectedOptionDisplay` type). Add a new exported `getTriggerSummary({ value, options, mode, children })` per D-04: single → `getSelectedOptionDisplays(...)[0]?.label`; multi (`_isMultiSelectMode`) → first label with `+N` suffix when count > 1, falling back to `N đã chọn` (copy from UI-SPEC) when no labels resolve; empty → `undefined`. `any` in helper signatures is acceptable (project is `strict:false`/`es5`, matches Select.tsx).
    Add co-located `normalizeDiacritics.test.ts` and `optionLabel.test.ts` covering the behaviors above (VN diacritic match incl. đ→d, single/multi/empty summaries).
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker/shared/normalizeDiacritics src/Components/SheetPicker/shared/optionLabel</automated>
  </verify>
  <acceptance_criteria>
    - `normalizeDiacritics` strips tone marks AND đ/Đ; test proves "do"→"đỏ" and "ca"→"cà chua" matches.
    - `getTriggerSummary` returns single label, multi "first +N"/"N đã chọn", and undefined for empty — asserted in optionLabel.test.ts.
    - Both test files pass under jsdom.
  </acceptance_criteria>
  <done>Filter and label/summary utilities exist with passing co-located tests; helpers mirror the Form/Select.tsx shapes so the closed-trigger summary reads identically to today's Select.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useSheetPickerField hook + SheetTrigger component</name>
  <read_first>
    - src/Routing/SheetGestureFixture.screen.tsx (lines 11-22 — triggerStyle input-box shape, ≥44px bar)
    - src/Components/SmartForm/SmartFormItem/SmartFormItem.tsx (the verified Form.Item child-clone pass-through — PICK-08 mechanism)
    - .planning/phases/08-sheet-picker-component-layer/08-RESEARCH.md (§Pattern 1, §Pattern 3 draft seed, anti-pattern: never pass explicit zIndex)
    - .planning/phases/08-sheet-picker-component-layer/08-UI-SPEC.md (§Shared — Trigger: colors, focus rings, clear ×, aria)
  </read_first>
  <behavior>
    - useSheetPickerField returns { open, setOpen, draft, setDraft, dirty, commit, cancel }; draft re-seeds from value on each open transition
    - dirty is false right after open, true after a draft edit, back to false when draft matches value
    - commit() calls the injected onChange with the draft then closes; cancel() closes without calling onChange
    - SheetTrigger forwards injected id to its root button and sets aria-invalid when status==='error'
    - clicking the clear × calls onClear and does NOT open the sheet (stops propagation)
  </behavior>
  <action>
    Create `useSheetPickerField.ts` exporting `useSheetPickerField({ value, onChange, id, status })`. Own `open` via `useState`. For multi/range consumers own `draft` seeded `useState(() => toArray(value))` and re-seed in `useEffect(() => { if (open) setDraft(toArray(value)); }, [open])` (never persist across opens — RESEARCH §Pattern 3). Expose `dirty = !sameSet(draft, toArray(value))`, `commit = () => { onChange?.(draft); setOpen(false); }`, `cancel = () => setOpen(false)`. Memoize the returned object with `useMemo`/`useCallback` per CONVENTIONS. Do NOT compute or pass any `zIndex` (Phase 7 `useResolvedOverlayZIndex` token-stacks automatically — RESEARCH anti-pattern).
    Create `SheetTrigger.tsx`: a dedicated `<button type="button">` styled as the AntD input box per UI-SPEC §Shared — `minHeight:44`, `padding:'0 11px'`, `border:'1px solid #d9d9d9'`, `borderRadius:6`, `background:'#fff'`, full width, left text 16px/400. Forward injected `id` to the button; set `aria-haspopup="dialog"`, `aria-expanded`, and `aria-invalid` when `status==='error'`. Error state: border `#ff4d4f` + focus ring `0 0 0 2px rgba(255,77,79,0.12)`; normal focus ring `0 0 0 2px rgba(116,54,220,0.12)`; placeholder text muted `#bfbfbf`; disabled background `#f5f5f5`, text `rgba(0,0,0,0.25)`. Trailing affordance: `CloseCircleFilled` clear × (`rgba(0,0,0,0.25)`, hover `rgba(0,0,0,0.45)`) when `allowClear` and a value exists — its onClick calls `onClear` and `event.stopPropagation()` so it does NOT open the sheet; otherwise the type's idle glyph (`DownOutlined` default, overridable via a `glyph` prop so SheetDatePicker can pass `CalendarOutlined`) in `#bfbfbf`. Props: `id`, `status`, `disabled`, `placeholder`, `summary` (the resolved label node), `allowClear`, `onClear`, `onOpen`, `glyph`. per D-03/D-04/D-02.
    Add `SheetTrigger.test.tsx` (jsdom RTL) asserting the behaviors above.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker/shared/SheetTrigger</automated>
  </verify>
  <acceptance_criteria>
    - SheetTrigger root button carries the forwarded `id` and `aria-invalid="true"` when `status==='error'` (RTL assertion).
    - Clicking the clear × fires `onClear` and does NOT fire `onOpen` (stopPropagation verified).
    - Disabled trigger does not fire `onOpen`.
    - useSheetPickerField: draft re-seeds on open, `dirty` flips with edits, `commit` calls onChange + closes, `cancel` closes without onChange (covered in SheetTrigger.test.tsx or a sibling test).
  </acceptance_criteria>
  <done>The hook and trigger encode the form-binding + draft + closed-state contract; tests prove id/status forwarding, clear-without-open, and draft dirty/commit/cancel. No explicit zIndex is passed anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → in-memory filter | Search/label strings are filtered client-side only; never eval'd, never rendered as raw HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering (XSS) | optionLabel summary / trigger render | mitigate | Render labels through JSX (React auto-escapes); never `dangerouslySetInnerHTML`; never build DOM from a string |
| T-08-02 | DoS (ReDoS) | normalizeDiacritics filter | mitigate | Fixed `/[̀-ͯ]/g` range + `String.includes`; no user-controlled regex |
| T-08-SC | Tampering | dependencies | accept | Zero new runtime deps (locked) — no install task in this phase |
</threat_model>

<verification>
- `CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker/shared` is green.
- No file passes an explicit `zIndex` to `<Sheet>` (none added this plan; verified in Wave 2-3 consumers).
</verification>

<success_criteria>
- normalizeDiacritics, optionLabel (getTriggerSummary/getSelectedOptionDisplays), useSheetPickerField, SheetTrigger exist under `shared/` with passing co-located tests.
- SheetTrigger forwards id + renders error ring on status==='error' (PICK-08 foundation); diacritic filter matches VN labels (PICK-02 foundation).
</success_criteria>

## Artifacts this phase produces (this plan)

New symbols/files created by this plan (exclude from drift verification — they are net-new):
- `normalizeDiacritics` — `src/Components/SheetPicker/shared/normalizeDiacritics.ts`
- `getTriggerSummary`, `getSelectedOptionDisplays`, `SelectedOptionDisplay` — `src/Components/SheetPicker/shared/optionLabel.ts`
- `useSheetPickerField` — `src/Components/SheetPicker/shared/useSheetPickerField.ts`
- `SheetTrigger` — `src/Components/SheetPicker/shared/SheetTrigger.tsx`
- Co-located tests: `normalizeDiacritics.test.ts`, `optionLabel.test.ts`, `SheetTrigger.test.tsx`

<output>
Create `.planning/phases/08-sheet-picker-component-layer/08-01-SUMMARY.md` when done.
</output>
