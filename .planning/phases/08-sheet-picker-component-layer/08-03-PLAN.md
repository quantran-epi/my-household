---
phase: 08-sheet-picker-component-layer
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/Components/SheetPicker/SheetSelect/SheetSelect.tsx
  - src/Components/SheetPicker/SheetSelect/SheetSelect.test.tsx
  - src/Components/SheetPicker/SheetSelect/SheetSelect.formbind.test.tsx
  - src/Components/SheetPicker/SheetSelect/index.ts
autonomous: true
requirements: [PICK-01, PICK-02, PICK-08]
must_haves:
  truths:
    - "Tapping a single-select option row commits the value, shows a checkmark, and dismisses the sheet"
    - "A long option list shows a search field that filters diacritic-insensitively"
    - "allowClear renders a 'Bỏ chọn' row that clears the value and dismisses"
    - "A SheetSelect inside an AntD Form submits the picked value unchanged"
  artifacts:
    - path: "src/Components/SheetPicker/SheetSelect/SheetSelect.tsx"
      provides: "single-select sheet picker (tap→check→dismiss + search + clear)"
      exports: ["SheetSelect", "SheetSelectProps"]
  key_links:
    - from: "src/Components/SheetPicker/SheetSelect/SheetSelect.tsx"
      to: "shared primitives"
      via: "useSheetPickerField + SheetTrigger + getTriggerSummary + normalizeDiacritics"
      pattern: "useSheetPickerField|SheetTrigger|getTriggerSummary|normalizeDiacritics"
    - from: "src/Components/SheetPicker/SheetSelect/SheetSelect.tsx"
      to: "@components/Sheet"
      via: "Sheet host"
      pattern: "from ['\"]@components/Sheet['\"]"
---

<objective>
Build `SheetSelect` (PICK-01/02, D-05): the single-select sheet picker. Tap an option row → checkmark on that row → `onChange(value)` → sheet auto-dismisses. A search field auto-appears for long lists (threshold 8 or `showSearch`) and filters diacritic-insensitively. `allowClear` renders a leading "Bỏ chọn" row. The component is a controlled AntD-`Form`-bindable child (D-02) that composes the Wave-1 shared primitives.

Purpose: Deliver the highest-traffic picker (single Select is the most common site) with full parity and the native tap-to-dismiss feel.
Output: `src/Components/SheetPicker/SheetSelect/` with component, behavior test, form-binding test, and folder barrel.
</objective>

<execution_context>
@/Users/admin/personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-sheet-picker-component-layer/08-RESEARCH.md
@.planning/phases/08-sheet-picker-component-layer/08-PATTERNS.md
@.planning/phases/08-sheet-picker-component-layer/08-UI-SPEC.md
@.planning/phases/08-sheet-picker-component-layer/08-01-SUMMARY.md
@src/Components/Form/Select/Select.tsx
@src/Components/Sheet/Sheet.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SheetSelect component (tap→check→dismiss, search, clear)</name>
  <read_first>
    - src/Components/SheetPicker/shared/SheetTrigger.tsx (trigger contract from Plan 01)
    - src/Components/SheetPicker/shared/useSheetPickerField.ts (open/value normalize from Plan 01)
    - src/Components/SheetPicker/shared/optionLabel.ts + normalizeDiacritics.ts (summary + filter from Plan 01)
    - src/Components/Form/Select/Select.tsx (parity prop surface: value/onChange/options/mode/showSearch/allowClear/placeholder/disabled)
    - .planning/phases/08-sheet-picker-component-layer/08-UI-SPEC.md (§SheetSelect — row metrics, CheckOutlined #7436dc, search threshold 8, Bỏ chọn row)
  </read_first>
  <behavior>
    - Tapping an option row calls onChange(value) once and sets open=false
    - The selected row shows a trailing CheckOutlined and has aria-selected="true"
    - Search field renders when options.length >= 8 OR showSearch is passed; absent for short lists without showSearch
    - Typing "do" in search filters to an option labeled "Đỏ" (diacritic-insensitive)
    - With allowClear, a leading "Bỏ chọn" row calls onChange(undefined) and closes
    - Empty filtered result renders "Không tìm thấy" copy
  </behavior>
  <action>
    Create `SheetSelect.tsx` exporting `SheetSelect` and `SheetSelectProps`. Props mirror the AntD wrapper (D-01): `value`, `onChange`, `options`, `showSearch`, `allowClear`, `placeholder`, `disabled`, plus injected `id`/`status` (D-02). Compose: render `SheetTrigger` (closed state) passing `id`/`status`/`disabled`/`allowClear`, the `getTriggerSummary({ value, options })` result as `summary`, default `DownOutlined` glyph, and `onClear={() => onChange?.(undefined)}`. Use `useSheetPickerField` for `open`/`setOpen` (single-select needs no draft). On trigger open → `<Sheet open onClose={() => setOpen(false)} title={placeholder or 'Chọn'}>` from `@components/Sheet` (never pass explicit `zIndex` — RESEARCH anti-pattern). In the sheet body render option rows as real `<button role="option" aria-selected={isSelected}>`, `min-height:44`, `padding:'11px 16px'`, label 16px/400; selected row shows trailing `CheckOutlined` `#7436dc` and label `#7436dc` (a11y: `aria-selected`, not checkmark-only — Pitfall 6). Row tap → `onChange(option.value)` → `setOpen(false)` (PICK-01, D-05). When `allowClear`, render a leading "Bỏ chọn" row (label `#64748b`, 44px) → `onChange(undefined)` → close (PICK-02). Search: render an AntD `<Input>` with `SearchOutlined` prefix + `Tìm kiếm` placeholder, 16px text, pinned at TOP of the sheet body (Pitfall 4), shown when `options.length >= 8` OR `showSearch`; filter via `normalizeDiacritics(label).includes(normalizeDiacritics(query))`. Empty-filter state renders centered `Không tìm thấy` / `Thử từ khóa khác` at 14px `#64748b`; no-options-at-all renders `Chưa có lựa chọn`. Single root element returned so `Form.Item` child-clone binds cleanly (PICK-08, Pitfall 1).
    Add folder barrel `SheetSelect/index.ts` re-exporting component + props type.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker/SheetSelect/SheetSelect.test</automated>
  </verify>
  <acceptance_criteria>
    - Row tap fires onChange(value) exactly once and the sheet closes (RTL: sheet testid gone / onClose called).
    - Selected row carries `aria-selected="true"` and renders a CheckOutlined.
    - Search appears at length>=8 and filters "do"→"Đỏ"; absent at length<8 without showSearch.
    - "Bỏ chọn" row fires onChange(undefined) and closes.
    - SheetSelect.test.tsx passes under jsdom.
  </acceptance_criteria>
  <done>SheetSelect commits on tap with a checkmark, auto-dismisses, supports diacritic search and "Bỏ chọn" clear; behavior test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SheetSelect Form-binding test (PICK-08)</name>
  <read_first>
    - src/Components/Sheet/Sheet.test.tsx (jsdom RTL setup + Vietnamese-label query pattern)
    - src/Components/SmartForm/SmartFormItem/SmartFormItem.tsx (Form.Item child-clone — the binding under test)
    - .planning/phases/08-sheet-picker-component-layer/08-RESEARCH.md (§Validation Architecture — <Form onFinish> + fireEvent pattern; Pitfall 1)
  </read_first>
  <behavior>
    - A SheetSelect inside <Form onFinish={spy} initialValues={{ field: 'a' }}> renders the bound value on the trigger
    - Opening the sheet, tapping a different option, then submitting the Form calls onFinish with the new value unchanged
    - A required-rule validation fails when no value is selected and submit is attempted
  </behavior>
  <action>
    Create `SheetSelect.formbind.test.tsx` (jsdom RTL): wrap `<SheetSelect>` in an AntD `<Form onFinish={spy}>` under a `<Form.Item name="field">` (use the real `antd` Form, the binding mechanism is stock per `SmartFormItem`). Drive selection with `fireEvent.click` (plain click, not a gesture — jsdom-safe per RESEARCH A3): open the sheet, click an option row, submit the form, assert `onFinish` received `{ field: <picked value> }` unchanged. Add a second case wrapping with a `rules={[{ required: true }]}` Form.Item and asserting submit without a selection does not call `onFinish` (validation blocks). This is the only guard against Pitfall 1 (`strict:false` won't catch a lost binding). per PICK-08, D-12.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker/SheetSelect</automated>
  </verify>
  <acceptance_criteria>
    - onFinish receives the picked value unchanged after a tap-commit (RTL assertion on the spy payload).
    - A required Form.Item blocks submit (onFinish not called) when no value is chosen.
    - Both SheetSelect test files pass.
  </acceptance_criteria>
  <done>SheetSelect proven Form-bindable: submit collects its value unchanged and required validation fires (PICK-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user search input → in-memory filter | Query is normalized and `includes`-matched only; never eval'd or rendered as raw HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering (XSS) | option label / search echo render | mitigate | Labels/query render through JSX (auto-escape); no `dangerouslySetInnerHTML`; DOM never built from the search string |
| T-08-06 | DoS (ReDoS) | search filter | mitigate | `normalizeDiacritics` + `String.includes`; no user-controlled regex |
| T-08-SC | Tampering | dependencies | accept | Zero new runtime deps (locked) — no install task |
</threat_model>

<verification>
- `CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker/SheetSelect` green (behavior + form-binding).
- Component imports shared primitives from `../shared/*` and `Sheet` from `@components/Sheet`; passes no explicit `zIndex`.
</verification>

<success_criteria>
- SheetSelect (PICK-01/02) commits on tap with checkmark + auto-dismiss, diacritic search at threshold 8/showSearch, "Bỏ chọn" clear; and binds to AntD Form (PICK-08) — proven by two co-located jsdom tests.
</success_criteria>

## Artifacts this phase produces (this plan)

New symbols/files created by this plan (exclude from drift verification — they are net-new):
- `SheetSelect`, `SheetSelectProps` — `src/Components/SheetPicker/SheetSelect/SheetSelect.tsx`
- Folder barrel `src/Components/SheetPicker/SheetSelect/index.ts`
- Co-located tests `SheetSelect.test.tsx`, `SheetSelect.formbind.test.tsx`

<output>
Create `.planning/phases/08-sheet-picker-component-layer/08-03-SUMMARY.md` when done.
</output>
