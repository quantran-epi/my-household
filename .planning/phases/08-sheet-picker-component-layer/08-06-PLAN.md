---
phase: 08-sheet-picker-component-layer
plan: 06
type: execute
wave: 3
depends_on: ["08-02", "08-03", "08-04", "08-05"]
files_modified:
  - src/Components/SheetPicker/index.ts
  - src/Routing/SheetPickerFixture.screen.tsx
  - src/Routing/RootRoutes.ts
  - src/Routing/RootRouter.tsx
  - tests/e2e/sheet-picker.spec.ts
autonomous: true
requirements: [PICK-01, PICK-03, PICK-04]
must_haves:
  truths:
    - "The four pickers are importable from a single @components/SheetPicker barrel"
    - "A test-only fixture route mounts all four pickers deterministically (not linked from nav)"
    - "On a touch device, tapping a SheetSelect row commits and dismisses the sheet"
    - "On a touch device, a SheetMultiSelect commits on 'Xong' and reverts on 'Hủy'/drag-dismiss"
  artifacts:
    - path: "src/Components/SheetPicker/index.ts"
      provides: "barrel exporting all four pickers + types (@components/SheetPicker)"
      exports: ["SheetSelect", "SheetMultiSelect", "SheetDatePicker", "SheetActionMenu"]
    - path: "src/Routing/SheetPickerFixture.screen.tsx"
      provides: "test-only fixture mounting the four pickers"
      exports: ["SheetPickerFixtureScreen"]
    - path: "tests/e2e/sheet-picker.spec.ts"
      provides: "mobile-safari touch e2e for tap-commit + Xong/Hủy"
  key_links:
    - from: "src/Routing/RootRouter.tsx"
      to: "SheetPickerFixtureScreen"
      via: "lazy route at StaticRoutes.SheetPickerFixture"
      pattern: "SheetPickerFixture"
    - from: "tests/e2e/sheet-picker.spec.ts"
      to: "fixture route"
      via: "page.goto(FIXTURE_ROUTE) + getByTestId"
      pattern: "__sheet-picker-fixture"
---

<objective>
Wire the layer together: the top-level `@components/SheetPicker` barrel (D-11, mirrors the `@components/Sheet` barrel), a test-only fixture route mounting all four pickers (mirrors `SheetGestureFixture.screen.tsx`), and the `mobile-safari` touch e2e proving tap-to-commit (PICK-01), "Xong" commit + count (PICK-03), and "Hủy"/drag-dismiss revert (PICK-04) on real gestures. This is the last wave — it depends on all four pickers existing.

Purpose: Expose the single import surface Phases 10-11 swap call sites onto, and provide the gesture-level proof jsdom can't give (RESEARCH: gestures live in e2e, logic lives in units).
Output: `src/Components/SheetPicker/index.ts`, `src/Routing/SheetPickerFixture.screen.tsx`, fixture route registration, `tests/e2e/sheet-picker.spec.ts`.
</objective>

<execution_context>
@/Users/admin/personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-sheet-picker-component-layer/08-RESEARCH.md
@.planning/phases/08-sheet-picker-component-layer/08-PATTERNS.md
@.planning/phases/08-sheet-picker-component-layer/08-02-SUMMARY.md
@.planning/phases/08-sheet-picker-component-layer/08-03-SUMMARY.md
@.planning/phases/08-sheet-picker-component-layer/08-04-SUMMARY.md
@.planning/phases/08-sheet-picker-component-layer/08-05-SUMMARY.md
@src/Components/Sheet/index.ts
@src/Routing/SheetGestureFixture.screen.tsx
@src/Routing/RootRoutes.ts
@src/Routing/RootRouter.tsx
@tests/e2e/native-sheet.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SheetPicker barrel + test-only fixture route</name>
  <read_first>
    - src/Components/Sheet/index.ts (barrel shape to mirror — explicit named re-exports + export type)
    - src/Routing/SheetGestureFixture.screen.tsx (full file — fixture structure: data-testid open buttons + state toggles + components under test, NOT linked from nav)
    - src/Routing/RootRoutes.ts (lines 110-124 — StaticRoutes block, CrashTest/SheetGestureFixture pattern + threat comment style)
    - src/Routing/RootRouter.tsx (lines 51-57, 110-112 — React.lazy import + <Route path={StaticRoutes.X}> registration)
    - .planning/phases/08-sheet-picker-component-layer/08-PATTERNS.md (§barrel, §fixture analog)
  </read_first>
  <action>
    Create `src/Components/SheetPicker/index.ts` (D-11) mirroring `@components/Sheet`'s explicit-named shape: re-export `SheetSelect`, `SheetMultiSelect`, `SheetDatePicker` (consumers reach `SheetDatePicker.RangePicker` via the Object.assign static — barrel exports only the base), and `SheetActionMenu` from their folders, plus `export type` for each picker's props type. This resolves through the existing `@components/*` alias (tsconfig + craco + jest moduleNameMapper) — no alias config change needed.
    Create `src/Routing/SheetPickerFixture.screen.tsx` exporting `SheetPickerFixtureScreen`, copying the structure of `SheetGestureFixture.screen.tsx` verbatim: a root `data-testid="sheet-picker-fixture"`, `data-testid`'d open triggers, and the four pickers mounted with deterministic test fixtures — a SheetSelect (with allowClear + a >=8-option list to exercise search), a SheetMultiSelect (small option set), a SheetDatePicker (single, with a min/max via disabledDate), and a SheetActionMenu (with one normal + one danger action). Drive each via local `useState` for value/open. Include a threat comment matching the existing fixture style (test-only, NOT linked from any user-facing nav, serves no real data/PII — threat T-08-FIX).
    Register the route exactly as `SheetGestureFixture` is: add `SheetPickerFixture: '/__sheet-picker-fixture'` to `StaticRoutes` in `RootRoutes.ts` (with the test-only threat comment), add a `React.lazy` import in `RootRouter.tsx`, and add a `<Route path={RootRoutes.StaticRoutes.SheetPickerFixture} element={<React.Suspense fallback={<RouteLoadingFallback />}><SheetPickerFixtureScreen /></React.Suspense>} />` line beside the SheetGestureFixture route.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `@components/SheetPicker` exports `SheetSelect`, `SheetMultiSelect`, `SheetDatePicker`, `SheetActionMenu` + their prop types (tsc resolves the barrel).
    - `RootRoutes.StaticRoutes.SheetPickerFixture === '/__sheet-picker-fixture'`.
    - `RootRouter.tsx` registers the fixture route with a lazy import (grep confirms `SheetPickerFixture`).
    - Full unit suite for `src/Components/SheetPicker` stays green; `tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The barrel exposes all four pickers under @components/SheetPicker and the test-only fixture route mounts them deterministically, registered like the Phase 7 gesture fixture.</done>
</task>

<task type="auto">
  <name>Task 2: mobile-safari touch e2e (tap-commit + Xong/Hủy revert)</name>
  <read_first>
    - tests/e2e/native-sheet.spec.ts (full file — mobile-safari fixture-route pattern, appTest harness import, dragGrabberDown helper lines 30-48, FIXTURE_ROUTE/goto/getByTestId, post-gesture assertions)
    - src/Routing/SheetPickerFixture.screen.tsx (the data-testids created in Task 1)
    - .planning/phases/08-sheet-picker-component-layer/08-RESEARCH.md (§Validation Architecture e2e row; jsdom note — gestures in e2e only)
  </read_first>
  <action>
    Create `tests/e2e/sheet-picker.spec.ts` mirroring `native-sheet.spec.ts`: import the `{ expect, test }` harness from `./fixtures/appTest`, set `FIXTURE_ROUTE = '__sheet-picker-fixture'`, `page.goto(FIXTURE_ROUTE)` + assert the fixture root visible in `beforeEach`. Reuse the `dragGrabberDown` helper shape (copy it — e2e uses 2-space indent per CONVENTIONS). Tests (all on the default `mobile-safari` project, post-gesture assertions only — never mid-drag transforms):
    1. PICK-01: open the SheetSelect, tap an option row, assert the sheet is gone (`toHaveCount(0)`) and the trigger now shows the picked label.
    2. PICK-03: open the SheetMultiSelect, tap two checkbox rows, assert the sheet STAYS visible, assert the "Xong" button label reflects the count, tap "Xong", assert dismiss and the trigger shows the multi summary.
    3. PICK-04: open the SheetMultiSelect, toggle a row (dirty), tap "Hủy", assert dismiss and the trigger reverted to its original summary; then a second flow: toggle a row and `dragGrabberDown` past threshold, assert the dirty sheet springs back (still visible — maskClosable={false} protection) so the draft can't be lost via drag.
    Keep all selection/draft logic assertions in the jsdom units (already covered); this spec only proves the real-gesture path (RESEARCH jsdom note).
  </action>
  <verify>
    <automated>npm run test:e2e -- --project=mobile-safari sheet-picker.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - SheetSelect tap-commit: sheet dismisses and trigger reflects the picked value (PICK-01).
    - SheetMultiSelect: sheet stays open on toggle, "Xong (N)" reflects count and commits on tap (PICK-03).
    - SheetMultiSelect: "Hủy" reverts, and a drag-dismiss on a dirty draft springs back instead of losing edits (PICK-04).
    - `sheet-picker.spec.ts` is green on the `mobile-safari` project.
  </acceptance_criteria>
  <done>The mobile-safari e2e proves tap-to-commit, Xong commit-with-count, and Hủy/drag-dismiss revert on real gestures — the integration proof jsdom can't give.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-only fixture route → app router | Fixture is mounted at a static `/__`-prefixed route, not linked from nav, serving no real data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-FIX | Information disclosure | SheetPickerFixture route | accept | Test-only route (mirrors CrashTest/SheetGestureFixture), lazy-loaded, not linked from nav, serves no real data/PII — matches the existing fixture threat posture |
| T-08-11 | Tampering (XSS) | barrel re-exports | mitigate | Barrel only re-exports already-audited components; no new render surface |
| T-08-SC | Tampering | dependencies | accept | Zero new runtime deps (locked); Playwright already installed — no install task |
</threat_model>

<verification>
- `CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker` (full layer) green.
- `tsc --noEmit` passes (barrel + fixture type-check).
- `npm run test:e2e -- --project=mobile-safari sheet-picker.spec.ts` green.
- Fixture route is `/__`-prefixed, lazy, and not added to any nav menu.
</verification>

<success_criteria>
- `@components/SheetPicker` is the single import surface for all four pickers (D-11); the fixture route + mobile-safari e2e prove tap-commit (PICK-01), Xong commit+count (PICK-03), and Hủy/drag revert (PICK-04) on real gestures.
</success_criteria>

## Artifacts this phase produces (this plan)

New symbols/files created by this plan (exclude from drift verification — they are net-new):
- Top-level barrel `src/Components/SheetPicker/index.ts` (exports `SheetSelect`, `SheetMultiSelect`, `SheetDatePicker`, `SheetActionMenu` + prop types)
- `SheetPickerFixtureScreen` — `src/Routing/SheetPickerFixture.screen.tsx`
- `StaticRoutes.SheetPickerFixture` (`/__sheet-picker-fixture`) — `src/Routing/RootRoutes.ts`
- Route registration + lazy import in `src/Routing/RootRouter.tsx`
- `tests/e2e/sheet-picker.spec.ts`

<output>
Create `.planning/phases/08-sheet-picker-component-layer/08-06-SUMMARY.md` when done.
</output>
