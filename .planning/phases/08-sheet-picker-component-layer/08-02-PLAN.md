---
phase: 08-sheet-picker-component-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Components/SheetPicker/SheetActionMenu/SheetActionMenu.tsx
  - src/Components/SheetPicker/SheetActionMenu/SheetActionMenu.test.tsx
  - src/Components/SheetPicker/SheetActionMenu/index.ts
autonomous: true
requirements: [PICK-07]
must_haves:
  truths:
    - "An action menu renders full-width tappable rows grouped in one rounded surface"
    - "A destructive action row renders its label and icon in red (#ff4d4f)"
    - "A separate 'Hủy' button sits detached below the action group"
    - "Tapping an action row runs its onClick then dismisses the sheet"
  artifacts:
    - path: "src/Components/SheetPicker/SheetActionMenu/SheetActionMenu.tsx"
      provides: "iOS grouped action-sheet hosted in <Sheet> (D-10)"
      exports: ["SheetActionMenu", "SheetActionMenuProps", "SheetAction"]
  key_links:
    - from: "src/Components/SheetPicker/SheetActionMenu/SheetActionMenu.tsx"
      to: "@components/Sheet"
      via: "Sheet host + SheetActions detached Hủy"
      pattern: "from ['\"]@components/Sheet['\"]"
---

<objective>
Build `SheetActionMenu` (PICK-07, D-10): the iOS grouped action-sheet that replaces ~17 AntD `Dropdown` overflow menus in Phases 10-11. Full-width tappable rows in one rounded surface, destructive rows in red, and a visually detached "Hủy" button below the group. This is the one picker that is NOT Form-bound (it dispatches actions, it does not hold a value), so it has no dependency on the shared field primitives — it composes only `<Sheet>` + `SheetActions`. That makes it a Wave-1 sibling of the shared-primitives plan.

Purpose: Ship the action-menu surface early and in isolation since it is independent of the form-binding machinery.
Output: `src/Components/SheetPicker/SheetActionMenu/` with component, co-located test, and folder barrel.
</objective>

<execution_context>
@/Users/admin/personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-sheet-picker-component-layer/08-RESEARCH.md
@.planning/phases/08-sheet-picker-component-layer/08-PATTERNS.md
@.planning/phases/08-sheet-picker-component-layer/08-UI-SPEC.md
@src/Components/Sheet/index.ts
@src/Components/Sheet/Sheet.test.tsx
@src/Routing/SheetGestureFixture.screen.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SheetActionMenu component (grouped rows, danger red, detached Hủy)</name>
  <read_first>
    - src/Components/Sheet/index.ts (Sheet + SheetActions exports)
    - src/Components/FastOverlay/FastOverlay.tsx (Sheet props: open/onClose/title/data-testid/maskClosable; SheetActions equal-flex sticky row, ~lines 636-650)
    - .planning/phases/08-sheet-picker-component-layer/08-UI-SPEC.md (§SheetActionMenu — card surface, radius 14px, danger #ff4d4f, lg/24px gap to Hủy)
    - .planning/phases/08-sheet-picker-component-layer/08-RESEARCH.md (§Pattern 6)
  </read_first>
  <behavior>
    - Renders a row per action with its label; each row min-height 44px, full width
    - A row with danger:true renders label + icon in #ff4d4f
    - A row with an icon renders the icon leading the label
    - Clicking a row calls that action's onClick then calls onClose
    - The detached 'Hủy' button calls onClose and never runs an action
  </behavior>
  <action>
    Create `SheetActionMenu.tsx` exporting `SheetActionMenu` plus types `SheetActionMenuProps` and `SheetAction`. Props: `open`, `onClose`, `title?`, `actions: SheetAction[]`, `data-testid?`. `SheetAction = { key: string; label: React.ReactNode; icon?: React.ReactNode; danger?: boolean; onClick: () => void }` (union/literal style per CONVENTIONS, `danger` boolean flag). Host inside `<Sheet open onClose title>` from `@components/Sheet` — never pass an explicit `zIndex` (Phase 7 token-stacking, RESEARCH anti-pattern). Render the action rows in ONE rounded surface per UI-SPEC §SheetActionMenu: card `background:#fff` on the `#f8fafc`-tinted body, `borderRadius:14`, rows divided by `1px solid #f0f0f0`, each row a full-width `<button type="button">` `minHeight:44`, label 16px, optional leading icon with 16px gap; `danger` → label + icon color `#ff4d4f` (AntD `colorError`, weight stays 400). Each row's onClick runs `action.onClick()` then `onClose()`. Render "Hủy" as a SEPARATE detached button BELOW the group (`lg`/24px gap) via `SheetActions`, its own rounded surface, label 16px/600, calling `onClose`. Not Form-bound — no value/onChange/id/status. Per D-10.
    Add folder barrel `SheetActionMenu/index.ts` re-exporting the component + types (`export * from './SheetActionMenu'` or explicit named — match Sheet barrel shape).
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker/SheetActionMenu</automated>
  </verify>
  <acceptance_criteria>
    - A `danger` row's label element computed color is `#ff4d4f` (or rgb equivalent) — RTL/style assertion.
    - Clicking an action row fires its `onClick` once, then `onClose` once (order asserted via mock call order).
    - The "Hủy" button fires `onClose` and triggers no action `onClick`.
    - Each action row has `min-height:44px`.
  </acceptance_criteria>
  <done>SheetActionMenu renders grouped full-width rows, red destructive rows, and a detached Hủy; tapping a row dispatches then closes. Co-located test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| call-site → action dispatch | Action `onClick` callbacks are supplied by the call site; SheetActionMenu only invokes them, holds no privileged capability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering (XSS) | action label render | mitigate | Labels render through JSX (auto-escape); no `dangerouslySetInnerHTML` |
| T-08-04 | Elevation | destructive dispatch | accept | Menu does not self-confirm; the call site owns any destructive confirmation Sheet (UI-SPEC) — no privilege held here |
| T-08-SC | Tampering | dependencies | accept | Zero new runtime deps (locked) — no install task |
</threat_model>

<verification>
- `CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker/SheetActionMenu` is green.
- Component imports `Sheet`/`SheetActions` from `@components/Sheet`; passes no explicit `zIndex`.
</verification>

<success_criteria>
- SheetActionMenu (PICK-07) renders grouped full-width rows, red `danger` rows, a detached "Hủy", and dispatches+closes on row tap — proven by the co-located jsdom test.
</success_criteria>

## Artifacts this phase produces (this plan)

New symbols/files created by this plan (exclude from drift verification — they are net-new):
- `SheetActionMenu`, `SheetActionMenuProps`, `SheetAction` — `src/Components/SheetPicker/SheetActionMenu/SheetActionMenu.tsx`
- Folder barrel `src/Components/SheetPicker/SheetActionMenu/index.ts`
- Co-located test `SheetActionMenu.test.tsx`

<output>
Create `.planning/phases/08-sheet-picker-component-layer/08-02-SUMMARY.md` when done.
</output>
