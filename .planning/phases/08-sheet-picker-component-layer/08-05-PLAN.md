---
phase: 08-sheet-picker-component-layer
plan: 05
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/Components/SheetPicker/SheetDatePicker/SheetDatePicker.tsx
  - src/Components/SheetPicker/SheetDatePicker/SheetDatePicker.test.tsx
  - src/Components/SheetPicker/SheetDatePicker/SheetDatePicker.formbind.test.tsx
  - src/Components/SheetPicker/SheetDatePicker/index.ts
autonomous: true
requirements: [PICK-05, PICK-06, PICK-08]
must_haves:
  truths:
    - "The date sheet hosts AntD's own calendar panel inline (not floated by the z-index:4200 popup)"
    - "Picking a date commits a Dayjs value and auto-dismisses; min/max are honored via disabledDate"
    - "A 'Hôm nay' quick-action sets today clamped to min/max"
    - "showTime and date-range (RangePicker sub-export) work; range commits only on 'Xong'"
    - "A SheetDatePicker inside an AntD Form submits the Dayjs value unchanged"
  artifacts:
    - path: "src/Components/SheetPicker/SheetDatePicker/SheetDatePicker.tsx"
      provides: "date/datetime/range sheet picker hosting AntD panel; RangePicker sub-export"
      exports: ["SheetDatePicker", "SheetDatePickerProps"]
  key_links:
    - from: "src/Components/SheetPicker/SheetDatePicker/SheetDatePicker.tsx"
      to: "embedded AntD DatePicker panel"
      via: "open forced true + getPopupContainer into sheet body + z-index neutralized"
      pattern: "getPopupContainer"
    - from: "src/Components/SheetPicker/SheetDatePicker/SheetDatePicker.tsx"
      to: "RangePicker sub-export"
      via: "Object.assign(SheetDatePickerBase, { RangePicker })"
      pattern: "Object.assign"
---

<objective>
Build `SheetDatePicker` (PICK-05/06, D-08/D-09): host AntD's own `<DatePicker>`/`<RangePicker>` calendar panel INSIDE the sheet (forced `open` + `getPopupContainer` into the sheet body, z-index:4200 neutralized so the panel flows inline). Value stays a `Dayjs`. `disabledDate` enforces min/max; a "Hôm nay" quick-action sets today clamped to min/max; `showTime` and date-range work, with range committing only on "Xong" (a half-picked range can't escape). `RangePicker` is a sub-export. Controlled and AntD-`Form`-bindable (PICK-08).

This plan carries the de-floating spike flagged in RESEARCH Open Q1 / Assumption A1 — confirm `getPopupContainer` + `popupStyle`/`popupClassName` makes the panel render inline; if it resists, fall back to rc-picker `PickerPanel` (verified available). Either path stays within D-08.

Purpose: Deliver the date/datetime/range parity surface — the highest-risk picker (calendar embedding) — with AntD's panel doing the heavy lifting.
Output: `src/Components/SheetPicker/SheetDatePicker/` with component (+ RangePicker sub-export), behavior test, form-binding test, and folder barrel.
</objective>

<execution_context>
@/Users/admin/personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-sheet-picker-component-layer/08-RESEARCH.md
@.planning/phases/08-sheet-picker-component-layer/08-PATTERNS.md
@.planning/phases/08-sheet-picker-component-layer/08-UI-SPEC.md
@.planning/phases/08-sheet-picker-component-layer/08-01-SUMMARY.md
@src/Components/Form/DatePicker/DatePicker.tsx
@src/App.tsx
@src/Components/Sheet/Sheet.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-floating spike + single date/datetime SheetDatePicker</name>
  <read_first>
    - src/Components/Form/DatePicker/DatePicker.tsx (RangePicker sub-export via Object.assign; the popupStyle z-index:4200 hack this plan REVERSES)
    - src/App.tsx (global zIndexPopupBase:4000 + DatePicker.zIndexPopup:4200 + viVN locale + dayjs.locale('vi'))
    - src/Components/SheetPicker/shared/SheetTrigger.tsx + useSheetPickerField.ts (trigger + open state from Plan 01)
    - .planning/phases/08-sheet-picker-component-layer/08-RESEARCH.md (§Pattern 5 embedding snippet; Pitfall 3 z-index; Open Q1/A1 spike + rc-picker PickerPanel fallback)
    - .planning/phases/08-sheet-picker-component-layer/08-UI-SPEC.md (§SheetDatePicker — inline panel, Hôm nay accent, inputReadOnly, CalendarOutlined glyph)
  </read_first>
  <action>
    First, the de-floating spike (RESEARCH Open Q1 / A1, ~30 min): render AntD `<DatePicker>` inside the sheet body with `open` forced to the sheet's open state and `getPopupContainer={() => sheetBodyRef.current!}`; neutralize the global `z-index:4200` (set in App.tsx) with `popupStyle={{ position:'static', zIndex:'auto', boxShadow:'none' }}` and a scoped `popupClassName="sheet-date-popup"`. Confirm via the behavior test that the panel renders inline in the sheet body (Pitfall 3 — not floated top-left or above the backdrop). If `popupStyle` alone does not flow it inline, add a scoped injected `<style>` block (`.sheet-date-popup .ant-picker-dropdown { position: static; ... }`) per the injected-style convention; if the popup still resists, fall back to rc-picker's exported `PickerPanel` (verified available, RESEARCH Alternatives) — document which path was taken in the SUMMARY. Either path stays within D-08.
    Then create `SheetDatePicker.tsx` (the `SheetDatePickerBase`). Props mirror the AntD DatePicker wrapper (D-01): `value`, `onChange`, `picker`, `showTime`, `disabledDate`, `format`, `placeholder`, `disabled`, plus injected `id`/`status` (D-02). Render `SheetTrigger` with the formatted-date summary (D-04, use `format` or a sensible default) and `CalendarOutlined` glyph; `useSheetPickerField` owns `open`. On open → `<Sheet open onClose title={placeholder or 'Chọn ngày'}>` from `@components/Sheet` (NEVER explicit `zIndex`); render the embedded panel in `sheetBodyRef`. Value stays `Dayjs` — NEVER convert to moment (PICK-05, Pitfall 5). `inputReadOnly` to suppress the iOS keyboard. Single date/datetime: commit on panel `onChange` → auto-dismiss (single-select feel, D-05). For `showTime`, let the confirm fire `onChange` then close (prefer the unified flow over AntD's inline OK footer). Add a prominent `Hôm nay` quick-action (16px/600 accent `#7436dc`) → `dayjs()` clamped to min/max (`disabledDate(today) ? nearest allowed : today`) (D-09, PICK-05). Leave the `RangePicker` sub-export + `Object.assign` to Task 2 (define `SheetDatePickerBase` here so Task 2 attaches to it).
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker/SheetDatePicker/SheetDatePicker.test</automated>
  </verify>
  <acceptance_criteria>
    - The embedded AntD panel renders inside the sheet body element (RTL: panel node is a descendant of the sheet body, NOT portaled to document.body top-left) — proves the de-floating recipe.
    - Picking a day calls onChange with a value where `dayjs.isDayjs(value) === true` and the sheet closes.
    - A `disabledDate` (min/max) marks out-of-range days disabled (RTL/aria-disabled assertion).
    - "Hôm nay" sets today (or nearest allowed when today is disabled) as a Dayjs and closes.
    - SheetDatePicker.test.tsx passes under jsdom.
  </acceptance_criteria>
  <done>SheetDatePicker hosts AntD's panel inline (de-floated), commits a Dayjs on tap with auto-dismiss, honors min/max, and offers a clamped "Hôm nay"; the de-floating path is documented in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: showTime + RangePicker sub-export (commit on Xong) + Form-binding test</name>
  <read_first>
    - src/Components/Form/DatePicker/DatePicker.tsx (RangePicker sub-export pattern: const RangePicker = forwardRef; Object.assign(Base, { RangePicker }))
    - src/Components/SheetPicker/shared/useSheetPickerField.ts (draft/dirty/commit/cancel for range — Plan 01)
    - src/Components/FastOverlay/FastOverlay.tsx (SheetActions sticky Xong/Hủy ~636-650; maskClosable spring-back)
    - src/Components/Sheet/Sheet.test.tsx (jsdom RTL setup)
    - .planning/phases/08-sheet-picker-component-layer/08-RESEARCH.md (§Pattern 5 range draft; §Validation Architecture Form-bind; Pitfall 1/5)
  </read_first>
  <action>
    Add the range mode + the sub-export. Create a `RangePicker` (mirror `Form/DatePicker.tsx` lines 14-24 shape) that embeds AntD `<DatePicker.RangePicker>` inside the sheet via the same forced-`open` + `getPopupContainer` + z-index-neutralize recipe from Task 1. Range drives a `draft` from `useSheetPickerField` (RESEARCH §Pattern 3 / D-09): the panel edits the draft; the sheet STAYS open; commit ONLY on a `Xong` button via Phase-7 `SheetActions` (so a half-picked range can't escape); `Hủy`/drag-dismiss reverts (discard draft, never call `onChange`). Pass `maskClosable={!dirty}` to `<Sheet>` (Pitfall 2). Value stays a `[Dayjs, Dayjs]` tuple — never moment (Pitfall 5). Export `export const SheetDatePicker = Object.assign(SheetDatePickerBase, { RangePicker })` and `SheetDatePickerProps` (mirrors `Form/DatePicker.tsx`, D-01). Verify `showTime` on the single picker still commits a Dayjs with the time component preserved.
    Add folder barrel `SheetDatePicker/index.ts` re-exporting `SheetDatePicker` (consumers reach `SheetDatePicker.RangePicker`) + props type.
    Create `SheetDatePicker.formbind.test.tsx` (jsdom RTL): wrap `<SheetDatePicker>` in a real AntD `<Form onFinish={spy}>` under `<Form.Item name="date">`; drive a date pick with `fireEvent.click` (jsdom-safe per RESEARCH A3); submit; assert `onFinish` received a value with `dayjs.isDayjs(value) === true`, unchanged. Add a range case: wrap `<SheetDatePicker.RangePicker>` under `name="range"`, pick start+end, click `Xong`, submit, assert the `[Dayjs, Dayjs]` tuple is collected unchanged. Guards Pitfall 1. per PICK-06/08, D-12.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker/SheetDatePicker</automated>
  </verify>
  <acceptance_criteria>
    - `SheetDatePicker.RangePicker` exists (Object.assign sub-export) and commits a `[Dayjs, Dayjs]` tuple ONLY on "Xong"; "Hủy"/dismiss reverts.
    - Range mode passes `maskClosable={false}` while the draft is dirty.
    - `showTime` single mode commits a Dayjs preserving the time component.
    - Form-binding: onFinish collects the Dayjs single value and the range tuple unchanged (both cases pass).
    - All SheetDatePicker test files pass.
  </acceptance_criteria>
  <done>SheetDatePicker supports showTime and a RangePicker sub-export that commits on "Xong", preserves Dayjs values, and binds to AntD Form (PICK-06/08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| call-site date constraints → panel | `disabledDate`/min-max supplied by the call site; the picker enforces via AntD's own panel, holds no privileged state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering | embedded date panel value | mitigate | Value stays `Dayjs`, emitted unchanged; min/max enforced by AntD `disabledDate`; no eval, no HTML injection |
| T-08-10 | Information disclosure | inline panel render | accept | Local-first PWA, no PII in dates; panel renders inside the sheet, no network |
| T-08-SC | Tampering | dependencies | accept | Zero new runtime deps (locked); rc-picker fallback is already installed (transitive) — no install task |
</threat_model>

<verification>
- `CI=true npx react-scripts test --watchAll=false src/Components/SheetPicker/SheetDatePicker` green (single/datetime/range + form-binding).
- The embedded panel renders inline in the sheet body (de-floated); component passes no explicit `zIndex` and reverses the App.tsx z-index:4200 popup.
- All emitted date values satisfy `dayjs.isDayjs(...)`.
</verification>

<success_criteria>
- SheetDatePicker (PICK-05/06) hosts AntD's panel inline, commits Dayjs values, honors min/max + "Hôm nay", supports showTime + a RangePicker sub-export committing on "Xong", and binds to AntD Form (PICK-08) — proven by co-located jsdom tests. The de-floating recipe (Open Q1/A1) is resolved and documented.
</success_criteria>

## Artifacts this phase produces (this plan)

New symbols/files created by this plan (exclude from drift verification — they are net-new):
- `SheetDatePicker`, `SheetDatePickerProps`, `SheetDatePicker.RangePicker` (sub-export) — `src/Components/SheetPicker/SheetDatePicker/SheetDatePicker.tsx`
- Scoped `sheet-date-popup` style class (injected `<style>` or popupClassName) for de-floating
- Folder barrel `src/Components/SheetPicker/SheetDatePicker/index.ts`
- Co-located tests `SheetDatePicker.test.tsx`, `SheetDatePicker.formbind.test.tsx`

<output>
Create `.planning/phases/08-sheet-picker-component-layer/08-05-SUMMARY.md` when done.
</output>
