---
phase: 06-differentiator-enhancements
plan: 05
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/Common/Copy/AppCopy.ts
  - src/Modules/MealPlanning/Screens/Wizard.screen.tsx
  - src/Modules/MealPlanning/Screens/WizardServingsStep.widget.tsx
  - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
autonomous: true
requirements: [WIZ2-01, WIZ2-03, WIZ2-04]
must_haves:
  truths:
    - "Clearing remembered defaults asks for confirmation before deleting; nothing is removed silently"
    - "The remembered-defaults hint shows the title above its two actions (column layout), not crammed onto one row"
    - "Each member on the portions step is a full-width card showing name, health status, and a short description"
    - "When adding missing ingredients with an open list present, the user can choose between the existing list and a brand-new list"
  artifacts:
    - path: "src/Modules/MealPlanning/Screens/Wizard.screen.tsx"
      provides: "confirm-gated clear-defaults + stacked hint layout"
    - path: "src/Modules/MealPlanning/Screens/WizardServingsStep.widget.tsx"
      provides: "full-width member cards with status tag and description"
    - path: "src/Modules/MealPlanning/Screens/WizardResult.widget.tsx"
      provides: "existing-vs-new shopping-list target selector"
---

<objective>
Close three wizard UI gaps from Phase 6 UAT, all in the route-hosted meal wizard and compliant with 06-UI-SPEC (spacing multiples of 4, accent `#7436dc` only where specified, copy in `AppCopy.wizard`, `SheetActions` rules):
1. (major, test 2) Gate clear-defaults behind a confirmation and restack the remembered-defaults hint to title-above-two-buttons.
2. (cosmetic, test 1) Render each member on the portions step as a full-width card with name + health status + short description.
3. (minor, test 6) Offer a brand-new shopping list as a target when an open list already exists.

Purpose: Match the validated journey to the design contract and the UAT-reported expectations without model/reducer changes.
Output: Updated wizard copy, screen, servings step, and result widget.
</objective>

<context>
@.planning/phases/06-differentiator-enhancements/06-UAT.md
@.planning/phases/06-differentiator-enhancements/06-UI-SPEC.md
@src/Common/Copy/AppCopy.ts
@src/Modules/MealPlanning/Screens/Wizard.screen.tsx
@src/Modules/MealPlanning/Screens/WizardServingsStep.widget.tsx
@src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
@src/Modules/Home/Screens/HouseholdHealth.widget.tsx
@src/Store/Selectors.ts
</context>

<interface_context>
Reusable pieces already in the codebase (do not re-implement):
- `HouseholdHealthStatusTag` — exported from `src/Modules/Home/Screens/HouseholdHealth.widget.tsx`, signature `{ status?: HouseholdHealthStatus; note?: string; compact?: boolean }`. Defaults to `neutral` when status is undefined.
- `selectHouseholdHealthProfiles` — `src/Store/Selectors.ts`, returns `Record<memberId, { status?, statusNote?, ... }>`.
- `useModal` — `@components/Modal/ModalProvider`; `const modal = useModal();` then `modal.confirm({ title, content, okText, cancelText, okButtonProps: { danger: true }, onOk })`. Pattern in `DishesDetail.screen.tsx`.
- `HouseholdMemberProfile` fields for the description: `favoriteDishIds`, `favoriteIngredientIds`, `preferredTags`, `avoidedDishIds`, `avoidedIngredientIds`, `avoidedTags`, `allergenIngredientIds`, `hardExcludedIngredientIds`, `notes?: string`. The "X thích · Y tránh · Z chặn" summary pattern is in `HouseholdProfiles.screen.tsx` (~line 407).
- `addShoppingList` + `addIngredientGroupsToShoppingList` reducers already support create-then-append (see `confirmAddMissingIngredients` in WizardResult). No reducer change needed.
</interface_context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Confirm-gate clear-defaults and restack the remembered-defaults hint</name>
  <files>src/Common/Copy/AppCopy.ts, src/Modules/MealPlanning/Screens/Wizard.screen.tsx</files>
  <read_first>
    - src/Modules/MealPlanning/Screens/Wizard.screen.tsx
    - src/Common/Copy/AppCopy.ts
    - src/Modules/Dishes/Screens/DishesManageIngredient/DishesDetail.screen.tsx
  </read_first>
  <action>
    Per UAT gap (clear-defaults-confirm-and-layout): (1) Add `AppCopy.wizard` destructive-confirm copy keys — a confirm title and a clear body (e.g. `clearDefaultsConfirmTitle` reusing the contract string `Xóa lựa chọn đã nhớ`, and `clearDefaultsConfirmBody` explaining the remembered choices will be removed). Add a cancel label only if `AppCopy.common.cancel` is not suitable. (2) In `Wizard.screen.tsx`, import `useModal` from `@components/Modal/ModalProvider`; wrap the `wizard-clear-defaults` button's onClick in `modal.confirm({ title: clearDefaultsConfirmTitle, content: clearDefaultsConfirmBody, okText: AppCopy.wizard.clearDefaultsAction, cancelText: AppCopy.common.cancel, okButtonProps: { danger: true }, onOk: () => dispatch(clearWizardDefaults()) })` — no silent deletion (UI-SPEC destructive-confirmation rule). (3) Restack the `wizard-defaults-hint` Box: change it to `flexDirection: column`, `alignItems: flex-start`, drop `justifyContent: space-between`, so the `usingLastChoices` title sits ABOVE the two-button row; keep the two action buttons in their own row beneath it with an 8px (sm token) gap. Keep secondary panel background `#f5f5f5` and accent links per UI-SPEC. Use spacing multiples of 4.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `AppCopy.wizard` exports a clear-defaults confirm title and body key
    - `Wizard.screen.tsx` routes the clear-defaults click through `useModal().confirm` with a danger ok button and `onOk` dispatching `clearWizardDefaults()`
    - The `wizard-defaults-hint` container uses `flexDirection: column` with the title above the two-button row (no `space-between`)
    - `yarn build` exits 0
  </acceptance_criteria>
  <done>Clearing remembered defaults requires confirmation and the hint reads title-above-buttons.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render portions-step members as full-width status cards</name>
  <files>src/Common/Copy/AppCopy.ts, src/Modules/MealPlanning/Screens/WizardServingsStep.widget.tsx</files>
  <read_first>
    - src/Modules/MealPlanning/Screens/WizardServingsStep.widget.tsx
    - src/Modules/Home/Screens/HouseholdHealth.widget.tsx
    - src/Modules/Home/Screens/HouseholdProfiles.screen.tsx
    - src/Store/Selectors.ts
    - src/Common/Copy/AppCopy.ts
  </read_first>
  <action>
    Per UAT gap (member-selection-layout): replace the single-line member `<button>` in `WizardServingsStep.widget.tsx` with a full-width selectable card (column content, `md` (16px) padding, `>=44px` tap target, `borderRadius` consistent with existing). Selected state keeps the accent `#7436dc` border/tint already in the file. Each card shows: (a) member name (existing), (b) health status via the reusable `HouseholdHealthStatusTag` fed from `selectHouseholdHealthProfiles` (`healthProfiles[member.id]?.status`, defaulting to `neutral`), and (c) a short description composed from `member.notes` if present, else the preference summary `X thích · Y tránh · Z chặn` (mirror the counts pattern from HouseholdProfiles.screen.tsx). Keep the selected/unselected check affordance and the toggle behavior intact. Add any new visible strings (e.g. a member-description fallback label) to `AppCopy.wizard`; do not hardcode user-facing Vietnamese inline. No model changes. Keep the existing `data-testid="wizard-member-{id}"` on each card so the current wizard screen test still passes.
  </action>
  <verify>
    <automated>yarn test --watchAll=false --runTestsByPath src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Each member renders as a full-width card with `data-testid="wizard-member-{id}"` preserved
    - The card renders the member name, a `HouseholdHealthStatusTag` (status from `selectHouseholdHealthProfiles`, default `neutral`), and a short description (notes or preference summary)
    - Selected state uses accent `#7436dc`; card padding is `md` (16px) and tap target `>=44px`
    - Any new visible strings live in `AppCopy.wizard`
    - Focused wizard screen test exits 0 and `yarn build` exits 0
  </acceptance_criteria>
  <done>Portions-step members appear as full-width cards with name, status, and description.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Offer a brand-new shopping list target when an open list exists</name>
  <files>src/Common/Copy/AppCopy.ts, src/Modules/MealPlanning/Screens/WizardResult.widget.tsx</files>
  <read_first>
    - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
    - src/Common/Copy/AppCopy.ts
    - src/Components/Sheet/index.ts
  </read_first>
  <action>
    Per UAT gap (add-to-new-shopping-list-option): the gap is UI wiring only — `addShoppingList` + `addIngredientGroupsToShoppingList` already support create-then-append. (1) Add `missingTargetMode` state (`'existing' | 'new'`), defaulting to `'existing'` when an active list exists else `'new'`; reset it in `openMissingSheet` (alongside the existing `setSelectedMissingIds`/`setCreateShoppingListName`). (2) In the missing-ingredient Sheet body, when `activeShoppingList` exists, render a selector (antd `Radio.Group` or `Segmented`) between `Thêm vào {name}` (mode `existing`) and `Tạo danh sách mới` (mode `new`); when mode is `new`, reveal the existing name `Input` (currently only shown when no active list exists). When no active list exists, keep current behavior (force `new`, show the Input). (3) Branch `confirmAddMissingIngredients` on `missingTargetMode`: for `new`, always create a fresh list via `addShoppingList` then append (do not reuse `getLatestIncompleteShoppingList`); for `existing`, keep current append-to-active behavior. Key the footer confirm label off the resolved mode (`missingAddAction` vs `missingCreateAndAddAction`). Add the two selector option labels to `AppCopy.wizard` (reuse `missingTargetList`/`missingCreateListName` where natural). Respect `SheetActions` rules (two actions horizontal) and spacing multiples of 4. No reducer change.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `WizardResult.widget.tsx` has `missingTargetMode` state defaulting by active-list presence and reset in `openMissingSheet`
    - When an open list exists, the Sheet shows an existing-vs-new selector; choosing new reveals the name Input
    - `confirmAddMissingIngredients` forces `addShoppingList` + append on `new` mode and keeps append-to-active on `existing`
    - New selector labels live in `AppCopy.wizard`; no reducer change
    - `yarn build` exits 0
  </acceptance_criteria>
  <done>Users can add missing ingredients to a brand-new shopping list even when an open list exists.</done>
</task>

</tasks>

<verification>
- `yarn test --watchAll=false --runTestsByPath src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx`
- `yarn build`
- Mobile smoke: portions-step member cards stretch full width; remembered-defaults hint reads title-above-buttons; clear-defaults shows a confirm; missing-ingredient Sheet offers existing-vs-new list without horizontal overflow.
</verification>

<success_criteria>
- UAT gap (test 2) closed: clear-defaults confirms before deleting and the hint is stacked title-above-buttons.
- UAT gap (test 1) closed: members are full-width cards with name, status, and description.
- UAT gap (test 6) closed: a brand-new shopping list is selectable even when an open list exists.
- All new visible strings live in `AppCopy.wizard`; UI-SPEC spacing/color/SheetActions rules respected; no model or reducer changes.
</success_criteria>

<output>
Create `.planning/phases/06-differentiator-enhancements/06-05-SUMMARY.md` when done.
</output>
