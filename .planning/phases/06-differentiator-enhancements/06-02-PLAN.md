---
phase: 06-differentiator-enhancements
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/Common/Copy/AppCopy.ts
  - src/Modules/MealPlanning/Screens/WizardPreferenceStep.widget.tsx
  - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
  - src/Modules/DishSuggester/Helpers/DishScorer.ts
  - src/Modules/DishSuggester/Helpers/DishScorer.test.ts
autonomous: true
requirements: [WIZ2-02, WIZ2-05]
must_haves:
  truths:
    - "Cook-now mode is optional and ranks/groups results instead of hiding all near matches"
    - "Cook-now groups render as Nấu ngay, Cần mua thêm ít, and Dự phòng"
    - "Every rendered suggestion card has a natural one-line reason and a question-mark detail sheet trigger"
    - "Reason detail sheets are precise but non-numeric; they do not expose raw scorer values"
  artifacts:
    - path: "src/Modules/MealPlanning/Screens/WizardResult.widget.tsx"
      provides: "grouped cook-now result cards and reason detail sheet"
---

<objective>
Extend the result path to use cook-now grouping and practical non-numeric reason text, while keeping the existing full-catalog fallback when inventory signals are weak or absent.
</objective>

<context>
@.planning/phases/06-differentiator-enhancements/06-CONTEXT.md
@.planning/phases/06-differentiator-enhancements/06-UI-SPEC.md
@src/Modules/DishSuggester/Helpers/DishScorer.ts
@src/Modules/DishSuggester/Helpers/DishScorer.test.ts
@src/Modules/MealPlanning/Screens/WizardPreferenceStep.widget.tsx
@src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
@src/Store/Selectors.ts
@src/Common/Copy/AppCopy.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add optional cook-now toggle to wizard preferences</name>
  <files>src/Common/Copy/AppCopy.ts, src/Modules/MealPlanning/Screens/WizardPreferenceStep.widget.tsx</files>
  <read_first>
    - src/Common/Copy/AppCopy.ts
    - src/Modules/MealPlanning/Screens/WizardPreferenceStep.widget.tsx
    - src/Store/Models/Wizard.ts
  </read_first>
  <action>
    Add `AppCopy.wizard.cookNowToggle` and weak-signal helper copy. Add an optional `Switch`/chip-style control to `WizardPreferenceStep` that initializes from `answers.cookNowOnly ?? false`, commits `cookNowOnly` with preferred tags, and keeps the step skippable.
  </action>
  <verify>
    <automated>yarn test --watchAll=false --runTestsByPath src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `WizardPreferenceStep.widget.tsx` renders `data-testid="wizard-cook-now-toggle"`
    - Preference advance commits `cookNowOnly` alongside `preferredTags`
    - Focused wizard screen test command exits 0
  </acceptance_criteria>
  <done>Cook-now preference is user-controlled and stored in wizard answers/defaults.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Align cook-now grouping label with Phase 6 contract</name>
  <files>src/Modules/DishSuggester/Helpers/DishScorer.ts, src/Modules/DishSuggester/Helpers/DishScorer.test.ts</files>
  <read_first>
    - src/Modules/DishSuggester/Helpers/DishScorer.ts
    - src/Modules/DishSuggester/Helpers/DishScorer.test.ts
  </read_first>
  <action>
    Change the middle `groupCookNow` label from `Mua thêm ít` to `Cần mua thêm ít` to match D-09. Update the existing characterization test accordingly without changing bucket thresholds unless a test failure proves the current thresholds break Phase 6 grouping.
  </action>
  <verify>
    <automated>yarn test --watchAll=false --runTestsByPath src/Modules/DishSuggester/Helpers/DishScorer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `DishScorer.groupCookNow` contains `Cần mua thêm ít`
    - Focused DishScorer test command exits 0
  </acceptance_criteria>
  <done>Cook-now bucket labels match the Phase 6 UI contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Render grouped result cards with reason line and detail sheet</name>
  <files>src/Common/Copy/AppCopy.ts, src/Modules/MealPlanning/Screens/WizardResult.widget.tsx</files>
  <read_first>
    - src/Common/Copy/AppCopy.ts
    - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
    - src/Modules/DishSuggester/Helpers/DishScorer.ts
    - src/Store/Selectors.ts
    - src/Store/Reducers/AppContextReducer.ts
  </read_first>
  <action>
    In `WizardResult`, read ingredients, inventory, inventory config, household members, and household preference profile through selectors. When `answers.cookNowOnly` is true and inventory has data, use `DishScorer.scoreCookNow(...)` with a profile derived from wizard servings/member IDs/preferences. Render `DishScorer.groupCookNow(scored)` groups up to five total cards, preserving fallback groups if ready-to-cook items are sparse. When cook-now is off or inventory is empty, keep the existing selected-ingredient scorer/full-catalog fallback and show weak-signal copy if relevant. Add one natural reason line per card and a small `QuestionCircleOutlined` button opening a `Sheet` with matched ingredients, missing ingredients, preference matches/avoids, household fit, and no raw score numbers.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - Result cards render a reason line with `data-testid="wizard-reason-{dish.id}"`
    - Detail trigger uses `data-testid="wizard-reason-detail-{dish.id}"` and opens a Sheet titled by AppCopy
    - `WizardResult.widget.tsx` calls `DishScorer.scoreCookNow` and `DishScorer.groupCookNow`
    - Build exits 0
  </acceptance_criteria>
  <done>Results are cook-now grouped when requested and every suggestion explains itself naturally.</done>
</task>

</tasks>

<verification>
- `yarn test --watchAll=false --runTestsByPath src/Modules/DishSuggester/Helpers/DishScorer.test.ts src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx`
- `yarn build`
</verification>

<success_criteria>
- WIZ2-02 cook-now mode guides and groups results without dead-ending.
- WIZ2-05 every suggestion has a reason line and precise non-numeric detail affordance.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 02)

New copy keys: `wizard.cookNowToggle`, `wizard.cookNowWeakSignal`, reason/detail labels.
New result behavior: grouped cook-now rendering, reason detail Sheet.
</artifacts>

<output>
Create `.planning/phases/06-differentiator-enhancements/06-02-SUMMARY.md` when done.
</output>
