---
phase: 06-differentiator-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Store/Models/Wizard.ts
  - src/Store/Reducers/WizardReducer.ts
  - src/Store/Selectors.ts
  - src/Store/Reducers/WizardReducer.test.ts
  - src/Common/Copy/AppCopy.ts
  - src/Modules/MealPlanning/Screens/Wizard.screen.tsx
  - src/Modules/MealPlanning/Screens/WizardServingsStep.widget.tsx
  - src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx
autonomous: true
requirements: [WIZ2-01, WIZ2-04]
must_haves:
  truths:
    - "Wizard flow contains ingredients -> servings -> preferences -> result"
    - "Servings step selects household member IDs when members exist and falls back to serving count when no members exist"
    - "Restarting after a completed wizard pre-fills remembered defaults, while start-fresh clears only the current run"
    - "Clear-defaults removes stored defaults without creating a new persisted root"
  artifacts:
    - path: "src/Modules/MealPlanning/Screens/WizardServingsStep.widget.tsx"
      provides: "skippable portions/member-selection step"
    - path: "src/Store/Models/Wizard.ts"
      provides: "wizard defaults and Phase 6 answer fields under existing personal.wizard state"
---

<objective>
Add the remembered-defaults state contract and the skippable "who's eating?" portions step to the existing route-hosted meal wizard. The work must stay under the existing `personal.wizard` persisted root and reuse selector-only reads.
</objective>

<context>
@.planning/phases/06-differentiator-enhancements/06-CONTEXT.md
@.planning/phases/06-differentiator-enhancements/06-UI-SPEC.md
@src/Store/Models/Wizard.ts
@src/Store/Reducers/WizardReducer.ts
@src/Store/Selectors.ts
@src/Modules/MealPlanning/Screens/Wizard.screen.tsx
@src/Modules/MealPlanning/Screens/WizardIngredientStep.widget.tsx
@src/Modules/MealPlanning/Screens/WizardPreferenceStep.widget.tsx
@src/Common/Copy/AppCopy.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Persist remembered wizard defaults without a new root</name>
  <files>src/Store/Models/Wizard.ts, src/Store/Reducers/WizardReducer.ts, src/Store/Selectors.ts, src/Store/Reducers/WizardReducer.test.ts</files>
  <read_first>
    - src/Store/Models/Wizard.ts
    - src/Store/Reducers/WizardReducer.ts
    - src/Store/Selectors.ts
    - src/Store/Reducers/WizardReducer.test.ts
  </read_first>
  <action>
    Add explicit Phase 6 answer fields to `WizardAnswers`: `memberIds?: string[]`, `cookNowOnly?: boolean`, while keeping `extras` for future optional data. Extend `WizardState` with `lastCompletedAnswers?: WizardAnswers`. Update reducers so `advanceWizardStep('result')` and `completeWizard()` save current answers as defaults. Change `restartWizard()` to prefill current answers from `lastCompletedAnswers` when available. Add `startFreshWizard()` to clear only the current run and keep defaults, and `clearWizardDefaults()` to permanently remove defaults. Add `selectWizardDefaults` with the same defensive fallback pattern as other selectors. Update reducer tests for these semantics.
  </action>
  <verify>
    <automated>yarn test --watchAll=false --runTestsByPath src/Store/Reducers/WizardReducer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `WizardState` contains `lastCompletedAnswers?: WizardAnswers`
    - `restartWizard` pre-fills from stored defaults, `startFreshWizard` clears current answers only, and `clearWizardDefaults` removes stored defaults
    - `selectWizardDefaults` exists in `src/Store/Selectors.ts`
    - Focused reducer test command exits 0
  </acceptance_criteria>
  <done>Wizard defaults are persisted under `personal.wizard` and reducer semantics are covered by tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insert portions/member-selection step into the wizard</name>
  <files>src/Common/Copy/AppCopy.ts, src/Modules/MealPlanning/Screens/Wizard.screen.tsx, src/Modules/MealPlanning/Screens/WizardServingsStep.widget.tsx, src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx</files>
  <read_first>
    - src/Common/Copy/AppCopy.ts
    - src/Modules/MealPlanning/Screens/Wizard.screen.tsx
    - src/Modules/MealPlanning/Screens/WizardIngredientStep.widget.tsx
    - src/Modules/MealPlanning/Screens/WizardPreferenceStep.widget.tsx
    - src/Store/Selectors.ts
    - src/Store/Reducers/AppContextReducer.ts
    - src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx
  </read_first>
  <action>
    Add `AppCopy.wizard` keys for the portions step, default hint, reset/start-fresh, clear defaults, member count, and serving count. Create `WizardServingsStep.widget.tsx`: read `selectHouseholdMembers` and `selectWizardAnswers`; if household members exist, render selectable member rows with 44px minimum height and derive servings from selected members plus a small guest/leftover adjustment; if not, render a simple serving-count stepper. Skip commits default servings without requiring household setup. Wire `WIZARD_STEPS` to `['ingredients', 'servings', 'preferences', 'result']`, render the new step, and expose a subtle remembered-defaults hint with a start-fresh action when defaults are in use. Update wizard screen tests for the new step order and re-entry default behavior.
  </action>
  <verify>
    <automated>yarn test --watchAll=false --runTestsByPath src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `Wizard.screen.tsx` includes `servings` between ingredients and preferences
    - `WizardServingsStep.widget.tsx` renders `data-testid="wizard-step-servings"`, `wizard-serving-minus`, and `wizard-serving-plus`
    - Household member rows render with `data-testid="wizard-member-{id}"`; no-member fallback still advances with `servingCount`
    - Focused wizard screen test command exits 0
  </acceptance_criteria>
  <done>The wizard has a skippable portions/member-selection step and remembered-defaults hint.</done>
</task>

</tasks>

<verification>
- `yarn test --watchAll=false --runTestsByPath src/Store/Reducers/WizardReducer.test.ts src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx`
- `yarn build`
</verification>

<success_criteria>
- WIZ2-01 portions step is present and skippable.
- WIZ2-04 remembered defaults persist under the existing `personal` root and can be bypassed or cleared.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 01)

New symbols: `WizardAnswers.memberIds`, `WizardAnswers.cookNowOnly`, `WizardState.lastCompletedAnswers`, `startFreshWizard`, `clearWizardDefaults`, `selectWizardDefaults`, `WizardServingsStep`.
New file paths: `src/Modules/MealPlanning/Screens/WizardServingsStep.widget.tsx`.
</artifacts>

<output>
Create `.planning/phases/06-differentiator-enhancements/06-01-SUMMARY.md` when done.
</output>
