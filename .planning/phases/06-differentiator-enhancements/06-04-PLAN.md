---
phase: 06-differentiator-enhancements
plan: 04
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/Modules/DishSuggester/Helpers/DishScorer.ts
  - src/Modules/DishSuggester/Helpers/DishScorer.test.ts
autonomous: true
requirements: [WIZ2-02]
must_haves:
  truths:
    - "With cook-now enabled and inventory present, results show all three groups (Nấu ngay, Cần mua thêm ít, Dự phòng) as appropriate, never collapsing to a single group"
    - "A dish whose stock covers its base recipe lands in Nấu ngay even when servingCount exceeds the dish baseServings"
    - "Low-readiness dishes (many missing ingredients) fall to Dự phòng instead of being swallowed by the composite cook-now score baseline"
  artifacts:
    - path: "src/Modules/DishSuggester/Helpers/DishScorer.ts"
      provides: "cook-now grouping decoupled from serving-scaled readiness and a readiness-based middle/backup split"
    - path: "src/Modules/DishSuggester/Helpers/DishScorer.test.ts"
      provides: "real scoreCookNow -> groupCookNow pipeline test with servingCount > baseServings"
---

<objective>
Fix the cook-now result grouping (UAT gap, test 4) that collapses to only the middle "Cần mua thêm ít" group. The bug is in `DishScorer`, not the render layer: "Nấu ngay" is gated on zero missing against serving-scaled amounts (never zero when `servingCount > baseServings`), and "Dự phòng" rarely fills because the middle-bucket condition leans on a high-baseline `cookNowScore >= 0.58`. Pin the corrected three-bucket distribution with a test that exercises the real `scoreCookNow -> groupCookNow` pipeline.

Purpose: Restore the cook-now differentiator so a normal household (servingCount > 2) sees ready, near-ready, and backup dishes instead of one group.
Output: Corrected `DishScorer` grouping/readiness logic plus a real-pipeline characterization test.
</objective>

<context>
@.planning/phases/06-differentiator-enhancements/06-UAT.md
@.planning/phases/06-differentiator-enhancements/06-UI-SPEC.md
@src/Modules/DishSuggester/Helpers/DishScorer.ts
@src/Modules/DishSuggester/Helpers/DishScorer.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin three-bucket cook-now distribution with a real-pipeline test</name>
  <files>src/Modules/DishSuggester/Helpers/DishScorer.test.ts</files>
  <read_first>
    - src/Modules/DishSuggester/Helpers/DishScorer.test.ts
    - src/Modules/DishSuggester/Helpers/DishScorer.ts
  </read_first>
  <behavior>
    - A new test in the `scoreCookNow`/`groupCookNow` area runs the REAL pipeline (not synthetic `makeScoredDish` inputs): call `DishScorer.scoreCookNow(dishes, inventory, dishes, ingredients, profile, ...)` then `DishScorer.groupCookNow(result)`.
    - Profile uses `servingCount` strictly greater than the fixture dishes' `baseServings` (e.g. `servingCount: 4` against dishes with `baseServings: 2` or default 2), reproducing the serving-scaling condition from the UAT diagnosis.
    - Fixtures include: (a) a dish whose inventory fully covers its UNscaled recipe, (b) a dish missing 1-2 ingredients, and (c) a dish missing >3 ingredients.
    - Assertions: all three group labels are present (`Nấu ngay`, `Cần mua thêm ít`, `Dự phòng`); the fully-stocked dish appears in `Nấu ngay`; the many-missing dish appears in `Dự phòng`. This test FAILS against current code (group 0 empty / group 2 empty) — that is the RED state proving the bug.
    - The existing synthetic `groupCookNow` test must be updated to match the re-tuned middle/backup boundary from Task 2 (drop reliance on `cookNowScore >= 0.58`); a near-ready dish must be classified by readiness, not by composite score baseline.
  </behavior>
  <action>
    Add a real-pipeline test exercising `scoreCookNow -> groupCookNow` with `servingCount` above the fixture `baseServings`, reusing the `inventoryDishes`/`inventoryItems`/`inventoryIngredients` fixture style already in the file (add new fixtures as needed for the fully-stocked and many-missing cases). Assert the three-bucket distribution described above. Keep `jest.useFakeTimers` setup intact. Do not weaken existing `score`, `scoreWithInventory`, or `scoreCookNow` characterization assertions; only adjust the synthetic `groupCookNow` expectation to align with the readiness-based boundary that Task 2 introduces.
  </action>
  <verify>
    <automated>yarn test --watchAll=false --runTestsByPath src/Modules/DishSuggester/Helpers/DishScorer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `DishScorer.test.ts` contains a test that calls `scoreCookNow` then `groupCookNow` with `servingCount` > `baseServings`
    - That test asserts all three group labels present, fully-stocked dish in `Nấu ngay`, many-missing dish in `Dự phòng`
    - Before Task 2, this test fails (RED); after Task 2 it passes (GREEN)
  </acceptance_criteria>
  <done>The real cook-now pipeline is pinned by a test that distinguishes the three buckets under realistic serving counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decouple Nấu ngay readiness and re-tune the middle/backup split</name>
  <files>src/Modules/DishSuggester/Helpers/DishScorer.ts</files>
  <read_first>
    - src/Modules/DishSuggester/Helpers/DishScorer.ts
    - src/Modules/DishSuggester/Helpers/DishScorer.test.ts
  </read_first>
  <action>
    Two fixes per the UAT diagnosis (cook-now-single-group). (1) Decouple the "Nấu ngay" (group 0) readiness from serving-scaled amounts: the scaled `item.required` from `getDishServingScale(dish, profile.servingCount)` in `scoreInventoryDishes` makes `missingIngredientIds.length === 0` unreachable when `servingCount > baseServings`. Base group-0 membership on unscaled readiness — either compute an unscaled-readiness signal in `scoreInventoryDishes`/`scoreCookNow` (e.g. stock covers the base, unscaled requirement) exposed on `ScoredDish` and read it in `groupCookNow`, or allow a small shortfall tolerance so a near-fully-stocked dish still counts as ready. Do NOT change `scoreWithInventory`/`group` behavior — only the cook-now path. (2) Re-tune the middle/backup boundary in `groupCookNow`: remove the `|| (item.cookNowScore ?? 0) >= 0.58` clause (its ~0.5+ composite baseline swallows low-readiness dishes), and split the middle group on inventory readiness (e.g. missing count threshold / `score`) so dishes with many missing ingredients fall to "Dự phòng". Keep the existing `Nấu ngay`/`Cần mua thêm ít`/`Dự phòng` labels and colors from Plan 06-02. Make the Task 1 real-pipeline test pass without weakening the preserved characterization assertions.
  </action>
  <verify>
    <automated>yarn test --watchAll=false --runTestsByPath src/Modules/DishSuggester/Helpers/DishScorer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `groupCookNow` no longer references `cookNowScore >= 0.58` for the middle bucket
    - A fully-stocked dish lands in `Nấu ngay` when `servingCount` > `baseServings`
    - Many-missing dishes land in `Dự phòng`
    - The Task 1 real-pipeline test passes; existing `score`/`scoreWithInventory`/`scoreCookNow` assertions remain green
    - `yarn build` exits 0
  </acceptance_criteria>
  <done>Cook-now grouping yields all three buckets under realistic serving counts without collapsing to the middle group.</done>
</task>

</tasks>

<verification>
- `yarn test --watchAll=false --runTestsByPath src/Modules/DishSuggester/Helpers/DishScorer.test.ts`
- `yarn build`
</verification>

<success_criteria>
- UAT gap (test 4) closed: cook-now results group into Nấu ngay, Cần mua thêm ít, and Dự phòng as appropriate and never collapse to a single group.
- The fix is pinned by a test that runs the real `scoreCookNow -> groupCookNow` pipeline with `servingCount` > `baseServings`.
</success_criteria>

<output>
Create `.planning/phases/06-differentiator-enhancements/06-04-SUMMARY.md` when done.
</output>
