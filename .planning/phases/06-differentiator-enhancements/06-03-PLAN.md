---
phase: 06-differentiator-enhancements
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - src/Common/Copy/AppCopy.ts
  - src/Store/Reducers/ShoppingListReducer.ts
  - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
autonomous: true
requirements: [WIZ2-03]
must_haves:
  truths:
    - "Result cards with missing ingredients can open a Sheet to add selected missing ingredients to Đi chợ"
    - "Missing ingredients are preselected, duplicate existing shopping-list groups are skipped, and the user remains on the result page"
    - "If no active shopping list exists, the sheet offers an in-place create-list path instead of navigating away"
    - "After add, the card shows inline success/undo/manage state"
  artifacts:
    - path: "src/Store/Reducers/ShoppingListReducer.ts"
      provides: "selected missing ingredient append action with duplicate skipping"
---

<objective>
Add the inline missing-ingredient-to-shopping-list convenience from result cards without navigating away from the meal wizard.
</objective>

<context>
@.planning/phases/06-differentiator-enhancements/06-CONTEXT.md
@.planning/phases/06-differentiator-enhancements/06-UI-SPEC.md
@src/Store/Reducers/ShoppingListReducer.ts
@src/Store/Models/ShoppingList.ts
@src/Store/Models/Dishes.ts
@src/Store/Models/Ingredient.ts
@src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
@src/Common/Copy/AppCopy.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add selected missing ingredient append reducer</name>
  <files>src/Store/Reducers/ShoppingListReducer.ts</files>
  <read_first>
    - src/Store/Reducers/ShoppingListReducer.ts
    - src/Store/Models/ShoppingList.ts
    - src/Store/Models/Dishes.ts
  </read_first>
  <action>
    Add a typed `addIngredientGroupsToShoppingList` reducer payload containing `shoppingListId`, `dish`, and selected ingredient amount rows. For the target incomplete list, skip any incoming ingredient whose `ingredientId` already exists in `ingredients`. Append new `ShoppingListIngredientGroup` rows with generated IDs, one amount per selected missing ingredient, `isDone: false`, and dish metadata. Do not increase duplicate quantities.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `ShoppingListReducer.ts` exports `addIngredientGroupsToShoppingList`
    - Reducer skips existing `ingredientId` groups and leaves completed lists unchanged
    - Build exits 0
  </acceptance_criteria>
  <done>Shopping lists can receive selected missing ingredient groups without duplicate quantity churn.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire result-card missing ingredient Sheet and inline state</name>
  <files>src/Common/Copy/AppCopy.ts, src/Modules/MealPlanning/Screens/WizardResult.widget.tsx</files>
  <read_first>
    - src/Common/Copy/AppCopy.ts
    - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
    - src/Store/Reducers/ShoppingListReducer.ts
    - src/Store/Selectors.ts
    - src/Components/Sheet/SheetActions.tsx
  </read_first>
  <action>
    Add missing-ingredient copy keys. In result cards with missing ingredients, render `Thêm vào Đi chợ`. Opening it shows a `Sheet` with missing ingredients preselected, amount labels when `ingredientDetails` provide amounts, and `SheetActions`. Choose the latest incomplete shopping list by default; if none exists, let the user create a new list in place with a household-friendly name. Dispatch `addIngredientGroupsToShoppingList` for selected rows, stay on the result page, and show inline added/manage/undo state. Undo may remove the just-added groups from the selected list if feasible; otherwise show a clear already-added/manage state and avoid re-adding duplicates.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `WizardResult.widget.tsx` renders `data-testid="wizard-add-missing-{dish.id}"` for cards with missing ingredients
    - Missing sheet rows use checkboxes and selected ingredients are preselected
    - No active-list path creates an incomplete shopping list in place, not a navigation
    - Duplicate existing ingredient groups are presented as already added or skipped
    - Build exits 0
  </acceptance_criteria>
  <done>Users can add selected missing ingredients to Đi chợ inline and remain on the result page.</done>
</task>

</tasks>

<verification>
- `yarn build`
- Mobile smoke: result page opens reason detail and missing-ingredient Sheet without horizontal overflow.
</verification>

<success_criteria>
- WIZ2-03 inline missing ingredient add is implemented with bottom sheets, duplicate skipping, and inline success state.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 03)

New symbols: `ShoppingListAddIngredientGroupsParams`, `addIngredientGroupsToShoppingList`.
New result behavior: missing ingredient Sheet, active-list/create-list path, inline added/manage state.
</artifacts>

<output>
Create `.planning/phases/06-differentiator-enhancements/06-03-SUMMARY.md` when done.
</output>
