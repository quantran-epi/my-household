---
phase: 02-shell-safety-extraction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Components/FastOverlay/FastOverlay.tsx
  - src/Components/Sheet/index.ts
  - src/Components/Sheet/Sheet.test.tsx
autonomous: true
requirements: [MOB-03]
must_haves:
  truths:
    - "A `Sheet` bottom-anchored overlay renders via the existing FastOverlay portal system (NOT antd Drawer) when open, and unmounts when closed"
    - "Sheet reuses the shared FastOverlay singletons (useBodyScrollLock, useEscapeClose, useResolvedOverlayZIndex, allocateOverlayStackToken) so its z-index stacks consistently with FastDrawerShell/FastModalShell"
    - "Sheet closes via mask click, Escape, and a close button, matching FastDrawerShell defaults (maskClosable/keyboard/closable default true)"
    - "@components/Sheet is importable via barrel and exposes Sheet + SheetProps"
    - "No existing picker/confirmation is migrated onto Sheet this phase (wrapper only, D-10)"
  artifacts:
    - path: "src/Components/FastOverlay/FastOverlay.tsx"
      provides: "Sheet bottom-placement variant + exported SheetProps type, co-located to share the module-private overlay singletons (Pitfall 3)"
      contains: "export const Sheet"
    - path: "src/Components/Sheet/index.ts"
      provides: "Barrel re-export for @components/Sheet"
      contains: "Sheet"
    - path: "src/Components/Sheet/Sheet.test.tsx"
      provides: "Jest/RTL smoke proof that Sheet mounts when open and unmounts when closed (D-10)"
      contains: "Sheet"
  key_links:
    - from: "src/Components/Sheet/index.ts"
      to: "src/Components/FastOverlay/FastOverlay.tsx"
      via: "re-exports Sheet + SheetProps from @components/FastOverlay"
      pattern: "FastOverlay"
    - from: "src/Components/FastOverlay/FastOverlay.tsx Sheet"
      to: "useResolvedOverlayZIndex / allocateOverlayStackToken singletons"
      via: "shared module-private overlay stacking (no re-declaration)"
      pattern: "useResolvedOverlayZIndex"
---

<objective>
Build the `@components/Sheet` bottom-sheet wrapper (FND-02 success criterion 4 / MOB-03 build-only) as a bottom-placement sibling to `FastDrawerShell`/`FastModalShell` on the existing `FastOverlay` portal system — a conscious divergence from the literal "over antd Drawer" wording (D-09), recorded so Phase 5 treats it as intentional. Ship the component + public API + a barrel + a mount/open smoke proof. Do NOT migrate any existing picker or confirmation onto it (D-10 — first real consumers arrive in Phase 5, MOB-03).

This plan is independent of the extraction work and of the e2e baseline: it adds net-new code only and is verified by a jest/RTL smoke test (so it needs no test-only route and does not contend with 02-02's RootRouter edits).

Purpose: Make a unified bottom-sheet primitive available before Phase 5 needs it for pickers/confirmations, without forking the shell's overlay z-index/motion behavior.
Output: `Sheet` + `SheetProps` added inside `FastOverlay.tsx` (re-exported), `src/Components/Sheet/index.ts` barrel (`@components/Sheet`), and a smoke test.

Requirement note: this realizes the build-only portion of MOB-03; the consumer migration remains a Phase 5 deliverable. Phase requirements FND-01/FND-02 are covered by plans 02-02/02-01.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-shell-safety-extraction/02-CONTEXT.md
@.planning/phases/02-shell-safety-extraction/02-RESEARCH.md
@.planning/phases/02-shell-safety-extraction/02-PATTERNS.md
@.planning/phases/02-shell-safety-extraction/02-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@src/Components/FastOverlay/FastOverlay.tsx
@src/Components/FastOverlay/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Sheet bottom-placement variant + SheetProps inside FastOverlay.tsx and ship the @components/Sheet barrel</name>
  <files>src/Components/FastOverlay/FastOverlay.tsx, src/Components/Sheet/index.ts</files>
  <read_first>
    - src/Components/FastOverlay/FastOverlay.tsx (FastDrawerShell lines 236-304 = the closest sibling; FastModalShell 140-234; shared hooks useBodyScrollLock L61, useEscapeClose L72, useResolvedOverlayZIndex L83, allocateOverlayStackToken L50, the module-singletons nextOverlayStackToken/activeOverlayStackTokens L47-48, overlayMotionStyles L107-115, closeButtonStyle L117-129, shellTitleStyle L131-138, toCssSize L32-35, FastOverlayBaseProps L7-17)
    - src/Components/FastOverlay/index.ts (barrel: `export * from './FastOverlay'`)
    - .planning/phases/02-shell-safety-extraction/02-PATTERNS.md (Sheet pattern block + Pitfall 3 z-index note)
    - .planning/phases/02-shell-safety-extraction/02-UI-SPEC.md (Net-New Surface Specs §2: placement, surface, a11y, API surface, spacing exceptions)
    - .planning/codebase/CONVENTIONS.md (single named export per file, strict:false, match surrounding style)
  </read_first>
  <behavior>
    - When `open` is true, `Sheet` portals a bottom-anchored panel into `document.body` (`role="dialog"`, `aria-modal="true"`), passes through `data-testid`, and renders `title` + `children`.
    - When `open` is false, `Sheet` returns null (nothing in `document.body`).
    - Clicking the backdrop calls `onClose` only when `maskClosable` (default true) and the click target is the backdrop itself; pressing Escape calls `onClose` when `keyboard` (default true); the close button (shown when `closable`, default true) calls `onClose`.
    - `Sheet` derives its z-index from `useResolvedOverlayZIndex(open, zIndex, 1200)` — sharing the existing stacking singletons, not a re-declared copy.
  </behavior>
  <action>
    Add `Sheet` INSIDE `src/Components/FastOverlay/FastOverlay.tsx` (D-09 / Pitfall 3 — co-locating keeps the module-private singletons `nextOverlayStackToken`/`activeOverlayStackTokens` and the hooks `useBodyScrollLock`/`useEscapeClose`/`useResolvedOverlayZIndex`/`allocateOverlayStackToken` shared; a separate module that re-declares them forks the z-index stack). Export a `SheetProps` type as `FastOverlayBaseProps & { height?: number | string }` (export `FastOverlayBaseProps` too if needed for the alias). Implement `export const Sheet: React.FunctionComponent<SheetProps>` modeled on `FastDrawerShell` but bottom-placement: call `useBodyScrollLock(open)`, `useEscapeClose(open && keyboard, onClose)`, `const resolvedZIndex = useResolvedOverlayZIndex(open, zIndex, 1200)`, early-return `null` when `!open`, then `createPortal` a backdrop div (`className="my-recipes-fast-overlay"`, `position:fixed`, `inset:0`, `display:flex`, `alignItems:"flex-end"`, `justifyContent:"center"`, backdrop `rgba(16,24,40,0.30)` to match the drawer, `animation: backdropInAnimation`, mask `onMouseDown` close guard identical to FastDrawerShell) wrapping the shared `{overlayMotionStyles}` and a `<section role="dialog" aria-modal="true" data-testid={testId}>` panel with `width:"100%"`, `maxWidth:720`, `maxHeight: toCssSize(height, "min(85vh, 720px)")`, `borderRadius:"18px 18px 0 0"`, white background, a bottom slide-up enter animation, and `onMouseDown` stopPropagation. Reuse the FastDrawerShell header row (title via `shellTitleStyle`, close button via `closeButtonStyle` with `aria-label`) and a scrollable body for `children`. Add a new keyframe `my-recipes-fast-sheet-in` (translateY enter) to the existing `overlayMotionStyles` `<style>` block so reduced-motion handling via the existing `@media (prefers-reduced-motion: reduce)` rule applies automatically (do not add a second style block). Do NOT change `FastDrawerShell`/`FastModalShell`.
    Create `src/Components/Sheet/index.ts` re-exporting `Sheet` and `SheetProps` from `@components/FastOverlay` (so `@components/Sheet` resolves) — mirror the one-line barrel style of `src/Components/FastOverlay/index.ts` and `src/Components/Result/index.ts`.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `src/Components/FastOverlay/FastOverlay.tsx` defines `export const Sheet` and `export type SheetProps` (grep: both present), and contains a `my-recipes-fast-sheet-in` keyframe inside the existing `overlayMotionStyles` block.
    - `Sheet` calls `useResolvedOverlayZIndex` and `useBodyScrollLock`/`useEscapeClose` (grep: present) and does NOT re-declare `nextOverlayStackToken` or `activeOverlayStackTokens` (those identifiers appear at their original single definition only).
    - The panel uses `borderRadius: "18px 18px 0 0"` and a bottom-anchored backdrop (`alignItems: "flex-end"`) — grep both present.
    - `src/Components/Sheet/index.ts` re-exports `Sheet` (grep: `Sheet` present).
    - `FastDrawerShell` and `FastModalShell` definitions are unchanged (no edits to their bodies).
    - `yarn build` completes with no type errors.
  </acceptance_criteria>
  <done>`@components/Sheet` exposes a themed bottom-sheet wrapper built on the shared FastOverlay portal system, compiling cleanly, with the drawer/modal shells untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a jest/RTL smoke proof that Sheet mounts and unmounts</name>
  <files>src/Components/Sheet/Sheet.test.tsx</files>
  <read_first>
    - src/Components/FastOverlay/FastOverlay.tsx (the Sheet just added — props + the data-testid passthrough + title/children render)
    - src/Components/Sheet/index.ts (the barrel under test)
    - src/App.test.tsx (the existing CRA jest + @testing-library/react idiom in this repo)
    - .planning/codebase/TESTING.md (jest via react-scripts test; run with CI=true --watchAll=false to avoid watch mode)
  </read_first>
  <behavior>
    - Rendering `<Sheet open title="Bộ chọn" data-testid="smoke-sheet" onClose={noop}>nội dung</Sheet>` puts an element with `data-testid="smoke-sheet"` and the text "nội dung" into the document.
    - Rendering the same with `open={false}` results in no `smoke-sheet` element in the document.
    - Clicking the close button (aria-label) invokes the supplied `onClose`.
  </behavior>
  <action>
    Create `src/Components/Sheet/Sheet.test.tsx` using `@testing-library/react` (`render`, `screen`, `fireEvent`) — the same stack as `src/App.test.tsx`. Import `Sheet` from `@components/Sheet` (proves the barrel). Write three assertions matching the behavior block: (1) open → `screen.getByTestId('smoke-sheet')` and the body text are present; (2) `open={false}` → `screen.queryByTestId('smoke-sheet')` is null; (3) clicking the close button (`getByLabelText` of the close aria-label used by the shell) calls a jest mock `onClose`. Keep it a minimal smoke proof (D-10) — do NOT mount it inside any router or migrate a real consumer. Match the 2-space indentation / quote style of `App.test.tsx`.
  </action>
  <verify>
    <automated>CI=true yarn test --watchAll=false src/Components/Sheet</automated>
  </verify>
  <acceptance_criteria>
    - `src/Components/Sheet/Sheet.test.tsx` imports `Sheet` from `@components/Sheet` (grep: present).
    - `CI=true yarn test --watchAll=false src/Components/Sheet` exits 0 with the three smoke assertions passing (mount when open, absent when closed, onClose on close-button click).
    - No test-only route or RootRouter edit was introduced (this plan does not touch `src/Routing/`).
  </acceptance_criteria>
  <done>A jest/RTL smoke test proves Sheet mounts via the barrel when open, unmounts when closed, and fires onClose — satisfying D-10's mount/open proof without a route harness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sheet portal → document.body | Sheet renders an overlay into the body via createPortal; it shares the existing overlay stacking, introducing no new global state beyond what FastOverlay already owns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-OV | Tampering | overlay z-index stacking singletons | mitigate | Sheet reuses the module-private `useResolvedOverlayZIndex`/`allocateOverlayStackToken` by co-locating in `FastOverlay.tsx`; it must NOT re-declare the singletons (Pitfall 3) — enforced by the acceptance grep. |
| T-02-ID | Information Disclosure | Sheet content surface | accept | Sheet is an empty wrapper this phase; it renders only caller-provided `children`/`title`. No data path or error detail is introduced (no consumers migrated, D-10). |
| T-02-SC | Tampering | npm/yarn installs | accept | Zero new packages (built on in-repo FastOverlay + existing @testing-library). No supply-chain checkpoint required. |
</threat_model>

<verification>
- `yarn build` clean (craco type-check) with Sheet + SheetProps exported and FastDrawer/Modal shells unchanged.
- `CI=true yarn test --watchAll=false src/Components/Sheet` green.
- Source assertion: no re-declared overlay stacking singletons (shared stack preserved, Pitfall 3).
- No `src/Routing/` changes (independence from 02-02 RootRouter edits).
</verification>

<success_criteria>
A reusable `@components/Sheet` bottom-sheet wrapper exists on the FastOverlay system (D-09), with public API covering Phase 5's picker/confirmation needs, proven to mount/open/close by a smoke test, and no consumer migrated this phase (D-10).
</success_criteria>

<output>
Create `.planning/phases/02-shell-safety-extraction/02-03-SUMMARY.md` when done.
</output>
