---
phase: 02-shell-safety-extraction
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/Routing/Shell/shellStyles.ts
  - src/Routing/Shell/PageActionsMenu.tsx
  - src/Routing/Shell/BottomTabNavigator.tsx
  - src/Routing/Shell/CookingPill.tsx
  - src/Routing/MasterPage.tsx
requirements: [FND-02]
must_haves:
  truths:
    - "PageActionsMenu, BottomTabNavigator, and CookingPill live in their own files under src/Routing/Shell/ and are imported by MasterPage"
    - "Every data-testid hook on the moved pieces is preserved verbatim (page-actions-button, bottom-tab-navigator, bottom-tab-dishes, bottom-tab-scheduled-meals, bottom-tab-suggester, bottom-tab-shopping-list, bottom-tab-expense-planner, active-cooking-floating-button)"
    - "The shell e2e baseline (app-shell-navigation.spec.ts, cooking-pill.spec.ts) still passes unchanged after the move — this re-run IS the identity proof (D-07)"
    - "MasterPage's rendered output is unchanged: only the component definitions moved out, the JSX tree it returns is identical"
    - "Relative imports are re-pathed for the one-level-deeper Shell/ location (assets ../../ -> ../../../, sibling Routing modules ./ -> ../)"
  artifacts:
    - path: "src/Routing/Shell/shellStyles.ts"
      provides: "Shared headerActionButtonStyle constant used by MasterPage, PageActionsMenu, SidebarDrawer"
      contains: "headerActionButtonStyle"
    - path: "src/Routing/Shell/PageActionsMenu.tsx"
      provides: "Extracted header overflow menu (reads usePageActionsState)"
      contains: "page-actions-button"
    - path: "src/Routing/Shell/BottomTabNavigator.tsx"
      provides: "Extracted bottom-tab navigator chrome"
      contains: "bottom-tab-navigator"
    - path: "src/Routing/Shell/CookingPill.tsx"
      provides: "Extracted floating cooking pill + cooking modals"
      contains: "active-cooking-floating-button"
  key_links:
    - from: "src/Routing/MasterPage.tsx"
      to: "src/Routing/Shell/BottomTabNavigator.tsx, CookingPill.tsx, PageActionsMenu.tsx"
      via: "imports the extracted components from ./Shell/*"
      pattern: "from \"./Shell/"
    - from: "tests/e2e/app-shell-navigation.spec.ts + cooking-pill.spec.ts"
      to: "the moved Shell components"
      via: "getByTestId against the verbatim hooks after the move"
      pattern: "bottom-tab-|active-cooking-floating-button"
---

<objective>
Begin the "pure move, verified identical" extraction (FND-02 / D-04) by moving the three smaller top-level shell pieces out of the 1366-line `src/Routing/MasterPage.tsx` into `src/Routing/Shell/`: `PageActionsMenu` (lines 299-324), `CookingPill` (826-1017), and `BottomTabNavigator` (1019-1275), plus a shared `shellStyles.ts` for the one constant used by three units (`headerActionButtonStyle`). This is a mechanical move — no logic, copy, or `data-testid` changes (D-05/D-08). The identity proof is re-running the green baseline from plan 02-01 unchanged (D-07).

SidebarDrawer + DataBackup + the MasterPage collapse + the route inventory are deliberately deferred to plan 02-05 (SidebarDrawer is ~500 lines and shares `MasterPage.tsx`, so it must be a sequential wave-3 plan to keep each move reviewable and within context budget).

Purpose: Decompose the shell incrementally so each diff reads as a move, with the green e2e baseline confirming behavior is identical after every step.
Output: `src/Routing/Shell/{shellStyles.ts, PageActionsMenu.tsx, BottomTabNavigator.tsx, CookingPill.tsx}` and a MasterPage that imports them while rendering identically.

Depends on 02-01: the move can only be "verified identical" against the trustworthy green baseline that plan 02-01 establishes (wave 1). FND-02 is proven by re-running that same baseline here.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-shell-safety-extraction/02-CONTEXT.md
@.planning/phases/02-shell-safety-extraction/02-RESEARCH.md
@.planning/phases/02-shell-safety-extraction/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@src/Routing/MasterPage.tsx
@src/Routing/AppShellNavigationContext.tsx
@src/Routing/PageActionsContext.tsx
@tests/e2e/app-shell-navigation.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shellStyles.ts + PageActionsMenu into src/Routing/Shell/</name>
  <files>src/Routing/Shell/shellStyles.ts, src/Routing/Shell/PageActionsMenu.tsx, src/Routing/MasterPage.tsx</files>
  <read_first>
    - src/Routing/MasterPage.tsx (headerActionButtonStyle L93-104; PageActionsMenu L299-324 incl. data-testid="page-actions-button" L319; the MasterPage header that renders <PageActionsMenu/> at L270 and uses headerActionButtonStyle at L250/L268)
    - src/Routing/PageActionsContext.tsx (usePageActionsState — the hook PageActionsMenu reads)
    - .planning/phases/02-shell-safety-extraction/02-PATTERNS.md (PageActionsMenu pattern block; "Relative-import re-path" + "Verbatim data-testid preservation" + "Single named export per file" shared patterns)
    - .planning/codebase/CONVENTIONS.md (single named export per file, 4-space indent in src/, double quotes)
  </read_first>
  <action>
    Create `src/Routing/Shell/shellStyles.ts` and move the `headerActionButtonStyle` constant (MasterPage L93-104) into it verbatim, exported as `export const headerActionButtonStyle`. This constant is shared by MasterPage, PageActionsMenu, and (later) SidebarDrawer — a natural shared module (RESEARCH Extraction Inventory). Do NOT move the MasterPage-only header-visual helpers here; those stay with MasterPage.
    Create `src/Routing/Shell/PageActionsMenu.tsx` and move the `PageActionsMenu` component (MasterPage L299-324) verbatim, changing `const PageActionsMenu` to `export const PageActionsMenu`. Preserve `data-testid="page-actions-button"` (L319) and the `if (actions.length === 0) return null;` early return byte-for-byte. Re-path the relative import `./PageActionsContext` -> `../PageActionsContext` (one level deeper); keep alias imports (`@components/Button`) unchanged; import `headerActionButtonStyle` from `./shellStyles`. Add the antd `Dropdown` + `MoreOutlined` imports it needs.
    In `src/Routing/MasterPage.tsx`: delete the now-moved `PageActionsMenu` definition and the `headerActionButtonStyle` constant; add `import { PageActionsMenu } from "./Shell/PageActionsMenu";` and `import { headerActionButtonStyle } from "./Shell/shellStyles";`. Do NOT change the MasterPage JSX tree (it still renders `<PageActionsMenu />` and passes `headerActionButtonStyle`). Remove any now-unused imports (e.g. `Dropdown`, `MoreOutlined`) only if no longer referenced in MasterPage.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `src/Routing/Shell/shellStyles.ts` exports `headerActionButtonStyle` (grep present); the constant no longer defined in `MasterPage.tsx` (grep `const headerActionButtonStyle` in MasterPage returns 0).
    - `src/Routing/Shell/PageActionsMenu.tsx` defines `export const PageActionsMenu` and contains `data-testid="page-actions-button"` verbatim (grep present); it imports from `../PageActionsContext` (not `./PageActionsContext`).
    - `MasterPage.tsx` imports `PageActionsMenu` from `./Shell/PageActionsMenu` and `headerActionButtonStyle` from `./Shell/shellStyles` (grep present), and no longer defines `PageActionsMenu` locally.
    - `yarn build` passes (craco type-check catches any broken relative path — Pitfall 2).
  </acceptance_criteria>
  <done>PageActionsMenu and the shared header button style live under Shell/, MasterPage imports them, and the build is clean with the page-actions-button hook preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Extract BottomTabNavigator and CookingPill, then re-run the baseline as the identity proof</name>
  <files>src/Routing/Shell/BottomTabNavigator.tsx, src/Routing/Shell/CookingPill.tsx, src/Routing/MasterPage.tsx</files>
  <read_first>
    - src/Routing/MasterPage.tsx (CookingPill L826-1017 incl. active-cooking-floating-button L869 and the DeferredModalContent+CookingSessionWidget modals; BottomTabNavigator L1019-1275 incl. all bottom-tab-* hooks L1203-1262 and the _containerStyles/_dockStyles/_buttonStyles closures; the asset imports L34-50 and sibling-module imports ./AppShellNavigationContext / ./RootRoutes L54-56)
    - src/Routing/AppShellNavigationContext.tsx (useAppShellNavigation — consumed by both moved pieces)
    - .planning/phases/02-shell-safety-extraction/02-PATTERNS.md (BottomTabNavigator + CookingPill pattern blocks; the CRITICAL re-path gotcha: assets ../../assets -> ../../../assets, ./AppShellNavigationContext/./RootRoutes -> ../...; "Deferred / eager render perf idioms — preserve verbatim")
    - tests/e2e/app-shell-navigation.spec.ts and tests/e2e/cooking-pill.spec.ts (the green baseline that must still pass after the move — DO NOT edit these specs)
  </read_first>
  <action>
    Create `src/Routing/Shell/CookingPill.tsx` and move the `CookingPill` component (MasterPage L826-1017) verbatim as `export const CookingPill`. Preserve `data-testid="active-cooking-floating-button"` (L869), the `if (activeSessions.length === 0) return null;` guard (L835), and every `DeferredModalContent` gate (preserve the timing-sensitive perf idiom). Re-path: alias imports (`@store/Selectors`, `@components/Modal`, `@modules/Dishes/Screens/CookingSession.widget`, `@components/Typography`, `@components/Layout/Space`) unchanged; this unit has no `../../assets` or sibling-Routing relative imports, but verify against source and re-path any that exist.
    Create `src/Routing/Shell/BottomTabNavigator.tsx` and move the `BottomTabNavigator` component (MasterPage L1019-1275) verbatim as `export const BottomTabNavigator`. Preserve every `data-testid` (`bottom-tab-navigator`, `bottom-tab-dishes`, `bottom-tab-scheduled-meals`, `bottom-tab-suggester`, `bottom-tab-shopping-list`, `bottom-tab-expense-planner`) and the `_*Styles` closures and the `onNavigate` active-state guard (L1197-1200) byte-for-byte. RE-PATH the asset imports it uses (`DishesIcon`, `DietPlanIcon`, `SuggesterIcon`, `ShoppingListIcon`, `BudgetIcon`) from `../../assets/icons/*.png` -> `../../../assets/icons/*.png`, and `./AppShellNavigationContext` -> `../AppShellNavigationContext`, `./RootRoutes` -> `../RootRoutes`. Carry the `DishSuggesterScreen` alias import and the `useToggle`/`useLocation` imports.
    In `MasterPage.tsx`: delete the now-moved `CookingPill` and `BottomTabNavigator` definitions; add `import { BottomTabNavigator } from "./Shell/BottomTabNavigator";` and `import { CookingPill } from "./Shell/CookingPill";`. The MasterPage JSX (`<BottomTabNavigator />` L292, `<CookingPill />` L293) is unchanged. Remove imports now unused only by these moved units from MasterPage (e.g. icons/selectors that MasterPage itself no longer references) — but keep anything SidebarPrimary/MasterPage still uses (SidebarDrawer is NOT moved in this plan). Run `yarn build`, then re-run the baseline specs unchanged: this re-run is the D-07 identity proof.
  </action>
  <verify>
    <automated>yarn test:e2e tests/e2e/app-shell-navigation.spec.ts tests/e2e/cooking-pill.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/Routing/Shell/BottomTabNavigator.tsx` defines `export const BottomTabNavigator`, contains all six `bottom-tab-*` data-testid strings verbatim, and imports assets from `../../../assets/` and sibling modules from `../AppShellNavigationContext` / `../RootRoutes` (grep: no `../../assets/` and no `./AppShellNavigationContext` remain in this file).
    - `src/Routing/Shell/CookingPill.tsx` defines `export const CookingPill` and contains `active-cooking-floating-button` verbatim plus the `DeferredModalContent` gates.
    - `MasterPage.tsx` imports both from `./Shell/*` and no longer defines them locally; its returned JSX tree is unchanged (still renders `<BottomTabNavigator />` and `<CookingPill />` in the same positions).
    - `yarn build` passes AND `yarn test:e2e tests/e2e/app-shell-navigation.spec.ts tests/e2e/cooking-pill.spec.ts` exits 0 with the specs unchanged from plan 02-01 (the identity proof).
  </acceptance_criteria>
  <done>BottomTabNavigator and CookingPill are moved into Shell/ with re-pathed imports and verbatim test ids, and the unchanged baseline specs still pass green — proving the move is behavior-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| moved component → existing contexts/store | Extracted Shell/* components consume the same AppShellNavigationContext / @store selectors; the move must not alter what crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-MV | Tampering | mechanical move fidelity (test ids, relative paths) | mitigate | Preserve every data-testid verbatim (D-08) and re-path relative imports for the one-level-deeper location (Pitfall 2); enforced by acceptance greps + the re-run baseline e2e (D-07). |
| T-02-RG | Repudiation / regression | behavior drift during "pure move" | mitigate | The unchanged 02-01 baseline specs are re-run as the identity proof; any behavior change surfaces as a spec failure. No logic edits permitted (D-05). |
| T-02-SC | Tampering | npm/yarn installs | accept | Zero new packages (pure code move). No supply-chain checkpoint required. |
</threat_model>

<verification>
- `yarn build` clean after each extraction (craco type-check surfaces broken relative paths — Pitfall 2; restart the dev server / kill port 3010 before e2e per Pitfall 6).
- `yarn test:e2e tests/e2e/app-shell-navigation.spec.ts tests/e2e/cooking-pill.spec.ts` green with the specs unchanged (D-07 identity proof).
- Source assertions: verbatim data-testid preservation (D-08) and re-pathed relative imports (no stale `../../assets/` or `./AppShellNavigationContext` in moved files).
- MasterPage rendered JSX tree unchanged (manual diff: only definitions removed + imports added).
</verification>

<success_criteria>
PageActionsMenu, BottomTabNavigator, and CookingPill are extracted into `src/Routing/Shell/` with a shared `shellStyles.ts`, MasterPage imports them and renders identically, and the green baseline from 02-01 passes unchanged — proving FND-02 identity for these three pieces.
</success_criteria>

<output>
Create `.planning/phases/02-shell-safety-extraction/02-04-SUMMARY.md` when done.
</output>
