---
phase: 02-shell-safety-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/app-shell-navigation.spec.ts
  - tests/e2e/cooking-pill.spec.ts
  - tests/e2e/fixtures/testData.ts
autonomous: true
requirements: [FND-02]
must_haves:
  truths:
    - "The shell e2e specs pass green on a clean checkout before any extraction begins"
    - "Drawer primary-nav assertions match actual source labels (expense planner is 'Tính chi phí', not 'Kế hoạch chi phí')"
    - "Backup-center labels ('Sao lưu cá nhân', sync button 'Đồng bộ mới') are asserted inside the modal that opens after clicking 'Dữ liệu & sao lưu', not in the always-rendered tools region"
    - "The cooking pill is exercised: a seeded cooking session renders 'active-cooking-floating-button' and opens the cooking modal"
    - "Bottom-tab active-state is asserted for the active route"
  artifacts:
    - path: "tests/e2e/app-shell-navigation.spec.ts"
      provides: "Repaired + extended shell navigation baseline spec"
      contains: "Tính chi phí"
    - path: "tests/e2e/cooking-pill.spec.ts"
      provides: "Cooking pill render + modal-open coverage"
      contains: "active-cooking-floating-button"
    - path: "tests/e2e/fixtures/testData.ts"
      provides: "A status:'cooking' session in the regression seed"
      contains: "cooking"
  key_links:
    - from: "tests/e2e/app-shell-navigation.spec.ts"
      to: "src/Routing/MasterPage.tsx"
      via: "getByTestId against the verbatim data-testid hooks"
      pattern: "getByTestId\\('sidebar-drawer"
    - from: "tests/e2e/cooking-pill.spec.ts"
      to: "src/Routing/MasterPage.tsx CookingPill"
      via: "seeded cooking session drives selectCookingSessions"
      pattern: "active-cooking-floating-button"
---

<objective>
Establish a real, green Playwright baseline for the shell interactions BEFORE any extraction. This is Wave 0 work and gates the entire phase (RESEARCH Pitfall 1): the existing `app-shell-navigation.spec.ts` asserts strings that do not exist in the current `src/Routing/MasterPage.tsx` source, so the "before/after identity" proof (D-07) is meaningless until the baseline is actually green.

Repair the stale assertions to match current source (extraction is "verified identical", so source is the source of truth — RESEARCH Assumption A1 default), close the two coverage gaps the identity proof depends on (cooking pill, bottom-tab active state), and confirm the shell specs run green on a clean checkout.

Purpose: A trustworthy green baseline is the only thing that makes the post-extraction re-run a valid identity proof for FND-02.
Output: Repaired `app-shell-navigation.spec.ts`, new `cooking-pill.spec.ts`, a seeded cooking session in the e2e fixtures.
</objective>

## Artifacts this phase produces

This phase (across all plans 02-01 .. 02-05) creates the following NEW symbols/paths. They are net-new and should not be flagged as drift:

- `tests/e2e/cooking-pill.spec.ts`, `tests/e2e/error-boundary.spec.ts` (new e2e specs), `src/Components/Sheet/Sheet.test.tsx` (new jest/RTL smoke test, produced by 02-03 per D-10)
- A cooking-session entry + `TEST_IDS.cookingSessions.*` in `tests/e2e/fixtures/testData.ts`
- `src/Components/ErrorBoundary/ErrorBoundary.tsx` (class `ErrorBoundary`), `src/Components/ErrorBoundary/index.ts` barrel
- A test-only render-throw probe component + its route registration (e2e crash trigger)
- `Sheet` component + `SheetProps` type (added inside `src/Components/FastOverlay/FastOverlay.tsx`, re-exported), `src/Components/Sheet/index.ts` barrel (`@components/Sheet`)
- `src/Routing/Shell/` directory with exports: `BottomTabNavigator`, `CookingPill`, `SidebarDrawer`, `PageActionsMenu`, `DataBackup`, and optional `shellStyles.ts` (e.g. `headerActionButtonStyle`)
- `src/Routing/MasterPage.tsx` collapsed to a thin composition root
- `.planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md` (route reachability doc)

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shell-safety-extraction/02-CONTEXT.md
@.planning/phases/02-shell-safety-extraction/02-RESEARCH.md
@.planning/phases/02-shell-safety-extraction/02-PATTERNS.md
@.planning/codebase/TESTING.md
@tests/e2e/app-shell-navigation.spec.ts
@tests/e2e/fixtures/appTest.ts
@tests/e2e/fixtures/seedApp.ts
@tests/e2e/fixtures/testData.ts
@src/Routing/MasterPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair the stale app-shell-navigation baseline spec to match current source</name>
  <files>tests/e2e/app-shell-navigation.spec.ts</files>
  <read_first>
    - tests/e2e/app-shell-navigation.spec.ts (the stale spec being repaired)
    - src/Routing/MasterPage.tsx (source of truth for labels and data-testid hooks)
    - tests/e2e/fixtures/appTest.ts (the seeded `test`/`expect` fixture idiom)
  </read_first>
  <action>
    Run `yarn test:e2e tests/e2e/app-shell-navigation.spec.ts` on the clean checkout first to observe the actual red assertions and confirm RESEARCH Pitfall 1. Then repair the assertions to match the CURRENT source (do not change source — extraction is "verified identical", source is truth):
    - The expense-planner primary-nav label is `Tính chi phí` (MasterPage.tsx L502), NOT `Kế hoạch chi phí`. Change the `primaryNav` containText assertion to `Tính chi phí`.
    - `Lịch sử nấu ăn` is a PRIMARY-NAV item (MasterPage.tsx L498), not a tools-region item. Move/keep that assertion against `sidebar-drawer-primary-nav`, not `sidebar-drawer-tools`.
    - `Sao lưu cá nhân` (L795) and the sync button `Đồng bộ mới` (L672) live INSIDE the backup-center Modal that only opens after clicking `Dữ liệu & sao lưu` (L573/L578). The spec must click `Dữ liệu & sao lưu` to open that modal before asserting those strings — they are NOT in the always-rendered `sidebar-drawer-tools` region. Replace the stale `tools` containText for `Đồng bộ dữ liệu mới`/`Sao lưu cá nhân` with: open the backup-center modal, then assert it contains `Đồng bộ mới` and `Sao lưu cá nhân`.
    - Keep the genuinely-present tools assertions (`Hướng dẫn sử dụng`, the Admin login/state text) and the route-feedback behavior test (second `test()` block) unchanged — they already match source.
    - Add bottom-tab active-state assertions: navigate to a known route, open via the corresponding `bottom-tab-*` button, and assert the active tab reflects the current route (use the existing `bottom-tab-navigator` + `bottom-tab-dishes`/`bottom-tab-suggester` hooks; assert active styling/aria or selected state as rendered by `BottomTabNavigator`).
    Preserve the `import { expect, test } from './fixtures/appTest'` idiom; do not add new fixtures.
  </action>
  <verify>
    <automated>yarn test:e2e tests/e2e/app-shell-navigation.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `yarn test:e2e tests/e2e/app-shell-navigation.spec.ts` exits 0 (all tests green) on a clean checkout with no source changes to `src/`.
    - The spec asserts `Tính chi phí` (grep: `grep -c 'Tính chi phí' tests/e2e/app-shell-navigation.spec.ts` returns ≥ 1) and contains NO occurrence of `Kế hoạch chi phí` (grep returns 0).
    - The spec clicks `Dữ liệu & sao lưu` before asserting `Đồng bộ mới` / `Sao lưu cá nhân` (those assertions are scoped to the opened modal, not `sidebar-drawer-tools`).
    - At least one assertion verifies a `bottom-tab-*` active state for the current route.
  </acceptance_criteria>
  <done>The shell navigation baseline spec passes green against current source, with labels and modal-scoped assertions corrected, plus bottom-tab active-state coverage added.</done>
</task>

<task type="auto">
  <name>Task 2: Seed a cooking session and add cooking-pill coverage</name>
  <files>tests/e2e/fixtures/testData.ts, tests/e2e/cooking-pill.spec.ts</files>
  <read_first>
    - tests/e2e/fixtures/testData.ts (seed shape: `createRegressionSeed`, `TEST_IDS`)
    - tests/e2e/fixtures/seedApp.ts (how shared/personal persist roots are written to IndexedDB)
    - src/Routing/MasterPage.tsx (CookingPill at L826-1017: early `if (activeSessions.length === 0) return null`, `active-cooking-floating-button` L869, the `DeferredModalContent`+`CookingSessionWidget` modal)
    - src/Store/Selectors.ts entry for `selectCookingSessions` (confirm which persist root/slice holds cooking sessions and the session shape, including a `status` of `cooking`)
  </read_first>
  <action>
    In `tests/e2e/fixtures/testData.ts`, add a cooking session with `status: "cooking"` referencing the existing `dishes.comGa` (`dish-com-ga`) so `selectCookingSessions` returns a non-empty array and `selectDishesById` resolves the focused dish. Add a `TEST_IDS.cookingSessions.active` id for it. Match the session field shape used by the store reducer/selector (inspect `selectCookingSessions` and its slice to get the exact field names — do NOT invent a shape). Wire the session into `createRegressionSeed()` so `seedApp(page)` persists it into the correct root (`shared` vs `personal` per the selector source).
    Create `tests/e2e/cooking-pill.spec.ts` using the `./fixtures/appTest` idiom: after seed, navigate to `./`, assert `active-cooking-floating-button` is visible, click it, and assert the cooking modal renders the `CookingSessionWidget` content (assert a stable visible element from that widget, e.g. the dish name `Cơm gà` from the seeded dish). Do NOT add new `data-testid`s to source — only consume existing hooks.
  </action>
  <verify>
    <automated>yarn test:e2e tests/e2e/cooking-pill.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/fixtures/testData.ts` contains a session with `status: "cooking"` (grep: `grep -c "cooking" tests/e2e/fixtures/testData.ts` returns ≥ 1) and a `TEST_IDS.cookingSessions` entry.
    - `yarn test:e2e tests/e2e/cooking-pill.spec.ts` exits 0: `active-cooking-floating-button` is visible after seed, and clicking it opens a modal showing the seeded dish content.
    - No `data-testid` was added or renamed in `src/`.
  </acceptance_criteria>
  <done>A seeded cooking session makes the pill render, and a green spec proves the pill appears and opens its cooking modal — closing the FND-02 pill coverage gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → IndexedDB | e2e seed writes JSON into the app's `persist:shared`/`persist:personal` roots; test-only, not shipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | e2e seed data in `testData.ts` | accept | Test-only fixture; never bundled into the app. No production data path touched. |
| T-02-SC | Tampering | npm/yarn installs | mitigate | This plan installs ZERO new packages (RESEARCH: phase installs nothing). No supply-chain checkpoint required. |
</threat_model>

<verification>
- `yarn test:e2e tests/e2e/app-shell-navigation.spec.ts` green on clean checkout.
- `yarn test:e2e tests/e2e/cooking-pill.spec.ts` green.
- Note (RESEARCH Pitfall 6): `playwright.config.ts` sets `reuseExistingServer: true` and `start` runs with `FAST_REFRESH=false`. If a stale dev server is running, kill it (port 3010) before the run so specs test current code.
- No changes under `src/` (this plan is test/fixtures only): `git diff --name-only src/` returns nothing.
</verification>

<success_criteria>
The shell e2e baseline (navigation + drawer + backup-center modal + cooking pill + bottom-tab active state) is green on a clean checkout against current source, providing a trustworthy "before" for the extraction identity proof.
</success_criteria>

<output>
Create `.planning/phases/02-shell-safety-extraction/02-01-SUMMARY.md` when done.
</output>
