---
phase: 02-shell-safety-extraction
plan: 05
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - src/Routing/Shell/SidebarDrawer.tsx
  - src/Routing/Shell/DataBackup.tsx
  - src/Routing/MasterPage.tsx
  - .planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md
requirements: [FND-02]
must_haves:
  truths:
    - "SidebarDrawer moves INTACT into src/Routing/Shell/SidebarDrawer.tsx as a single unit (nav + backup-center modal + admin publish + inventory config + PIN modal) with no internal refactor (D-05)"
    - "The dead DataBackup export moves into src/Routing/Shell/DataBackup.tsx preserving its export signature, flagged as currently-unused with a hardcoded GitHub raw URL, NOT deleted (D-06)"
    - "MasterPage.tsx collapses to a thin composition root (the MasterPage function + its own header-visual helpers), importing all five Shell/* pieces, rendering an identical JSX tree"
    - "Every SidebarDrawer data-testid is preserved verbatim (sidebar-drawer-button, sidebar-drawer, sidebar-drawer-primary-nav, sidebar-drawer-tools, sidebar-nav-group-*, sidebar-nav-*) and all 16 asset imports are re-pathed ../../ -> ../../../"
    - "ROUTE-INVENTORY.md lists every pre-refactor route + its entry path (bottom-tab / sidebar-nav / header / programmatic) ready to gate Phase 4 nav changes, and notes the dead DataBackup"
    - "The full shell e2e baseline from 02-01 passes unchanged after the collapse — the identity proof for the whole extraction (D-07)"
  artifacts:
    - path: "src/Routing/Shell/SidebarDrawer.tsx"
      provides: "Intact sidebar drawer (nav + backup center + admin + PIN), moved as one unit"
      contains: "sidebar-drawer"
    - path: "src/Routing/Shell/DataBackup.tsx"
      provides: "Moved (dead) DataBackup export, preserved + flagged per D-06"
      contains: "export const DataBackup"
    - path: "src/Routing/MasterPage.tsx"
      provides: "Thin composition root importing all Shell/* pieces"
      contains: "./Shell/SidebarDrawer"
    - path: ".planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md"
      provides: "Route reachability inventory gating Phase 4 nav changes"
      contains: "Entry path"
  key_links:
    - from: "src/Routing/MasterPage.tsx"
      to: "src/Routing/Shell/SidebarDrawer.tsx"
      via: "header renders <SidebarDrawer buttonStyle={headerActionButtonStyle} />"
      pattern: "SidebarDrawer"
    - from: "src/Routing/Shell/SidebarDrawer.tsx"
      to: "../AppShellNavigationContext / ../RootRoutes / ../../../assets/icons"
      via: "re-pathed relative imports for the one-level-deeper Shell/ location"
      pattern: "\\.\\./\\.\\./\\.\\./assets"
    - from: ".planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md"
      to: "src/Routing/RootRouter.tsx + sidebarNavGroups"
      via: "derived route + entry-path table"
      pattern: "sidebar-nav-|bottom-tab-"
---

<objective>
Complete the "pure move, verified identical" extraction (FND-02 / D-04) by moving the two largest remaining units out of `src/Routing/MasterPage.tsx`: `SidebarDrawer` (lines 326-824, ~500 lines) moved INTACT as a single block (D-05), and the dead `DataBackup` export (lines 1277-1366) moved + preserved + flagged (D-06). After both moves, MasterPage collapses to a thin composition root — the `MasterPage` function plus the header-visual helpers only it uses — importing all five `Shell/*` pieces and rendering an identical JSX tree. Finally, produce `ROUTE-INVENTORY.md`, the route reachability doc that gates Phase 4 nav changes. The full green baseline from 02-01 re-run unchanged is the identity proof (D-07).

Purpose: Finish decomposing the 1366-line shell into reviewable per-component files so journey/nav/mobile work can proceed on a stable, crash-contained base — without any behavior change.
Output: `src/Routing/Shell/{SidebarDrawer.tsx, DataBackup.tsx}`, a thin `MasterPage.tsx`, and `.planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md`.

Depends on 02-04: this plan shares `MasterPage.tsx` with 02-04 (file-ownership conflict forces a later wave) and continues the same extraction, so it runs in wave 3 after the leaf pieces are out. FND-02 is proven by re-running the same 02-01 baseline.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-shell-safety-extraction/02-CONTEXT.md
@.planning/phases/02-shell-safety-extraction/02-RESEARCH.md
@.planning/phases/02-shell-safety-extraction/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@src/Routing/MasterPage.tsx
@src/Routing/RootRouter.tsx
@src/Routing/RootRoutes.ts
@src/Routing/AppShellNavigationContext.tsx
@tests/e2e/app-shell-navigation.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move SidebarDrawer INTACT into src/Routing/Shell/SidebarDrawer.tsx</name>
  <files>src/Routing/Shell/SidebarDrawer.tsx, src/Routing/MasterPage.tsx</files>
  <read_first>
    - src/Routing/MasterPage.tsx (SidebarDrawer L326-824 as ONE block: nav L545-565, tools region L567-632, PIN modal L634-652, backup-center modal L653-808, admin publish L750-790, ScheduledMealToolkitWidget + SharedSyncModal tail L809-821; co-located helpers drawerToolsPlaceholderStyle/sidebarNavListStyle/sidebarNavGroupStyle/sidebarNavSectionLabelStyle L62-89, APP_CONFIRM_Z_INDEX L91, sidebarNavButtonStyle L165-182, useDeferredDrawerTools L184-211; the 16 PNG asset imports L34-50; sibling imports ./AppShellNavigationContext L54, ./RootRoutes L56)
    - src/Routing/AppShellNavigationContext.tsx (useAppShellNavigation — consumed by the drawer's onNavigate)
    - .planning/phases/02-shell-safety-extraction/02-PATTERNS.md (SidebarDrawer pattern block — MOVE INTACT D-05; the "Relative-import re-path" + "Verbatim data-testid preservation" + "Deferred / eager render perf idioms" + "Reload-as-recovery idiom (keep, do NOT fix)" shared patterns)
    - .planning/phases/02-shell-safety-extraction/02-RESEARCH.md (Extraction Inventory: SidebarDrawer import contract + the CRITICAL re-path gotcha; Pitfall 2 broken relative imports; security note: admin/PIN/token logic moves untouched)
    - .planning/codebase/CONVENTIONS.md (single named export per file, 4-space indent in src/, double quotes)
  </read_first>
  <action>
    Create `src/Routing/Shell/SidebarDrawer.tsx` and move the entire `SidebarDrawer` component (MasterPage L326-824) as ONE block, changing `const SidebarDrawer = ({ buttonStyle })` to `export const SidebarDrawer = ({ buttonStyle }: { buttonStyle?: React.CSSProperties })`. D-05 CONSTRAINT: move intact — the PIN modal, backup-center modal, admin publish section, inventory-config section, and the `ScheduledMealToolkitWidget` + `SharedSyncModal` tail all stay inside this single component. NO internal splitting, NO "while I'm here" cleanups, NO logic/copy changes. Preserve every `data-testid` verbatim: `sidebar-drawer-button` (L526), `sidebar-drawer` (L541), `sidebar-drawer-primary-nav` (L545), `sidebar-drawer-tools` (L567), `sidebar-nav-group-${group.key}` (L548), `sidebar-nav-${item.key}` (L554). Keep the `useDeferredDrawerTools` double-`requestAnimationFrame` timing and the `window.location.reload()` in `onUnlock`/`onLock` (L389/L397) UNCHANGED — the reload-as-recovery fix is out of scope (Deferred Ideas).
    Co-locate the SidebarDrawer-only helpers into this file (move them verbatim from MasterPage): `drawerToolsPlaceholderStyle`, `sidebarNavListStyle`, `sidebarNavGroupStyle`, `sidebarNavSectionLabelStyle` (L62-89), `APP_CONFIRM_Z_INDEX` (L91), `sidebarNavButtonStyle` (L165-182), `useDeferredDrawerTools` (L184-211). Import the shared `headerActionButtonStyle` from `./shellStyles` only if the drawer references it (it receives `buttonStyle` as a prop, so it likely does not need to import it — verify against source and do not add an unused import).
    RE-PATH all relative imports for the one-level-deeper location (Pitfall 2 / RESEARCH gotcha): all 16 PNG asset imports `../../assets/icons/*.png` -> `../../../assets/icons/*.png`; `./AppShellNavigationContext` -> `../AppShellNavigationContext`; `./RootRoutes` -> `../RootRoutes`. Keep ALL alias imports unchanged (`@components/*`, `@store/*`, `@hooks`, `@modules/*`). Carry the full SidebarDrawer import contract (useDispatch/useSelector, useAdminMode/useSharedPublish/useSharedDataSync/SyncedVersions, useMessage, useModal, Modal/DeferredModalContent, FastDrawerShell, SharedSyncModal, GistBackupWidget, ScheduledMealToolkitWidget, selectInventoryHealthConfig, inventory models + updateInventoryConfig, NumberStepper/SmartForm/TextArea/Image/ActionButton/Button/Box/Space/Stack/Flex/Divider, the @ant-design/icons used, useLocation).
    In `MasterPage.tsx`: delete the moved `SidebarDrawer` definition AND the co-located helpers listed above; add `import { SidebarDrawer } from "./Shell/SidebarDrawer";`. Do NOT change the header JSX that renders `<SidebarDrawer buttonStyle={headerActionButtonStyle} />` (L250). Leave DataBackup and the MasterPage collapse to Task 2.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `src/Routing/Shell/SidebarDrawer.tsx` defines `export const SidebarDrawer` and contains all six SidebarDrawer data-testid strings verbatim (`sidebar-drawer-button`, `sidebar-drawer`, `sidebar-drawer-primary-nav`, `sidebar-drawer-tools`, `sidebar-nav-group-`, `sidebar-nav-`) — grep returns ≥ 1 each.
    - Asset imports use `../../../assets/` and sibling modules use `../AppShellNavigationContext` / `../RootRoutes`; no `../../assets/` and no `./AppShellNavigationContext` / `./RootRoutes` remain in the file (grep returns 0 for the stale forms).
    - The backup-center modal, PIN modal, admin publish section, and `ScheduledMealToolkitWidget`/`SharedSyncModal` tail are all present in this one file (move intact, D-05) — grep `Dữ liệu & sao lưu`, `Nhập mã PIN`, `Xuất bản dữ liệu dùng chung`, `ScheduledMealToolkitWidget` each return ≥ 1.
    - `MasterPage.tsx` imports `SidebarDrawer` from `./Shell/SidebarDrawer` and no longer defines it (or the co-located drawer helpers) locally.
    - `yarn build` passes (craco type-check surfaces any broken re-path — Pitfall 2).
  </acceptance_criteria>
  <done>SidebarDrawer lives intact in Shell/ as a single reviewable unit with re-pathed imports and verbatim test ids, MasterPage imports it, and the build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Move the dead DataBackup (flag, don't delete), collapse MasterPage to a thin root, re-run the full baseline</name>
  <files>src/Routing/Shell/DataBackup.tsx, src/Routing/MasterPage.tsx</files>
  <read_first>
    - src/Routing/MasterPage.tsx (DataBackup L1277-1366 incl. the hardcoded raw.githubusercontent.com URL L1306 and `_restorePersonalFromText` reload idiom L1295; the keeper MasterPage function L213-297 and its header-visual helpers layoutStyles L58-60 + HeaderVisual/defaultHeaderVisual/headerVisualByFeatureName/getHeaderVisualByPath/getHeaderVisual/createHeaderBackground/getHeaderDateLabel L106-163)
    - .planning/phases/02-shell-safety-extraction/02-PATTERNS.md (DataBackup pattern block — DEAD EXPORT D-06, preserve export + flag, do NOT delete; the thin MasterPage composition-root shape)
    - .planning/phases/02-shell-safety-extraction/02-RESEARCH.md (D-06 finding: DataBackup is confirmed dead — single self-reference; Deferred Ideas: hardcoded URL flagged for follow-up, not changed; security note T-02 below)
    - 02-04-PLAN.md "Artifacts this phase produces" expectations + the already-moved Shell/* imports (PageActionsMenu, BottomTabNavigator, CookingPill, shellStyles) from plan 02-04
  </read_first>
  <action>
    First, re-confirm DataBackup is dead before moving: grep the tree for `DataBackup` and verify the only references are its definition (and, after the move, its new file) — per D-06 / RESEARCH Assumption A3. Create `src/Routing/Shell/DataBackup.tsx` and move the `DataBackup` component (MasterPage L1277-1366) verbatim, preserving the `export const DataBackup` signature. Add a top-of-component code comment flagging it as currently-unused (no in-app caller) and containing a hardcoded GitHub raw URL at the fetch site (do NOT change the URL — Deferred Ideas; this is tracked for a separate follow-up, not fixed here). Re-path relative imports if any (its imports are all aliases — `@common/*`, `@components/*`, `react-copy-to-clipboard` — so verify and change only what is relative). Do NOT delete it.
    Collapse `MasterPage.tsx` to a thin composition root: after removing DataBackup, the file should contain only the `MasterPage` function (L213-297) plus the header-visual helpers it alone uses (`layoutStyles`, `HeaderVisual` type, `defaultHeaderVisual`, `headerVisualByFeatureName`, `getHeaderVisualByPath`, `getHeaderVisual`, `createHeaderBackground`, `getHeaderDateLabel`) and the imports MasterPage itself still needs. Ensure the five Shell imports are present: `SidebarDrawer`, `PageActionsMenu` (from 02-04), `BottomTabNavigator`, `CookingPill` (from 02-04), and `headerActionButtonStyle` from `./Shell/shellStyles`. Add `import { DataBackup } from "./Shell/DataBackup";` ONLY if MasterPage references it — it does not render DataBackup, so do NOT add an unused import; the export is consumed (or not) independently of MasterPage. Prune every now-unused import from MasterPage (icons, selectors, antd pieces, hooks no longer referenced by the thin root). The returned JSX tree of `MasterPage` must be byte-for-byte identical to before (Header + Stack chrome + global-search button L262-269 + `<PageActionsMenu/>` + offline banner + `<Outlet/>` + `<BottomTabNavigator/>` + `<CookingPill/>` + the GlobalSearchScreen toggle). Run `yarn build`, restart the dev server / free port 3010 (Pitfall 6), then re-run the FULL shell baseline unchanged as the D-07 identity proof.
  </action>
  <verify>
    <automated>yarn test:e2e tests/e2e/app-shell-navigation.spec.ts tests/e2e/cooking-pill.spec.ts tests/e2e/global-search.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/Routing/Shell/DataBackup.tsx` defines `export const DataBackup` verbatim and carries a code comment flagging it as currently-unused with a hardcoded GitHub raw URL; the URL string `raw.githubusercontent.com/quantran-epi/my-recipes` is preserved unchanged (grep present), and DataBackup is NOT deleted.
    - `MasterPage.tsx` no longer defines `SidebarDrawer`, `PageActionsMenu`, `CookingPill`, `BottomTabNavigator`, or `DataBackup` (grep for each `const <Name> = ` / `export const DataBackup` in MasterPage returns 0); it imports `SidebarDrawer`, `PageActionsMenu`, `BottomTabNavigator`, `CookingPill` from `./Shell/*` and `headerActionButtonStyle` from `./Shell/shellStyles`.
    - `MasterPage.tsx` retains the `MasterPage` function and its header-visual helpers; its returned JSX tree is unchanged (still renders `global-search-button`, `<PageActionsMenu />`, the offline banner, `<Outlet />`, `<BottomTabNavigator />`, `<CookingPill />`, and the GlobalSearchScreen toggle in the same order).
    - `yarn build` passes AND `yarn test:e2e tests/e2e/app-shell-navigation.spec.ts tests/e2e/cooking-pill.spec.ts tests/e2e/global-search.spec.ts` exits 0 with the specs unchanged from 02-01 (the full identity proof for the extraction).
  </acceptance_criteria>
  <done>DataBackup is preserved + flagged in Shell/, MasterPage is a thin composition root importing all five Shell pieces with an identical render tree, and the full unchanged baseline passes green — proving the whole decomposition is behavior-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Write the route reachability inventory (ROUTE-INVENTORY.md)</name>
  <files>.planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md</files>
  <read_first>
    - src/Routing/RootRouter.tsx (the <Routes> tree: two lazy routes OUTSIDE MasterPage — UserGuideWelcome, UserGuideTour; everything else nested under the Root() layout route rendering <PageActionsProvider><MasterPage/></PageActionsProvider>)
    - src/Routing/RootRoutes.ts (the RootRoutes.AuthorizedRoutes route table — path builders for each route)
    - src/Routing/Shell/SidebarDrawer.tsx (the sidebarNavGroups array — maps nav item key -> route -> label, the primary entry-path source; data-testid sidebar-nav-${key})
    - src/Routing/Shell/BottomTabNavigator.tsx (the five bottom-tab entry points: dishes, scheduled-meals, suggester, shopping-list, expense-planner)
    - .planning/phases/02-shell-safety-extraction/02-PATTERNS.md (Route reachability inventory block — derive from RootRouter + RootRoutes + sidebarNavGroups; note the dead DataBackup)
    - .planning/REQUIREMENTS.md (NAV-02/NAV-03 — every pre-refactor route must stay reachable; this inventory is the gate)
  </read_first>
  <action>
    Create `.planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md` as a derived markdown doc (format is Claude's discretion per CONTEXT — a table is fine). Enumerate EVERY route in the app from `RootRouter.tsx` + `RootRoutes.ts`: the two lazy routes outside MasterPage (`UserGuideWelcome`, `UserGuideTour`) and every route nested under the `Root()` layout route (index Dashboard, Analytics, CookingHistory, DishSuggester, HouseholdProfiles, SmartMealPlanner, NutritionGoals, UserGuide, Templates, SyncBackupHealth, ExpensePlanner, and the Ingredient / Dishes / ShoppingList / ScheduledMeal sub-routers and their child routes). For each route record: the route path (from RootRoutes), the screen component, and its primary ENTRY PATH(S) — bottom-tab button (`bottom-tab-*`), sidebar-nav item (`sidebar-nav-${key}` with its group + label from `sidebarNavGroups`), header button (global search), or programmatic/redirect (e.g. the welcome-route redirect in MasterPage, detail routes reached from a list). Mark routes that have NO direct nav entry (reached only via in-screen navigation or query params) so Phase 4 knows they depend on ~3-tap/search reachability (NAV-02/NAV-03). Add a short "Notes" section flagging the dead `DataBackup` export (D-06): currently unused, hardcoded GitHub raw URL, tracked for follow-up, not wired to any route. State explicitly that this inventory is the baseline gate for Phase 4 nav changes. Also list the test-only render-throw crash route registered in 02-02 (path `__crash-test` in RootRouter.tsx/RootRoutes.ts) and label it explicitly as test-only / not user-reachable (unlinked from any nav, exists only for the FND-01 e2e proof — threat T-02-CT).
  </action>
  <verify>
    <automated>test -f .planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md && grep -q "Entry path" .planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md && grep -c "sidebar-nav-\|bottom-tab-\|programmatic\|redirect" .planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md` exists and contains an entry for every route defined in `RootRouter.tsx` (the two lazy guide routes + all routes under the Root() layout route, including the four sub-router groups).
    - Each route row records its path, screen, and at least one entry path (bottom-tab / sidebar-nav / header / programmatic); routes with no direct nav entry are explicitly marked.
    - The doc flags the dead `DataBackup` export (currently-unused + hardcoded URL, D-06) in a notes section.
    - The doc states it is the reachability gate for Phase 4 nav changes (NAV-02/NAV-03).
    - The doc lists the test-only render-throw crash route (`__crash-test`, registered in 02-02) and labels it explicitly as test-only / not user-reachable (T-02-CT).
  </acceptance_criteria>
  <done>A route reachability inventory enumerates every pre-refactor route and its entry path, flags the dead DataBackup, and is ready to gate Phase 4 nav changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| moved SidebarDrawer → store + external GitHub | SidebarDrawer writes inventory config to the store and talks to GitHub via useSharedPublish/useSharedDataSync; the move must not alter what crosses this boundary (logic unchanged this phase) |
| moved DataBackup → remote fetch + persist:personal | DataBackup fetches a hardcoded GitHub raw URL and overwrites persist:personal; pre-existing, out of scope — moved verbatim and flagged, not changed |
| ROUTE-INVENTORY.md → planning artifact | Static doc derived from source; no runtime tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-MV | Tampering | mechanical move fidelity (test ids, 16 asset re-paths, intact internals) | mitigate | Preserve every data-testid verbatim (D-08), re-path all relative imports (Pitfall 2), and move SidebarDrawer as one block with no internal edits (D-05); enforced by acceptance greps + the re-run full baseline (D-07). |
| T-02-DB | Tampering / Supply chain | moved DataBackup hardcoded GitHub raw URL → setStorageString(persist:personal) | accept | Pre-existing, explicitly out of scope (Deferred Ideas). Move behavior untouched; flag in code comment + ROUTE-INVENTORY notes as a tracked follow-up. Do not change the URL or fetch logic. |
| T-02-AUTH | Information Disclosure | admin PIN + GitHub PAT logic inside SidebarDrawer | accept | useAdminMode/useSharedPublish logic moves intact, not modified this phase (RESEARCH Security Domain: no new attack surface). Pre-existing token/PIN concerns carried forward, flagged, not fixed. |
| T-02-SC | Tampering | npm/yarn installs | accept | Zero new packages (pure code move + a markdown doc). No supply-chain checkpoint required. |
</threat_model>

<verification>
- `yarn build` clean after each move (craco type-check surfaces broken re-paths — Pitfall 2; kill port 3010 / restart dev server before e2e — Pitfall 6).
- `yarn test:e2e tests/e2e/app-shell-navigation.spec.ts tests/e2e/cooking-pill.spec.ts tests/e2e/global-search.spec.ts` green with the specs unchanged from 02-01 (D-07 full identity proof).
- Source assertions: verbatim data-testid preservation (D-08), all 16 asset imports re-pathed, SidebarDrawer moved intact (D-05), DataBackup preserved + flagged not deleted (D-06).
- MasterPage is a thin composition root with an unchanged returned JSX tree (manual diff: definitions removed + Shell imports added only).
- ROUTE-INVENTORY.md exists, covers every route, and flags the dead DataBackup.
</verification>

<success_criteria>
The shell is fully decomposed: SidebarDrawer (intact) and DataBackup (preserved + flagged) live under `src/Routing/Shell/`, MasterPage is a thin composition root importing all five Shell pieces with an identical render tree, the full 02-01 baseline passes unchanged (FND-02 identity proof complete), and ROUTE-INVENTORY.md gates Phase 4 nav changes.
</success_criteria>

<output>
Create `.planning/phases/02-shell-safety-extraction/02-05-SUMMARY.md` when done.
</output>
