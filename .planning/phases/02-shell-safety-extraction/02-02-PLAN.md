---
phase: 02-shell-safety-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Components/ErrorBoundary/ErrorBoundary.tsx
  - src/Components/ErrorBoundary/index.ts
  - src/App.tsx
  - tests/e2e/error-boundary.spec.ts
  - src/Routing/RootRouter.tsx
  - src/Routing/RootRoutes.ts
autonomous: true
requirements: [FND-01]
must_haves:
  truths:
    - "A thrown render error shows a recovery UI (a Reload button with friendly Vietnamese copy) instead of white-screening the whole app"
    - "The fallback renders inside ConfigProvider so it inherits the antd theme (#7436dc) and viVN locale"
    - "The fallback never surfaces error.message or a stack trace to the user"
    - "The recovery affordance calls window.location.reload()"
    - "@components/ErrorBoundary is importable via barrel"
  artifacts:
    - path: "src/Components/ErrorBoundary/ErrorBoundary.tsx"
      provides: "Class error boundary with getDerivedStateFromError + componentDidCatch and a themed antd Result fallback"
      contains: "getDerivedStateFromError"
    - path: "src/Components/ErrorBoundary/index.ts"
      provides: "Barrel re-export for @components/ErrorBoundary"
      contains: "export"
    - path: "tests/e2e/error-boundary.spec.ts"
      provides: "e2e proof that a render-phase throw shows the recovery UI"
      contains: "Tải lại trang"
  key_links:
    - from: "src/App.tsx"
      to: "src/Components/ErrorBoundary/ErrorBoundary.tsx"
      via: "wraps <RootRouter /> inside ConfigProvider"
      pattern: "ErrorBoundary"
    - from: "tests/e2e/error-boundary.spec.ts"
      to: "the test-only render-throw route"
      via: "navigates to a route whose component throws during render"
      pattern: "reload|Tải lại"
---

<objective>
Add a single top-level React error boundary (FND-01) so an uncaught render error in shell chrome (header, nav, pill, drawer) shows a friendly Vietnamese recovery UI with a Reload button instead of white-screening the whole app. Mount it around `<RootRouter />` inside `ConfigProvider` (D-01) so the fallback keeps the antd theme and `viVN` locale. Prove it with an e2e spec that triggers a render-phase throw via a test-only route (RESEARCH Pitfall 5: error boundaries catch render/lifecycle errors only, not event-handler/async errors).

Purpose: Crash containment is the safety net that lets later journey/nav/mobile work proceed without one bad render taking down the whole app.
Output: `@components/ErrorBoundary` (class component + barrel), boundary mounted in `App.tsx`, a test-only render-throw trigger, and `tests/e2e/error-boundary.spec.ts`.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-shell-safety-extraction/02-CONTEXT.md
@.planning/phases/02-shell-safety-extraction/02-RESEARCH.md
@.planning/phases/02-shell-safety-extraction/02-PATTERNS.md
@.planning/phases/02-shell-safety-extraction/02-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@src/App.tsx
@src/Routing/RootRouter.tsx
@src/Routing/RootRoutes.ts
@src/Components/Result/index.ts
@tests/e2e/fixtures/appTest.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the ErrorBoundary class component + barrel</name>
  <files>src/Components/ErrorBoundary/ErrorBoundary.tsx, src/Components/ErrorBoundary/index.ts</files>
  <read_first>
    - src/Components/Result/index.ts and src/Components/Result/Result.tsx (the themed antd Result wrapper used for the fallback)
    - src/Components/Button (the in-repo Button barrel used for the Reload CTA)
    - .planning/phases/02-shell-safety-extraction/02-UI-SPEC.md (Copywriting Contract + Net-New Surface Specs §1 — exact copy + layout)
    - .planning/phases/02-shell-safety-extraction/02-PATTERNS.md (ErrorBoundary pattern block)
    - .planning/codebase/CONVENTIONS.md (single named export per file, strict:false, antd v5)
  </read_first>
  <behavior>
    - When children render without error: ErrorBoundary renders `this.props.children` unchanged.
    - When a descendant throws during render: `getDerivedStateFromError` flips `hasError` to true and the fallback renders.
    - Fallback contains the heading "Ứng dụng gặp chút trục trặc rồi", body "Nhà mình thử tải lại trang nhé, dữ liệu vẫn được giữ an toàn." and a primary Reload button labelled "Tải lại trang".
    - Reload button onClick calls `window.location.reload()`.
    - Fallback never renders `error.message` or stack text.
  </behavior>
  <action>
    Create a class component `ErrorBoundary` extending `React.Component<React.PropsWithChildren<{ fallback?: React.ReactNode }>, { hasError: boolean }>` (D-03 — class is the only mechanism for `getDerivedStateFromError`/`componentDidCatch`; this is the documented one-class exception per CONVENTIONS). Implement `static getDerivedStateFromError(): { hasError: true }` to drive the fallback render, and `componentDidCatch(error, info)` for side-effects only — a guarded `console.error` is acceptable; do NOT log tokens/PII and do NOT surface `error.message`/stack to the UI (V7 security control, T-02-IF below).
    `render()` returns `this.props.fallback ?? <DefaultFallback/>` when `hasError`, else `this.props.children`. The default fallback uses the in-repo `Result` (`@components/Result`, status="warning") + a primary `Button` (`@components/Button`) with `ReloadOutlined` and label `Tải lại trang`, `onClick={() => window.location.reload()}` (D-02). Copy verbatim from the UI spec Copywriting Contract: title `Ứng dụng gặp chút trục trặc rồi`, body `Nhà mình thử tải lại trang nhé, dữ liệu vẫn được giữ an toàn.`. Copy is inline this phase; AppCopy migration is Phase 5 (do NOT migrate now). Export as `export class ErrorBoundary`. Create `index.ts` barrel: `export * from './ErrorBoundary'` (matches `Result/index.ts`).
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `src/Components/ErrorBoundary/ErrorBoundary.tsx` defines `export class ErrorBoundary` with a `static getDerivedStateFromError` and a `componentDidCatch` (grep: both identifiers present).
    - The fallback string `Tải lại trang` and heading `Ứng dụng gặp chút trục trặc rồi` are present (grep returns ≥ 1 each).
    - No occurrence of `error.message` or `.stack` is rendered into JSX (the fallback does not display caught error detail).
    - `src/Components/ErrorBoundary/index.ts` re-exports `./ErrorBoundary`.
    - `yarn build` completes with no type errors (craco type-check passes).
  </acceptance_criteria>
  <done>A themed, importable `@components/ErrorBoundary` class component exists with a Vietnamese Reload fallback that leaks no error detail and compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the boundary in App.tsx and prove it with an e2e render-throw spec</name>
  <files>src/App.tsx, src/Routing/RootRouter.tsx, src/Routing/RootRoutes.ts, tests/e2e/error-boundary.spec.ts</files>
  <read_first>
    - src/App.tsx (provider stack: ConfigProvider > MessageProvider > ModalProvider > Provider > PersistGate > AppInitializer > RootRouter — boundary goes around RootRouter, inside ConfigProvider)
    - src/Routing/RootRouter.tsx (the <Routes> tree; where to register a test-only throw route)
    - src/Routing/RootRoutes.ts (route table; add the test-only route path here following existing route-definition style)
    - src/Components/ErrorBoundary/index.ts (the barrel to import from)
    - tests/e2e/fixtures/appTest.ts (seeded e2e fixture idiom)
    - .planning/phases/02-shell-safety-extraction/02-RESEARCH.md (Pitfall 4: stay inside ConfigProvider; Pitfall 5: trigger a render-phase throw, not a click handler)
  </read_first>
  <action>
    In `src/App.tsx`, add `import { ErrorBoundary } from "@components/ErrorBoundary";` and wrap `<RootRouter />` with `<ErrorBoundary>...</ErrorBoundary>` — innermost, around RootRouter, INSIDE `ConfigProvider` (D-01 / Pitfall 4: a fallback above ConfigProvider loses theme `#7436dc` + `viVN` locale). Do NOT reorder the existing provider nesting (`MessageProvider`/`ModalProvider`/`Provider`/`PersistGate`/`AppInitializer`).
    Add a test-only render-throw trigger so the e2e spec can force a render-phase crash: register a route (e.g. path `__crash-test`) in `RootRoutes.ts` + `RootRouter.tsx` whose component throws during render (`throw new Error('crash-test')` in the component body, not in a click handler — Pitfall 5). Keep it inside the MasterPage layout tree so it proves shell containment, and make it clearly test-only (name/comment it as such). It does not need to be hidden behind a build flag for this phase, but must not be linked from any user-facing nav.
    Create `tests/e2e/error-boundary.spec.ts` using the `./fixtures/appTest` idiom: navigate to the crash-test route and assert the recovery UI is shown — `Tải lại trang` button visible and the heading `Ứng dụng gặp chút trục trặc rồi` visible (proves no white screen). Optionally assert the page is not blank by checking the button is clickable.
  </action>
  <verify>
    <automated>yarn test:e2e tests/e2e/error-boundary.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - In `src/App.tsx`, `<ErrorBoundary>` wraps `<RootRouter />` and appears AFTER the `<ConfigProvider` opening tag and BEFORE its close (grep order check: `ErrorBoundary` line number is greater than the `ConfigProvider` open line).
    - The existing provider order in `App.tsx` is unchanged (MessageProvider → ModalProvider → Provider → PersistGate → AppInitializer still nest in that sequence).
    - A test-only route component that throws during render is registered in `RootRouter.tsx`/`RootRoutes.ts`.
    - `yarn test:e2e tests/e2e/error-boundary.spec.ts` exits 0: navigating to the crash route shows the `Tải lại trang` recovery button and the heading (not a blank page).
    - `yarn build` passes.
  </acceptance_criteria>
  <done>A render crash anywhere under RootRouter shows the themed Vietnamese recovery UI instead of a white screen, verified by an e2e spec that triggers a real render-phase throw.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caught render error → user-facing fallback | Error objects (may contain internal detail) cross into rendered UI; must be sanitized |
| test-only crash route → app render tree | A deliberately-throwing component registered for e2e; must not be reachable from user nav |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-IF | Information Disclosure | ErrorBoundary fallback (V7) | mitigate | Render only generic Vietnamese copy + Reload button; never display `error.message`/stack. `componentDidCatch` may `console.error` but must not log tokens/PII. |
| T-02-CT | Tampering / surface | test-only crash route | mitigate | Route is unlinked from any user-facing nav and named as test-only; it only throws, performs no writes. |
| T-02-SC | Tampering | npm/yarn installs | accept | Zero new packages this plan (class boundary is hand-rolled per D-03; antd Result/Button already present). No supply-chain checkpoint required. |
</threat_model>

<verification>
- `yarn build` clean (type-check via craco).
- `yarn test:e2e tests/e2e/error-boundary.spec.ts` green.
- Boundary is inside `ConfigProvider` (manual diff check of `src/App.tsx`); provider order unchanged.
- Fallback renders no `error.message`/stack (source assertion).
</verification>

<success_criteria>
FND-01 satisfied: a thrown render error shows a themed Vietnamese recovery UI with a working Reload button instead of white-screening the app, mounted once around RootRouter inside ConfigProvider, proven by e2e.
</success_criteria>

<output>
Create `.planning/phases/02-shell-safety-extraction/02-02-SUMMARY.md` when done.
</output>
