---
phase: 03-wizard-state-slice
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/Store/Store.ts
  - src/Store/Selectors.ts
autonomous: true
requirements: [FND-03, WIZ-06]
must_haves:
  truths:
    - "The wizard slice is registered under the existing personal persisted root (no new persist config)"
    - "Wizard state is read only through selectWizard* selectors — no raw state.personal.wizard access in consumers"
    - "Selectors return safe defaults when the wizard slice is absent from an older persisted blob (no throw)"
    - "selectIsWizardResumable reports in_progress sessions as resumable"
  artifacts:
    - path: "src/Store/Store.ts"
      provides: "wizard: WizardReducer registered in personalReducer combineReducers"
      contains: "wizard: WizardReducer"
    - path: "src/Store/Selectors.ts"
      provides: "selectWizard* selector family (sole read path, FND-03)"
      contains: "selectWizardStep"
  key_links:
    - from: "src/Store/Store.ts"
      to: "src/Store/Reducers/WizardReducer"
      via: "import WizardReducer + combineReducers entry"
      pattern: "wizard: WizardReducer"
    - from: "src/Store/Selectors.ts"
      to: "state.personal.wizard"
      via: "defensive optional-chained read"
      pattern: "state\\.personal\\.wizard\\?\\."
---

<objective>
Register the `WizardReducer` slice under the existing `personal` persisted root and add the `selectWizard*` selector family as the SOLE read path for wizard state. This closes FND-03 (slice under existing `personal` root, selector-only reads, no new persisted root) and completes the durability surface of WIZ-06 (registration under the already-persisted `personal` root makes every per-step commit survive forced reloads).

Purpose: Phase 4's wizard UI reads wizard state exclusively through these selectors — no component touches `state.personal.wizard.*` directly.

Output: one-line edits to `src/Store/Store.ts`; `selectWizard*` family appended to `src/Store/Selectors.ts`.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wizard-state-slice/03-CONTEXT.md
@.planning/phases/03-wizard-state-slice/03-PATTERNS.md
@.planning/phases/03-wizard-state-slice/03-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan CONSUMES the model + slice symbols created in `03-01` (`WizardState`, `WizardStepKey`, `WizardStatus`, `WIZARD_FIRST_STEP`, `DEFAULT_WIZARD_STATE`, `WizardReducer` default export). It CREATES these new symbols:
**Store wiring:** `wizard` key in `personalReducer` (`src/Store/Store.ts`); `RootState` now includes `personal.wizard`.
**Selectors (`src/Store/Selectors.ts`):** `selectWizard`, `selectWizardStep`, `selectWizardAnswers`, `selectWizardStatus`, `selectIsWizardResumable`.
Names are authoritative — Phase 4 imports exactly these. Defer naming changes to the executor only if a collision exists in `Selectors.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Register WizardReducer under personalReducer (no new persist root)</name>
  <files>src/Store/Store.ts</files>
  <read_first>
    - src/Store/Store.ts (read fully — line 13 is the last reducer import; lines 23-31 are the `personalReducer` combineReducers; lines 38-41 `personalPersistConfig` and lines 43-46 the `persistReducer(personalPersistConfig, personalReducer)` — these MUST stay untouched)
    - src/Store/Reducers/WizardReducer.ts (the default export this imports — created in 03-01)
    - .planning/phases/03-wizard-state-slice/03-PATTERNS.md (§ Store.ts — the single-line registration pattern, D-05)
  </read_first>
  <action>
    Add `import WizardReducer from "./Reducers/WizardReducer";` beside the existing reducer imports (after line 13). Add exactly ONE key — `wizard: WizardReducer,` — to the `personalReducer` `combineReducers` object (lines 23-31), alongside `appContext`, `inventory`, `shoppingList`, `scheduledMeal`, `cookingSession`, `householdHealth`, `smartPlannerTemplate`.

    Do NOT add a new persist config, do NOT touch `personalPersistConfig`, do NOT touch the `persistReducer(personalPersistConfig, personalReducer)` line, do NOT add a `sharedReducer` entry. The whole `personal` root is already persisted to IndexedDB via redux-persist, so registration under `personalReducer` alone makes every wizard commit durable (D-05, WIZ-06). This is the ONLY edit to `Store.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/Store/Store.ts` contains `import WizardReducer from "./Reducers/WizardReducer";`.
    - `personalReducer` `combineReducers` contains `wizard: WizardReducer`.
    - `git diff src/Store/Store.ts` shows NO change to `personalPersistConfig`, the `persistReducer` lines, or `sharedReducer` (only the import + one combineReducers key added).
    - `npx tsc --noEmit` reports no type errors.
  </acceptance_criteria>
  <done>The wizard slice is part of `personalReducer`; `RootState` now includes `personal.wizard`; no new persist root was added.</done>
</task>

<task type="auto">
  <name>Task 2: Add the selectWizard* family with defensive defaults (FND-03 read path)</name>
  <files>src/Store/Selectors.ts</files>
  <read_first>
    - src/Store/Selectors.ts (read fully — lines 41-54 the `// ── Personal ──` section; line 52 `selectAppContext` raw-access style; lines 53-54 `selectSmartPlannerTemplates` defensive `?? []` with its explanatory comment — THE pattern for D-06; lines 10/113-116 `createSelector` usage)
    - src/Store/Models/Wizard.ts (created in 03-01 — import `WizardState`, the step-key union `WizardStepKey`, the status type `WizardStatus`, `WIZARD_FIRST_STEP`, and `DEFAULT_WIZARD_STATE`; use the exact symbol names that file exports)
    - .planning/phases/03-wizard-state-slice/03-01-SUMMARY.md (confirms the exact exported symbol names from the model — read before importing)
  </read_first>
  <action>
    Append the wizard selector family in the `// ── Personal ──` section (after line 54, beside `selectSmartPlannerTemplates`). Import the wizard types/constants from `@store/Models/Wizard` (use the exact names 03-01 exported — verify against `03-01-SUMMARY.md`).

    Every selector MUST optional-chain `state.personal.wizard?.<field>` and default a safe value — NEVER `state.personal.wizard.<field>` (would throw on an older `personal` blob that has no `wizard` key, D-06). Mirror the `selectSmartPlannerTemplates` explanatory comment style.

    Add:
    - `selectWizard = (state: RootState): WizardState => state.personal.wizard ?? DEFAULT_WIZARD_STATE` — base slice read with full default.
    - `selectWizardStep = (state: RootState): WizardStepKey => state.personal.wizard?.currentStep ?? WIZARD_FIRST_STEP`
    - `selectWizardAnswers = (state: RootState) => state.personal.wizard?.answers ?? {}` (default to the empty-answers shape `DEFAULT_WIZARD_STATE.answers` if answers is a structured object rather than a plain map — match the model's actual answer shape from 03-01)
    - `selectWizardStatus = (state: RootState): WizardStatus => state.personal.wizard?.status ?? 'idle'`
    - `selectIsWizardResumable = createSelector([selectWizardStatus], status => status === 'in_progress')` — derived "resume-on-mount" read backing D-04/WIZ-06.

    Add a leading comment on `selectWizard` noting the `?? DEFAULT_WIZARD_STATE` tolerates devices whose persisted `personal` blob predates the wizard slice (D-06). These selectors are the SOLE wizard read path (FND-03) — Phase 4 consumes them and never reads `state.personal.wizard` directly.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/Store/Selectors.ts` exports `selectWizard`, `selectWizardStep`, `selectWizardAnswers`, `selectWizardStatus`, and `selectIsWizardResumable`.
    - Every wizard selector reads via optional-chained `state.personal.wizard?.` (grep: `grep -v '^\s*//' src/Store/Selectors.ts | grep -c 'state.personal.wizard\.' ` returns `0` — no non-optional raw access).
    - `selectIsWizardResumable` is built with `createSelector` over `selectWizardStatus`.
    - `npx tsc --noEmit` reports no type errors.
  </acceptance_criteria>
  <done>The full `selectWizard*` family exists, reads defensively (safe on old blobs), and is the only path to wizard state; types compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted blob → store rehydration | An older `personal` IndexedDB blob (no `wizard` key) rehydrates into the store; selectors must tolerate the absent slice. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of Service | `selectWizard*` on old persisted blob | mitigate | Every selector optional-chains `state.personal.wizard?.` and defaults (D-06) — a missing slice returns safe defaults instead of throwing and white-screening the journey. |
| T-03-05 | Information Disclosure | raw `state.personal.wizard.*` access | mitigate | FND-03 enforces selector-only reads; this plan provides the complete read surface so Phase 4 never needs raw access. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No installs — `@reduxjs/toolkit`, `reselect` already in `package.json`; no Package Legitimacy Gate needed. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` passes (slice registered, `RootState.personal.wizard` typed, selectors compile).
- `grep -c 'wizard: WizardReducer' src/Store/Store.ts` returns `1`.
- `git diff src/Store/Store.ts` confirms `personalPersistConfig` and `persistReducer` lines are unchanged.
- No raw (non-optional-chained) `state.personal.wizard.` access exists in `Selectors.ts`.
</verification>

<success_criteria>
- Wizard slice registered under `personalReducer` with no new persist root (D-05, FND-03).
- `selectWizardStep`, `selectWizardAnswers`, `selectWizardStatus`, `selectIsWizardResumable`, `selectWizard` exist and default safely on missing slices (D-06).
- Reads go only through `Selectors.ts` (FND-03); types compile end to end.
</success_criteria>

<output>
Create `.planning/phases/03-wizard-state-slice/03-03-SUMMARY.md` when done.
</output>
