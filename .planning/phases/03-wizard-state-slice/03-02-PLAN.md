---
phase: 03-wizard-state-slice
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Modules/DishSuggester/Helpers/DishScorer.test.ts
autonomous: true
requirements: [WIZ-06]
must_haves:
  truths:
    - "DishScorer.score output is pinned for deterministic fixtures"
    - "DishScorer.scoreWithInventory output is pinned for a non-empty inventory fixture"
    - "DishScorer.scoreCookNow output is pinned for a fixture with a HouseholdPreferenceProfile"
    - "DishScorer.group bucketing is pinned for representative scores"
    - "DishScorer.groupCookNow bucketing is pinned for representative scores"
    - "Running the characterization suite passes against current DishScorer output"
  artifacts:
    - path: "src/Modules/DishSuggester/Helpers/DishScorer.test.ts"
      provides: "Characterization tests pinning current output of all 5 DishScorer methods"
  key_links:
    - from: "src/Modules/DishSuggester/Helpers/DishScorer.test.ts"
      to: "src/Modules/DishSuggester/Helpers/DishScorer.ts"
      via: "import { DishScorer }"
      pattern: "import \\{ DishScorer \\}"
---

<objective>
Pin the CURRENT output of all five `DishScorer` methods (`score`, `scoreWithInventory`, `scoreCookNow`, `group`, `groupCookNow`) with deterministic golden fixtures. These characterization tests capture today's behavior as the baseline so Phase 4's wiring of the wizard result into the suggester cannot silently change scoring.

Purpose: ROADMAP Phase 3 success criterion 4 + WIZ-06 protection — later changes can't regress suggestions unnoticed. This phase only PINS behavior; it does NOT modify `DishScorer.ts` (D-07).

Output: `src/Modules/DishSuggester/Helpers/DishScorer.test.ts`.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wizard-state-slice/03-CONTEXT.md
@.planning/phases/03-wizard-state-slice/03-PATTERNS.md
</context>

<artifacts_this_phase_produces>
See `03-01-PLAN.md` for the full Phase 3 symbol list. This plan adds only:
**Test file:** `src/Modules/DishSuggester/Helpers/DishScorer.test.ts` (no production symbols — `DishScorer.ts` is read-only this phase per D-07).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build deterministic DishScorer fixtures + pin score/group/groupCookNow</name>
  <files>src/Modules/DishSuggester/Helpers/DishScorer.test.ts</files>
  <read_first>
    - src/Modules/DishSuggester/Helpers/DishScorer.ts (read fully — the subject; note line 275 `score`, lines 282-303 filter `isAccompaniment !== true` + sort by score desc then missing-count asc, lines 375-393 `group` thresholds `>=1 / >=0.5 / else`, lines 395-414 `groupCookNow` bucketing by missing-count/score then sort by `cookNowScore ?? score`)
    - src/Store/Models/Dishes.ts (the `Dishes` type, lines 52-65, and `DishesIngredientAmount`, lines 23-32 — fields the fixtures must populate: `id`, `name`, `ingredients`, `note`, `includeDishes`, `steps`, `isCompleted`, `duration`, optional `tags`, `isAccompaniment`, `baseServings`)
    - src/Store/Reducers/CookingSessionReducer.test.ts (the Jest describe/it/expect harness shape to mirror, D-08)
  </read_first>
  <action>
    Create the co-located characterization test (D-08) with `import { DishScorer } from './DishScorer'` and `import type { Dishes, DishesIngredientAmount } from '@store/Models/Dishes'`. No new harness — the CRA/Jest `react-scripts test` runner is already configured.

    Build small deterministic fixtures as typed literals (Claude's discretion on inline-literal vs. fixture-object, per D-07 — prefer inline `const` builders at the top of the file for readability). Create a `makeDish(partial)` helper that returns a fully-typed `Dishes` with sensible defaults (`note: ''`, `includeDishes: []`, `steps: []`, `isCompleted: true`, `duration: { unfreeze: null, prepare: null, cooking: null, serve: null, cooldown: null }`) so each test only overrides the fields it cares about. Build ~3-4 dishes referencing a handful of ingredient IDs (`'ing-1'`, `'ing-2'`, ...). Keep `isAccompaniment` unset or `false` on dishes that must survive the scorer's `isAccompaniment === true` filter; include one `isAccompaniment: true` dish to assert it is filtered out.

    Pin these methods in this task (the pure, inventory-free paths):
    - `DishScorer.score(dishes, selectedIngredientIds, allDishes)` — pass selected IDs that fully match one dish and partially match another; assert exact result ordering (score desc, then missing-count asc per lines 300-303), the matched/missing ID arrays, and that the `isAccompaniment: true` dish is absent. Also assert `score([], [], allDishes)` and `score(dishes, [], allDishes)` both return `[]` (line 276 guard).
    - `DishScorer.group(scored)` — feed hand-built `ScoredDish`-shaped objects with scores `1`, `0.6`, `0.3` and assert three groups with the expected labels/bucket membership; feed a set with an empty bucket and assert empty groups are dropped (line 392).
    - `DishScorer.groupCookNow(scored)` — feed items with `missingIngredientIds: []` (→ "Nấu ngay"), a near-match (`score >= 0.5`, `<= 3` missing → "Mua thêm ít"), and a fallback; assert bucket membership, that each group is sorted by `cookNowScore ?? score` desc (line 412), and empty groups dropped.

    Use explicit `toEqual` against committed expected values for the grouped/score outputs (more readable than snapshots given the deterministic sorts). Capture CURRENT output as the baseline — if any output looks surprising, PIN IT AS-IS and leave a `// characterization: current behavior, not corrected this phase` comment (D-07). Do NOT modify `DishScorer.ts`.
  </action>
  <verify>
    <automated>CI=true npm test -- DishScorer.test</automated>
  </verify>
  <acceptance_criteria>
    - `src/Modules/DishSuggester/Helpers/DishScorer.test.ts` imports `{ DishScorer }` from `./DishScorer`.
    - Test file contains `describe`/`it` blocks for `score`, `group`, and `groupCookNow`.
    - A test asserts `score(dishes, [], allDishes)` returns `[]` and that an `isAccompaniment: true` dish is excluded from `score` results.
    - `DishScorer.ts` is unmodified (git diff shows no change to the production file).
    - `CI=true npm test -- DishScorer.test` passes.
  </acceptance_criteria>
  <done>The fixture builder + `score`/`group`/`groupCookNow` characterization tests pass against current output.</done>
</task>

<task type="auto">
  <name>Task 2: Pin scoreWithInventory + scoreCookNow against current output</name>
  <files>src/Modules/DishSuggester/Helpers/DishScorer.test.ts</files>
  <read_first>
    - src/Modules/DishSuggester/Helpers/DishScorer.ts (lines 126-212 `scoreInventoryDishes` — note line 134 returns `[]` for empty inventory, the `InventoryHelper.availableAmount` / `isAlwaysAvailable` dependencies; lines 306-309 `scoreWithInventory` sort by `compareInventoryPriority`; lines 311-373 `scoreCookNow` takes a `HouseholdPreferenceProfile`, sorts by `cookNowScore` desc then `compareInventoryPriority`, caps `.slice(0, 60)`)
    - src/Store/Models/Ingredient.ts (lines 62-97 — `IngredientInventory` with `batches`/`lastUpdated`, `InventoryBatch`, `Ingredient` with `alwaysAvailable`/`baseUnit`/`shelfLife`; build a non-empty `Record<string, IngredientInventory>` inventory fixture)
    - src/Store/Reducers/AppContextReducer.ts (line 118 `DEFAULT_HOUSEHOLD_PREFERENCE_PROFILE` — fixture base for the `scoreCookNow` profile arg)
  </read_first>
  <action>
    Extend the same test file with the inventory-dependent paths. Reuse the `makeDish` helper from Task 1. Import `Ingredient`, `IngredientInventory` from `@store/Models/Ingredient` and `DEFAULT_HOUSEHOLD_PREFERENCE_PROFILE` from `@store/Reducers/AppContextReducer`.

    Build a NON-EMPTY inventory fixture (`scoreInventoryDishes` returns `[]` for empty inventory — line 134, so an empty map would not exercise the real path). Construct `Record<string, IngredientInventory>` with at least one ingredient stocked via a `batches` entry (`{ id, amount, ... }`, `lastUpdated: new Date('2026-06-15T00:00:00.000Z')`). Build matching `Ingredient[]` with stable `baseUnit`/units so `IngredientUnitHelper`/`InventoryHelper` resolve deterministically. Keep dishes' required ingredients pointing at the stocked + some unstocked IDs so scores land in a meaningful range.

    Pin:
    - `DishScorer.scoreWithInventory(dishes, inventory, allDishes, allIngredients)` — assert the result ordering follows `compareInventoryPriority` (urgent first, then days-left, then score desc, then missing-count asc — lines 103-117) and the matched/missing/stocked ID arrays for the deterministic fixture. Assert `scoreWithInventory(dishes, {}, allDishes, allIngredients)` returns `[]` (empty-inventory guard, line 134).
    - `DishScorer.scoreCookNow(dishes, inventory, allDishes, allIngredients, profile)` with `profile = { ...DEFAULT_HOUSEHOLD_PREFERENCE_PROFILE }` — assert each returned dish carries a `cookNowScore` (number in 0..1), that ordering is `cookNowScore` desc, and pin the resulting dish-id order + `cookNowScore` values as committed expected baselines. Because `cookNowScore` is a weighted float (lines 339-346), assert the dish-id ORDER exactly and assert each `cookNowScore` with `toBeCloseTo(expected, 5)` to avoid float-formatting fragility while still pinning the value.

    These are characterization tests (D-07): capture CURRENT output, pin as-is, comment anything surprising. Do NOT modify `DishScorer.ts`. If a helper dependency (e.g. `InventoryHelper`, `CostEstimateHelper`) makes a value hard to predict by hand, run the suite once to read the actual current output, then commit that value as the expected baseline (this is the legitimate characterization workflow — the baseline IS whatever the code currently produces).
  </action>
  <verify>
    <automated>CI=true npm test -- DishScorer.test</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains `describe`/`it` blocks for `scoreWithInventory` and `scoreCookNow`.
    - A test asserts `scoreWithInventory(dishes, {}, ...)` returns `[]` (empty-inventory guard).
    - `scoreCookNow` test passes a `HouseholdPreferenceProfile` and asserts both dish-id ordering and `cookNowScore` values (via `toBeCloseTo`).
    - `DishScorer.ts` is unmodified (git diff shows no change to the production file).
    - `CI=true npm test -- DishScorer.test` passes with all five methods covered.
  </acceptance_criteria>
  <done>All five `DishScorer` methods are pinned by passing characterization tests against current output; the production file is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan. No new trust boundary; no runtime code added or changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | `DishScorer.test.ts` baseline drift | mitigate | Characterization tests fail loudly if `DishScorer` output changes — that is the intended guard for Phase 4. No production code added; no install steps (`@reduxjs/toolkit`, jest already present). |
</threat_model>

<verification>
- `CI=true npm test -- DishScorer.test` passes, covering all five methods.
- `git diff src/Modules/DishSuggester/Helpers/DishScorer.ts` shows NO changes (D-07 — pin only, do not modify).
</verification>

<success_criteria>
- All five `DishScorer` methods (`score`, `scoreWithInventory`, `scoreCookNow`, `group`, `groupCookNow`) have passing characterization tests pinning current output.
- Fixtures are deterministic (stable ordering exercised); empty-input guards (`score [] `, empty inventory) are asserted.
- `DishScorer.ts` production logic is unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-wizard-state-slice/03-02-SUMMARY.md` when done.
</output>
