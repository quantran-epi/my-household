---
phase: 04-wizard-ui-hero-entry
plan: 04
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - src/Routing/Shell/BottomTabNavigator.tsx
  - src/Modules/Home/Screens/Dashboard.screen.tsx
autonomous: true
requirements: [WIZ-01, NAV-01, NAV-02, NAV-04]
must_haves:
  truths:
    - "The Home hero shows an obvious 'Hôm nay ăn gì?' top CTA that opens the wizard"
    - "The bottom-nav center action opens the wizard route instead of toggling the in-place suggester"
    - "The in-place suggester remains reachable via its /dish-suggester sidebar entry (not orphaned)"
    - "Other dashboard sections and metrics remain intact (entry-points-only reframe)"
  artifacts:
    - path: "src/Routing/Shell/BottomTabNavigator.tsx"
      provides: "Center button routes to MealPlanningRoutes.Wizard()"
      contains: "MealPlanningRoutes"
    - path: "src/Modules/Home/Screens/Dashboard.screen.tsx"
      provides: "DashboardHero 'Hôm nay ăn gì?' CTA into the wizard"
      contains: "onStartJourney"
  key_links:
    - from: "src/Routing/Shell/BottomTabNavigator.tsx"
      to: "MealPlanningRoutes.Wizard()"
      via: "onNavigate on center button"
      pattern: "MealPlanningRoutes"
    - from: "src/Modules/Home/Screens/Dashboard.screen.tsx"
      to: "MealPlanningRoutes.Wizard()"
      via: "openRoute from DashboardHero onStartJourney"
      pattern: "MealPlanningRoutes"
---

<objective>
Reframe the two primary entry points toward the guided journey: the Home hero gets an obvious "Hôm nay ăn gì?" top CTA, and the bottom-nav center action routes into the wizard — while keeping the existing in-place suggester reachable via its sidebar route.

Purpose: Satisfy WIZ-01 (Home hero entry), NAV-01 (reframe entry points from admin-style to guided journey), NAV-04 (center action opens wizard, suggester stays reachable), and NAV-02 (no route loses its only entry path). This is an entry-points-only reframe (D-10) — no sidebar IA teardown, no route renames, other dashboard sections stay intact.

Output: modified `BottomTabNavigator.tsx`, modified `Dashboard.screen.tsx`.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wizard-ui-hero-entry/04-RESEARCH.md
@.planning/phases/04-wizard-ui-hero-entry/04-PATTERNS.md
@.planning/phases/04-wizard-ui-hero-entry/04-UI-SPEC.md
@.planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md
@.planning/phases/04-wizard-ui-hero-entry/04-03-SUMMARY.md
</context>

<interface_context>
Consumes the live wizard route from 04-03: `RootRoutes.AuthorizedRoutes.MealPlanningRoutes.Wizard()` → `/meal-planning/wizard`.

BottomTabNavigator already imports `RootRoutes` and exposes `onNavigate` (wrapping `navigateWithFeedback` from `useAppShellNavigation()`) used by every other tab. The center button currently uses `useToggle()` + an inline `<DishSuggesterScreen>` render block — both are removed by this plan.

Dashboard's `DashboardHero` is a self-contained component rendered once by `DashboardScreen`; `DashboardScreen` has an `openRoute` helper. The hero already renders a white-pill `<Button>`; the new CTA reuses that style.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the bottom-nav center button to the wizard route (keep suggester reachable)</name>
  <files>src/Routing/Shell/BottomTabNavigator.tsx</files>
  <read_first>
    - src/Routing/Shell/BottomTabNavigator.tsx (line ~17 useToggle; ~19-22 route computations + isRouteActive; ~193-196 onNavigate/navigateWithFeedback; ~202-227 other tab buttons; ~228-240 the center suggester button; ~269 the inline DishSuggesterScreen render block)
    - src/Routing/RootRoutes.ts (MealPlanningRoutes.Wizard() registered in 04-01)
    - .planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md (confirm /dish-suggester sidebar entry sidebar-nav-dishSuggester is the suggester's surviving entry — NAV-02 gate)
    - .planning/phases/04-wizard-ui-hero-entry/04-UI-SPEC.md (NAV-02/04 interaction rule: preserve data-testid="bottom-tab-suggester")
  </read_first>
  <action>
    In `BottomTabNavigator.tsx`, compute `const wizardRoute = RootRoutes.AuthorizedRoutes.MealPlanningRoutes.Wizard()` alongside the other route constants and `const wizardActive = isRouteActive(wizardRoute)`. Change the center "Nấu gì?" button to navigate to the wizard via the existing `onNavigate`: `onClick={() => onNavigate(wizardRoute)}` and `aria-pressed={wizardActive}`. CRITICAL — preserve `data-testid="bottom-tab-suggester"` and the existing `aria-label` on that button (an e2e spec targets that testid; the 04-05 migration depends on it still existing). Remove the `useToggle` import/usage (the `toggleSuggester` const) and remove the trailing `{toggleSuggester.value && <DishSuggesterScreen ... />}` render block plus the now-unused `DishSuggesterScreen` import if it has no other use.
    NAV-02 gate: do NOT touch the `/dish-suggester` sidebar route — the in-place suggester stays reachable there (its surviving entry per ROUTE-INVENTORY.md). This change only moves the center button's destination; it must not orphan the suggester.
    Match the surrounding 4-space indentation and inline-style idiom; keep the button's existing label/icon copy ("Nấu gì?") unless it conflicts with the UI-SPEC.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>Tapping the bottom-nav center button navigates to /meal-planning/wizard; the in-place suggester still opens from the sidebar /dish-suggester entry; no other tab regressed.</human-check>
  </verify>
  <acceptance_criteria>
    - The center button's `onClick` calls `onNavigate(RootRoutes.AuthorizedRoutes.MealPlanningRoutes.Wizard())`.
    - `data-testid="bottom-tab-suggester"` is still present on the center button.
    - `useToggle`/`toggleSuggester` and the inline `<DishSuggesterScreen>` render block are removed; no dangling unused import remains (tsc clean).
    - The `/dish-suggester` sidebar route is untouched (no edits outside BottomTabNavigator.tsx in this task).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>The center nav action opens the wizard route while the suggester remains reachable via its sidebar entry (NAV-04 + NAV-02).</done>
</task>

<task type="auto">
  <name>Task 2: Add the 'Hôm nay ăn gì?' hero CTA to DashboardHero (reframe in place)</name>
  <files>src/Modules/Home/Screens/Dashboard.screen.tsx</files>
  <read_first>
    - src/Modules/Home/Screens/Dashboard.screen.tsx (lines ~255-338 DashboardHero component incl. the existing white-pill Button at ~289; ~533-539 openRoute helper; ~648-710 priorityAction computation — do NOT repurpose it; ~756-762 the DashboardHero usage site)
    - src/Routing/RootRoutes.ts (MealPlanningRoutes.Wizard())
    - .planning/phases/04-wizard-ui-hero-entry/04-UI-SPEC.md (CTA copy "Hôm nay ăn gì?", accent white-on-gradient hero treatment, Display role 28px/600; hero internal padding stays 14px — reframe in place, not a restyle)
  </read_first>
  <action>
    Reframe the hero in place using Option A (planner decision 5, D-09): add an optional prop `onStartJourney?: () => void` to `DashboardHero` and render a dominant CTA button labelled "Hôm nay ăn gì?" (`data-testid="hero-start-journey"`) at the top of the hero, reusing the hero's existing white-pill button style (`borderRadius: 999`, white background, primary-active `#5e2bbf` text, the existing boxShadow). Keep the existing `priorityAction` content and metrics rendered below it (NAV-01 is entry-points-only, D-10 — do NOT repurpose or remove `priorityAction`, which is the urgency surface). At the `DashboardHero` usage site, pass `onStartJourney={() => openRoute(RootRoutes.AuthorizedRoutes.MealPlanningRoutes.Wizard())}` (use the existing `openRoute` helper). Keep `dateLabel`, `mainValue`, `mainLabel`, `metrics` unchanged. Copy "Hôm nay ăn gì?" written in Vietnamese inline (no AppCopy — Phase 5). Match the surrounding inline-style idiom and 4-space indentation; hero internal padding stays at the existing 14px.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>The Dashboard hero shows a prominent "Hôm nay ăn gì?" button that opens /meal-planning/wizard; the existing priority action and metrics still render below it; desktop layout unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    - `DashboardHero` accepts an `onStartJourney?: () => void` prop and renders a CTA button with text exactly `Hôm nay ăn gì?` carrying `data-testid="hero-start-journey"`.
    - The usage site passes `onStartJourney={() => openRoute(RootRoutes.AuthorizedRoutes.MealPlanningRoutes.Wizard())}`.
    - The existing `priorityAction` rendering and `metrics` are still present (not removed/repurposed).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>The Home hero leads with the "Hôm nay ăn gì?" journey CTA while keeping the rest of the dashboard intact (WIZ-01, NAV-01).</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04-04)

- Modified files:
  - `src/Routing/Shell/BottomTabNavigator.tsx` — center button repointed to `/meal-planning/wizard`; `useToggle`/inline `DishSuggesterScreen` removed; `data-testid="bottom-tab-suggester"` preserved
  - `src/Modules/Home/Screens/Dashboard.screen.tsx` — `DashboardHero` gains `onStartJourney` prop + "Hôm nay ăn gì?" CTA
- New data-testids: `hero-start-journey`
- Reachability: `/dish-suggester` sidebar entry untouched (NAV-02); wizard reachable via hero + center nav (NAV-04)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User nav action → client route change | Center button + hero CTA trigger client-side navigation to `/meal-planning/wizard` (no server, no auth — local-first app) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Denial of Service | Removing the suggester's center-button entry | mitigate | NAV-02 gate enforced: the `/dish-suggester` sidebar route remains the suggester's reachable entry; this plan does not touch it, so no route is orphaned |
| T-04-09 | Spoofing | Navigation target string | accept | Route comes from the typed `RootRoutes.AuthorizedRoutes.MealPlanningRoutes.Wizard()` builder, not user input — no open-redirect surface in a client-only router |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — all symbols (RootRoutes, antd Button, existing shell hooks) already present |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Center button navigates to `/meal-planning/wizard`; `bottom-tab-suggester` testid preserved.
- `/dish-suggester` sidebar entry untouched — suggester not orphaned (NAV-02, confirmed against ROUTE-INVENTORY.md).
- Hero renders the "Hôm nay ăn gì?" CTA; priorityAction + metrics intact (D-10).
</verification>

<success_criteria>
- Home shows one obvious "Hôm nay ăn gì?" hero entry into the guided journey (WIZ-01, NAV-01).
- The bottom-nav center action opens the wizard while the existing suggester stays reachable (NAV-04, NAV-02).
</success_criteria>

<output>
Create `.planning/phases/04-wizard-ui-hero-entry/04-04-SUMMARY.md` when done.
</output>
