---
phase: 04-wizard-ui-hero-entry
plan: 05
type: execute
wave: 4
depends_on: [04-04]
files_modified:
  - tests/e2e/fixtures/seedApp.ts
  - tests/e2e/dish-suggester.spec.ts
  - tests/e2e/wizard-cold-start.spec.ts
autonomous: true
requirements: [WIZ-07, NAV-02, NAV-03, NAV-04]
must_haves:
  truths:
    - "A first-timer with a populated catalog reaches a scheduled meal via the wizard end-to-end (Home/center → wizard → result → add → scheduledMeal exists)"
    - "A first-timer with an empty catalog is routed to add their first dish instead of a dead-end"
    - "The in-place dish suggester stays reachable via its /dish-suggester sidebar route after the center button repoints to the wizard"
    - "Global search still reaches existing features after the nav reframe"
  artifacts:
    - path: "tests/e2e/wizard-cold-start.spec.ts"
      provides: "WIZ-07 cold-start E2E (populated + empty catalog scenarios)"
      min_lines: 40
    - path: "tests/e2e/dish-suggester.spec.ts"
      provides: "Migrated suggester spec reaching the suggester via /dish-suggester (not the repointed center button)"
      contains: "dish-suggester"
    - path: "tests/e2e/fixtures/seedApp.ts"
      provides: "Empty-catalog seed option for cold-start"
      contains: "welcome-complete"
  key_links:
    - from: "tests/e2e/wizard-cold-start.spec.ts"
      to: "/meal-planning/wizard"
      via: "navigate via bottom-tab-suggester / Home hero then complete the flow"
      pattern: "meal-planning|bottom-tab-suggester"
    - from: "tests/e2e/dish-suggester.spec.ts"
      to: "/dish-suggester"
      via: "sidebar route navigation (suggester's surviving entry)"
      pattern: "dish-suggester"
---

<objective>
Prove the milestone's named success metric end-to-end and lock the reachability gate: a first-timer reaches a scheduled meal through the wizard (WIZ-07), the suggester stays reachable after the center-button repoint (NAV-02/NAV-04), and global search is unaffected (NAV-03).

Purpose: WIZ-07 is the phase's headline acceptance criterion (cold-start empty IndexedDB → wizard → scheduled meal). This plan also migrates the existing `dish-suggester.spec.ts` (which breaks the moment 04-04 repoints `bottom-tab-suggester` to the wizard route) and adds the empty-catalog seed the cold-start scenario needs.

Output: empty-catalog seed option, migrated `dish-suggester.spec.ts`, new `wizard-cold-start.spec.ts`.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wizard-ui-hero-entry/04-RESEARCH.md
@.planning/phases/04-wizard-ui-hero-entry/04-PATTERNS.md
@.planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md
@.planning/phases/04-wizard-ui-hero-entry/04-04-SUMMARY.md
</context>

<interface_context>
This plan exercises the full wizard wired by 04-01..04-04. Relevant testids established upstream:
- Entry: `bottom-tab-suggester` (center button, now routes to `/meal-planning/wizard` after 04-04) and the Home hero "Hôm nay ăn gì?" CTA (`hero-start-journey` from 04-04).
- Steps: `wizard-screen`, `wizard-step-ingredients`, `wizard-ingredient-advance`, `wizard-skip-ingredients`, `wizard-step-preferences`, `wizard-skip-preferences`, `wizard-step-result`.
- Result: `wizard-result-item-${dishId}`, `wizard-add-today-${dishId}`, `wizard-empty-catalog`.

Seed contract (verified `seedApp.ts`): `seedApp(page, options)` writes `persist:shared` (ingredient + dishes) and `persist:personal` to IndexedDB and sets `localStorage['my-recipes-welcome-complete-v1'] = '1'` (line ~69) so the app does NOT redirect to `/guide/welcome`. The default dataset is the regression seed (populated). There is currently NO empty-catalog variant.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Add an empty-catalog seed option to seedApp</name>
  <files>tests/e2e/fixtures/seedApp.ts</files>
  <read_first>
    - tests/e2e/fixtures/seedApp.ts (whole file — the SeedAppOptions type, the persist:shared write with shared.dishes/shared.ingredient, and the welcome-complete flag at line ~69)
    - tests/e2e/fixtures/testData.ts (createRegressionSeed shape — what shared.dishes/shared.ingredient look like so the empty variant matches the structure)
    - tests/e2e/fixtures/appTest.ts (how the appTest fixture invokes seedApp — confirm the option surface)
  </read_first>
  <action>
    Extend `SeedAppOptions` with an optional flag `emptyCatalog?: boolean`. When `emptyCatalog` is true, write `persist:shared` with empty `dishes: []` and `ingredient: []` (preserve the exact `_persist` wrapper and the `shared_synced_versions` write so the app treats the data as synced, not unfetched), while STILL setting `localStorage['my-recipes-welcome-complete-v1'] = '1'` (CRITICAL — without it a cold-start lands on `/guide/welcome` and never reaches the wizard, per RESEARCH §10 / planner decision 6). Keep `persist:personal` empty/default (no scheduled meals) so the cold-start assertion that a meal appears is meaningful. Do not change the default (populated) behavior when `emptyCatalog` is absent. This is the seed-variant approach (planner decision 6) rather than a raw IndexedDB clear, so the welcome flag is replicated reliably.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `SeedAppOptions` exposes `emptyCatalog?: boolean`.
    - With `emptyCatalog: true`, the written `persist:shared` contains `dishes` and `ingredient` as empty arrays, and `localStorage['my-recipes-welcome-complete-v1']` is still set to `'1'`.
    - Default behavior (no flag) is unchanged — regression seed still populated.
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>seedApp supports an empty-catalog cold-start that does not redirect to onboarding.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate dish-suggester.spec.ts to reach the suggester via its sidebar route (NAV-02)</name>
  <files>tests/e2e/dish-suggester.spec.ts</files>
  <read_first>
    - tests/e2e/dish-suggester.spec.ts (current spec — line ~9 clicks bottom-tab-suggester and line ~10 expects 'Nấu gì hôm nay?'; this breaks once 04-04 repoints that button to the wizard)
    - .planning/phases/02-shell-safety-extraction/ROUTE-INVENTORY.md (confirm /dish-suggester sidebar entry: sidebar-nav-dishSuggester → DishSuggesterPageScreen — the suggester's surviving entry)
    - src/Routing/Shell/SidebarDrawer.tsx (confirm the sidebar nav testid that opens /dish-suggester, e.g. sidebar-nav-dishSuggester, and how the drawer is opened)
    - tests/e2e/app-shell-navigation.spec.ts (pattern for opening the sidebar drawer + clicking a nav entry in e2e)
  </read_first>
  <action>
    Update `dish-suggester.spec.ts` so it no longer relies on the center `bottom-tab-suggester` button to open the in-place suggester (that button now routes to the wizard after 04-04). Instead reach the suggester via its surviving entry — the `/dish-suggester` sidebar route (`sidebar-nav-dishSuggester` → `DishSuggesterPageScreen`, per ROUTE-INVENTORY.md): either navigate directly with `page.goto('dish-suggester')` or open the sidebar drawer and click the `sidebar-nav-dishSuggester` entry (mirror `app-shell-navigation.spec.ts`). Keep the rest of the assertions (Tủ lạnh → suggestion item → expense planner) intact; adjust only the entry navigation and any text/URL assertion that assumed the center-button modal. The goal is to prove the suggester is STILL reachable (NAV-02), not to test the wizard here. Do NOT delete this spec.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/dish-suggester.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - The spec reaches the in-place suggester via `/dish-suggester` (sidebar route or direct goto), NOT via `bottom-tab-suggester`.
    - The expense-planner flow assertions are preserved and the spec passes.
    - `npx playwright test tests/e2e/dish-suggester.spec.ts` exits 0.
  </acceptance_criteria>
  <done>The suggester spec proves the suggester remains reachable after the center-button repoint (NAV-02) and passes.</done>
</task>

<task type="auto">
  <name>Task 3: Add the WIZ-07 cold-start E2E (populated + empty catalog)</name>
  <files>tests/e2e/wizard-cold-start.spec.ts</files>
  <read_first>
    - tests/e2e/fixtures/appTest.ts (the appTest fixture + how seedApp options are passed per-test)
    - tests/e2e/fixtures/seedApp.ts (the emptyCatalog option added in Task 1; the welcome-complete flag)
    - tests/e2e/fixtures/testData.ts (TEST_IDS.dishes.* for asserting a specific dish reaches the result + scheduled meal)
    - tests/e2e/dashboard.spec.ts (pattern for asserting dashboard/scheduledMeal state in e2e)
    - .planning/phases/04-wizard-ui-hero-entry/04-RESEARCH.md (§10 the two WIZ-07 scenarios; §7a entry via bottom-tab-suggester→wizard)
    - src/Modules/ScheduledMeal/Routing/ScheduledMealRouteConfig.ts (the /scheduledMeal/list path to assert the added meal)
  </read_first>
  <action>
    Create `wizard-cold-start.spec.ts` with two tests proving WIZ-07 end-to-end:
    Scenario 1 — POPULATED cold-start (no pre-existing scheduled meals): seed the regression dataset (default), navigate to the wizard via `bottom-tab-suggester` (now routing to `/meal-planning/wizard`) — assert `wizard-screen` visible. On the ingredient step pick at least one ingredient and advance via `wizard-ingredient-advance` (or skip via `wizard-skip-ingredients` to exercise the full-catalog fallback — pick the path that yields a result). Advance through the preference step (skip via `wizard-skip-preferences` is acceptable). On `wizard-step-result` assert at least one `wizard-result-item-*` row, click its `wizard-add-today-${dishId}`, assert the "Đã thêm vào thực đơn" toast, then navigate to `/scheduledMeal/list` and assert the chosen dish is present for today. This proves Home/center → wizard → result → add → scheduled meal exists.
    Scenario 2 — EMPTY catalog: seed with `{ emptyCatalog: true }` (Task 1), navigate to the wizard, advance/skip to the result, and assert the `wizard-empty-catalog` empty state appears and its "Thêm món đầu tiên" button navigates to the dishes list route (`/dishes/list`). This proves a first-timer with empty data is NOT dead-ended (WIZ-04 branch c / WIZ-07).
    Also assert NAV-03 is intact: either run within this spec or rely on the existing `global-search.spec.ts` (note in the spec comment that global search reachability is covered by `global-search.spec.ts`, unchanged this phase). Use `data-testid` selectors throughout; do NOT add assertions that re-check `DishScorer` ranking (pinned).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/wizard-cold-start.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Scenario 1 navigates from `bottom-tab-suggester` to `wizard-screen`, completes the flow, and asserts the added dish appears in `/scheduledMeal/list` for today.
    - Scenario 2 uses `emptyCatalog: true`, reaches `wizard-empty-catalog`, and verifies the "Thêm món đầu tiên" button targets `/dishes/list`.
    - No test re-asserts `DishScorer` scoring output.
    - `npx playwright test tests/e2e/wizard-cold-start.spec.ts` exits 0 (both scenarios pass).
  </acceptance_criteria>
  <done>WIZ-07 is proven by a passing cold-start E2E for both populated and empty catalogs.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04-05)

- New file: `tests/e2e/wizard-cold-start.spec.ts` — WIZ-07 cold-start E2E (populated + empty scenarios)
- Modified: `tests/e2e/fixtures/seedApp.ts` — `emptyCatalog?: boolean` option on `SeedAppOptions` (preserves the `my-recipes-welcome-complete-v1` flag)
- Modified: `tests/e2e/dish-suggester.spec.ts` — reaches the suggester via `/dish-suggester` sidebar route instead of the repointed center button (NAV-02 proof)
- No new data-testids (consumes those from 04-01..04-04)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test seed → IndexedDB | The empty-catalog seed writes persisted state directly; must replicate the welcome-complete flag to avoid an onboarding redirect (a test-fidelity boundary, not a production one) |
| Repointed nav entry → route | The center button now routes to `/meal-planning/wizard`; the suggester must retain a separate reachable entry (NAV-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Spoofing | Reachability regression (orphaned suggester route) | mitigate | Migrated `dish-suggester.spec.ts` asserts the suggester is reachable via `/dish-suggester`; if that entry is gone the spec fails — gates NAV-02 |
| T-04-11 | Repudiation | Cold-start success not actually proven | mitigate | WIZ-07 spec asserts a concrete `ScheduledMeal` exists in `/scheduledMeal/list` after the flow, not just UI transitions |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — `@playwright/test` and existing fixtures already present; no legitimacy gate required |
</threat_model>

<verification>
- `npx playwright test tests/e2e/wizard-cold-start.spec.ts` passes (both scenarios).
- `npx playwright test tests/e2e/dish-suggester.spec.ts` passes (suggester reachable via sidebar — NAV-02).
- `npx playwright test tests/e2e/global-search.spec.ts` still passes unchanged (NAV-03 unaffected).
- `npx tsc --noEmit` passes after the seed change.
- Full reachability cross-check against ROUTE-INVENTORY.md: center button → wizard (NAV-04), suggester via sidebar (NAV-02), all bottom-tab routes untouched.
</verification>

<success_criteria>
- A first-timer with empty data reaches a scheduled meal unaided, proven by a passing cold-start E2E (WIZ-07).
- The bottom-nav center opens the wizard (NAV-04) while the suggester stays reachable (NAV-02) and global search is unaffected (NAV-03).
</success_criteria>

<output>
Create `.planning/phases/04-wizard-ui-hero-entry/04-05-SUMMARY.md` when done.
</output>
