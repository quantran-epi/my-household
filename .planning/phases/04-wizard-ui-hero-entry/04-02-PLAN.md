---
phase: 04-wizard-ui-hero-entry
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
autonomous: true
requirements: [WIZ-04, WIZ-05]
must_haves:
  truths:
    - "When the catalog has dishes and ingredients are selected, the result shows the top few scored dishes"
    - "When no dish matches (or ingredients skipped) but the catalog is non-empty, the result shows a full-catalog fallback with a neutral note"
    - "When the catalog is empty, the result routes to add the first dish instead of showing a blank list"
    - "The user can add a chosen dish to today's meals in one tap, defaulting to the dinner slot"
  artifacts:
    - path: "src/Modules/MealPlanning/Screens/WizardResult.widget.tsx"
      provides: "Result step: fallback ladder + add-to-meal via addScheduledMeal"
      min_lines: 70
  key_links:
    - from: "src/Modules/MealPlanning/Screens/WizardResult.widget.tsx"
      to: "DishScorer.score"
      via: "import + call (read-only, no re-sort)"
      pattern: "DishScorer\\.score"
    - from: "src/Modules/MealPlanning/Screens/WizardResult.widget.tsx"
      to: "addScheduledMeal"
      via: "dispatch with constructed ScheduledMeal"
      pattern: "addScheduledMeal"
    - from: "src/Modules/MealPlanning/Screens/WizardResult.widget.tsx"
      to: "DishesRoutes.List()"
      via: "navigate on empty catalog"
      pattern: "DishesRoutes"
---

<objective>
Build the wizard result step: the WIZ-04 fallback ladder (matches → full-catalog fallback → empty-catalog route) and the WIZ-05 add-to-meal single-tap path.

Purpose: Guarantee the result step always yields an actionable outcome for a first-timer (the milestone's named success metric) and lets them add the chosen dish to today's meals. This widget is self-contained — it reads everything via selectors and dispatches existing actions, so it builds in parallel with the input steps (04-01).

Output: `WizardResult.widget.tsx`.
</objective>

<execution_context>
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/tranducquan/Working/Personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wizard-ui-hero-entry/04-RESEARCH.md
@.planning/phases/04-wizard-ui-hero-entry/04-PATTERNS.md
@.planning/phases/04-wizard-ui-hero-entry/04-UI-SPEC.md
</context>

<interface_context>
The container (04-03) renders `<WizardResult />` with no required props (it reads everything via selectors and dispatches actions itself). Optional: accept `onBack?: () => void` if the container wants to pass a back affordance. After a successful add, dispatch `completeWizard()` (import from `@store/Reducers/WizardReducer`).

Key pinned/existing contracts (verified in RESEARCH/PATTERNS):
- `DishScorer.score(dishes, selectedIngredientIds, allDishes): ScoredDish[]` — PINNED/read-only, returns `[]` when ids empty, already sorted (score desc then missing-count asc). NEVER modify, NEVER re-sort, NEVER add tests re-asserting scoring.
- `ScoredDish` = `{ dish, score, matchedIngredientIds, missingIngredientIds, ... }`.
- `addScheduledMeal(payload: ScheduledMeal)` — `ScheduledMeal` model: `{ id, name, plannedDate: Date, meals: Record<'breakfast'|'lunch'|'dinner', string[]>, memberIds?, dishServings?, createdDate: Date }`.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Build the WIZ-04 fallback ladder + scored result list</name>
  <files>src/Modules/MealPlanning/Screens/WizardResult.widget.tsx</files>
  <read_first>
    - src/Modules/DishSuggester/Helpers/DishScorer.ts (lines ~13-31 ScoredDish shape, ~275-304 score() signature + that output is pre-sorted — PINNED, read-only)
    - src/Modules/Home/Screens/Dashboard.screen.tsx (lines ~425-443 SuggestionRow — compact ScoredDish row visual reference; ~628 .slice(0,4) inline-suggestion precedent)
    - src/Store/Selectors.ts (selectDishes, selectWizardAnswers — sole read path)
    - src/Routing/RootRoutes.ts (DishesRoutes.List() target for empty-catalog branch)
    - .planning/phases/04-wizard-ui-hero-entry/04-UI-SPEC.md (copy contract: empty-state + no-match note strings, color rules — fallback is friendly, never red error)
  </read_first>
  <action>
    Create `WizardResult.widget.tsx` exporting `WizardResult`. Read `const dishes = useSelector(selectDishes)` and `const answers = useSelector(selectWizardAnswers)`; `const ids = answers.selectedIngredientIds ?? []`. Implement the D-08 fallback ladder in this exact order:
    (c) EMPTY CATALOG — `dishes.length === 0`: render an empty state with heading "Chưa có món nào" and body "Thêm món đầu tiên để bắt đầu lên thực đơn." plus a primary button "Thêm món đầu tiên" that navigates (via `useNavigate`) to `RootRoutes.AuthorizedRoutes.DishesRoutes.List()` (planner decision 4). NOT an inline composer. `data-testid="wizard-empty-catalog"`.
    (a) MATCHES — otherwise compute `const scored = DishScorer.score(dishes, ids, dishes)`; if `ids.length > 0 && scored.length > 0`, show `scored.slice(0, 5)` (N=5, planner decision 3) as a ranked list. Do NOT re-sort — the scorer output is already sorted.
    (b) FULL-CATALOG FALLBACK — if `ids.length === 0 || scored.length === 0`, show `dishes.slice(0, 5)` as plain dish rows, preceded by the neutral note "Chưa có món khớp — đây là vài gợi ý từ toàn bộ món của bạn". This is a friendly suggestion: neutral tone/color, NEVER styled or worded as a red error (UI-SPEC color rules).
    Build a small purpose-built result row (do NOT reuse the suggester's coupled `DishSuggestionList`); mirror `SuggestionRow` visuals: dish name (Heading), and for scored rows a match meta line `${matchedIngredientIds.length} đủ · ${missingIngredientIds.length} thiếu` with an accent derived from match percent (>=100 "Đủ đồ" green, >=50 "Gần đủ" amber, else "Cần mua"). Each row carries `data-testid={`wizard-result-item-${dish.id}`}`. Use inline styles, Vietnamese copy inline, selector-only reads. Add `data-testid="wizard-step-result"` on the root. (Add-to-meal wiring is Task 2.)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>With a seeded catalog the result shows up to 5 scored rows; clearing the catalog shows the "Chưa có món nào" empty state with the "Thêm món đầu tiên" button.</human-check>
  </verify>
  <acceptance_criteria>
    - The three branches execute in order c → a → b as described; empty catalog never renders a dish list.
    - `DishScorer.score(dishes, ids, dishes)` is called and its result is sliced to 5 without any re-sort (`.sort(` does not appear applied to the scored array).
    - Empty-catalog branch renders heading exactly `Chưa có món nào`, button exactly `Thêm món đầu tiên`, and navigates to `RootRoutes.AuthorizedRoutes.DishesRoutes.List()`.
    - Full-catalog fallback renders the exact note `Chưa có món khớp — đây là vài gợi ý từ toàn bộ món của bạn` with neutral (non-error) styling.
    - Reads only via `selectDishes`/`selectWizardAnswers`; no direct state access; no modification of `DishScorer`.
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>The result step renders the correct branch for empty / matched / no-match catalogs and shows at most 5 rows.</done>
</task>

<task type="auto">
  <name>Task 2: Wire add-to-meal (today default, dinner slot, optional day picker) + completeWizard</name>
  <files>src/Modules/MealPlanning/Screens/WizardResult.widget.tsx</files>
  <read_first>
    - src/Modules/ScheduledMeal/Screens/ScheduledMealAdd.widget.tsx (lines ~95-103 transformFunc building the ScheduledMeal payload; ~74-81 onSubmit dispatch; ~3,158 DatePicker usage; ~6,77 useMessage)
    - src/Store/Reducers/ScheduledMealReducer.ts (addScheduledMeal action signature)
    - src/Store/Models/ScheduledMeal.ts (lines ~25-37 the ScheduledMeal model)
    - src/Store/Reducers/WizardReducer.ts (completeWizard action creator, lines ~63-70)
    - src/Components/Sheet/index.ts (Sheet props for the "Chọn ngày khác" picker)
    - src/Components/Form/DatePicker (the DatePicker component used by ScheduledMealAdd)
  </read_first>
  <action>
    Add the add-to-meal action to `WizardResult.widget.tsx`. Import `nanoid` from `@reduxjs/toolkit`, `addScheduledMeal` from `@store/Reducers/ScheduledMealReducer`, `ScheduledMeal` from `@store/Models/ScheduledMeal`, `completeWizard` from `@store/Reducers/WizardReducer`, and `useMessage` from `@components/Message`. Implement `addDishToDay(dish, day, slot = 'dinner')` that constructs a `ScheduledMeal` exactly mirroring the widget's transformFunc: `id: nanoid(12)`, `name: dish.name`, `plannedDate: day`, `meals: { breakfast: [], lunch: [], dinner: [], [slot]: [dish.id] }`, `memberIds: []`, `dishServings: {}`, `createdDate: new Date()`. Dispatch `addScheduledMeal(meal)`, then `message.success("Đã thêm vào thực đơn")`, then `dispatch(completeWizard())` (planner decision 1: default slot `'dinner'`, single-tap to today).
    On each result row render a primary add button "Thêm vào hôm nay" (`data-testid={`wizard-add-today-${dish.id}`}`, accent) that calls `addDishToDay(dish, new Date())`, and a neutral secondary "Chọn ngày khác" affordance that opens a `@components/Sheet` (`data-testid="wizard-day-sheet"`) hosting the `DatePicker`; confirming the picked date calls `addDishToDay(dish, pickedDate)`. Do NOT mount the full `ScheduledMealAddWidget` form — construct + dispatch inline (RESEARCH §6 recommended path). Vietnamese copy inline, inline styles.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>Clicking "Thêm vào hôm nay" on a result row shows the "Đã thêm vào thực đơn" toast and a corresponding ScheduledMeal appears in /scheduledMeal/list for today; "Chọn ngày khác" opens a date Sheet and adds to the picked day.</human-check>
  </verify>
  <acceptance_criteria>
    - `addDishToDay` constructs a `ScheduledMeal` with `id` from `nanoid`, `plannedDate` a `Date`, and `meals` containing `[dish.id]` in the chosen slot (default `dinner`).
    - The primary add button text is exactly `Thêm vào hôm nay` with `data-testid="wizard-add-today-${dish.id}"` and defaults the day to `new Date()`.
    - The alternate-day affordance text is exactly `Chọn ngày khác`, opens a `Sheet` with a `DatePicker`, and adds to the picked date.
    - After a successful add, `addScheduledMeal` is dispatched, `message.success("Đã thêm vào thực đơn")` fires, and `completeWizard()` is dispatched.
    - The full `ScheduledMealAddWidget` form is NOT mounted (payload constructed inline).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>Choosing a result dish adds a ScheduledMeal (today by default, dinner slot), confirms via toast, and completes the wizard.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04-02)

- New file: `src/Modules/MealPlanning/Screens/WizardResult.widget.tsx` — `WizardResult`
- New data-testids: `wizard-step-result`, `wizard-empty-catalog`, `wizard-result-item-${dishId}`, `wizard-add-today-${dishId}`, `wizard-day-sheet`
- Behavior: WIZ-04 fallback ladder + WIZ-05 add-to-meal via existing `addScheduledMeal` (no new scheduling mechanism, no new store action)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wizard answers → ScheduledMeal write | Selected dish id + chosen date construct a `ScheduledMeal` dispatched into the persisted `scheduledMeal` slice |
| Catalog data → DishScorer | Persisted dishes/ingredients feed the pinned read-only scorer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | Constructed ScheduledMeal payload | mitigate | `id` via `nanoid(12)` (collision-resistant); `plannedDate`/`createdDate` are server-of-record-free local Dates; dish id sourced from the selected `ScoredDish.dish.id`, not free user text |
| T-04-04 | Denial of Service | Result rendering over large catalogs | mitigate | List is sliced to 5 (`.slice(0,5)`); scorer is the pinned implementation, not re-run per row |
| T-04-05 | Information Disclosure | Dish names rendered in rows/toast | accept | Local-first single-user app, no PII boundary; dish names are user's own data rendered via escaped React text |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — `@reduxjs/toolkit`, antd `message`, existing `@components`/`@store` already present |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Fallback ladder branches verified by the 04-05 cold-start E2E (populated and empty catalog scenarios).
- `DishScorer` is called as a black box and never re-sorted or modified.
</verification>

<success_criteria>
- The result step always yields an actionable outcome (WIZ-04): top few, full-catalog fallback, or route-to-add-first-dish.
- A chosen dish is added to today's meals via the existing `addScheduledMeal` path and the wizard completes (WIZ-05).
</success_criteria>

<output>
Create `.planning/phases/04-wizard-ui-hero-entry/04-02-SUMMARY.md` when done.
</output>
