---
phase: 04-wizard-ui-hero-entry
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
  - src/Modules/MealPlanning/Screens/Wizard.screen.tsx
  - src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx
autonomous: true
gap_closure: true
requirements: [WIZ-02, WIZ-05]
must_haves:
  truths:
    - "The result step shows an explicit finish/done control that ends the wizard and leaves the flow"
    - "Re-entering the wizard after completing it starts fresh at the ingredients step with cleared answers"
    - "Result-row action buttons fit within the card on a phone-width viewport without overflowing"
  artifacts:
    - path: "src/Modules/MealPlanning/Screens/WizardResult.widget.tsx"
      provides: "Explicit finish button (dispatch completeWizard + navigate home) and vertically-stacked result-row action buttons"
      contains: "wizard-finish"
    - path: "src/Modules/MealPlanning/Screens/Wizard.screen.tsx"
      provides: "Mount effect that restarts a completed wizard"
      contains: "restartWizard"
  key_links:
    - from: "src/Modules/MealPlanning/Screens/Wizard.screen.tsx"
      to: "restartWizard"
      via: "useEffect on mount when status==='completed'"
      pattern: "restartWizard\\(\\)"
    - from: "src/Modules/MealPlanning/Screens/WizardResult.widget.tsx"
      to: "completeWizard"
      via: "finish button onClick"
      pattern: "completeWizard\\(\\)"
---

<objective>
Close the three UAT-diagnosed defects from 04-UAT.md so the guided wizard can be finished, reopened cleanly, and read on a phone.

Purpose: UAT test 2 found no way to finish the flow without adding a dish; test 3 found re-entry resumes on the stale completed result; test 6 found the result-row action buttons overflow narrow cards. All three block the milestone's "first-timer reaches a meal, smooth on mobile" success metric.
Output: An explicit finish affordance on the result step, a mount-time reset for completed wizards, vertically-stacked action buttons, and extended screen tests proving the reset.
</objective>

<execution_context>
@/Users/admin/personal/my-household/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/personal/my-household/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wizard-ui-hero-entry/04-UAT.md

@src/Modules/MealPlanning/Screens/WizardResult.widget.tsx
@src/Modules/MealPlanning/Screens/Wizard.screen.tsx
@src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx
@src/Store/Reducers/WizardReducer.ts
@src/Store/Models/Wizard.ts
@src/Store/Selectors.ts
@src/Components/Layout/Stack/Stack.tsx
@src/Routing/RootRoutes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add finish affordance and fix result-row button overflow (gaps 1 and 3)</name>
  <files>src/Modules/MealPlanning/Screens/WizardResult.widget.tsx</files>
  <action>
Two changes in WizardResult.widget.tsx, both on the result step.

GAP 3 (overflow, minor) — In the `ResultRow` component, the action `Stack` at line ~56 (`<Stack gap={8} style={{ marginTop: 4 }}>` wrapping the two `Button`s) has no `direction`, so it defaults to a CSS row with no wrap and the two padded buttons (each `paddingInline: 20`) overflow narrow phone cards. Change that Stack to `direction="column"` and add `fullwidth` so the buttons stack vertically and span the card width. Add `style={{ width: "100%" }}` to BOTH buttons inside it ("Thêm vào hôm nay" / wizard-add-today-* and "Chọn ngày khác") so each fills the column. Keep `gap={8}` and the existing `marginTop: 4`, and keep `borderRadius: 20`. Do NOT change the day-picker Sheet's Stack (it is already `direction="column" fullwidth`).

GAP 1 (finish button, major) — The wizard is currently marked complete only as a side-effect of `addDishToDay` (the `dispatch(completeWizard())` at line ~110). Add an explicit finish control so a user who has seen suggestions can end the flow without adding a dish.
  - `completeWizard` is already imported from `@store/Reducers/WizardReducer`; `useNavigate` is already imported and `navigate`/`dispatch` already exist in the `WizardResult` component.
  - Import `RootRoutes` is already present (used by the empty-catalog branch). Use `RootRoutes.AuthorizedRoutes.Root()` (returns "/") as the home destination.
  - In the main return (the `data-testid="wizard-step-result"` Box, the branch that renders the dish list), add a finish Button AFTER the matches/fallback list and BEFORE the `<Sheet>`. Give it `data-testid="wizard-finish"`, label "Xong" (Vietnamese for "done"), `type="primary"`, full width (`style={{ width: "100%", borderRadius: 20, paddingInline: 20 }}`), and an `onClick` that runs `dispatch(completeWizard()); navigate(RootRoutes.AuthorizedRoutes.Root());` in that order. This implements the WIZ-02 guided-journey completion affordance the UAT flagged as missing.
  - Do NOT add the finish button to the empty-catalog branch (it has its own "Thêm món đầu tiên" CTA and there is nothing to finish).
  - Leave the implicit `completeWizard()` inside `addDishToDay` as-is — adding a dish still completes the flow; the new button just gives an explicit exit.

Do not introduce a "v1"/placeholder; wire the real `completeWizard` dispatch and real navigation now.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx --watchAll=false</automated>
  </verify>
  <done>WizardResult.widget.tsx renders a `wizard-finish` button on the result step whose onClick dispatches completeWizard() then navigates to "/"; each ResultRow's two action buttons sit in a vertical full-width Stack (direction="column" fullwidth, each button width:100%); the screen test suite still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Restart the wizard on re-entry after completion + extend screen tests (gap 2)</name>
  <files>src/Modules/MealPlanning/Screens/Wizard.screen.tsx, src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx</files>
  <action>
GAP 2 (reopen doesn't reset, major) — Wizard.screen.tsx renders off the persisted `currentStep` with no status check, so after `completeWizard()` sets `status='completed'` (with `currentStep` still `'result'`), re-entry shows the stale result. `restartWizard` exists in WizardReducer.ts but is dispatched nowhere.

In src/Modules/MealPlanning/Screens/Wizard.screen.tsx:
  - Import `useEffect` from React (the file currently does `import React from "react"`; change to `import React, { useEffect } from "react"` or use `React.useEffect`).
  - Import `restartWizard` from `@store/Reducers/WizardReducer` (alongside the existing `advanceWizardStep, commitWizardAnswer, goBackWizardStep` import).
  - Import `selectWizardStatus` from `@store/Selectors` (alongside the existing `selectWizardStep` import) and read it: `const status = useSelector(selectWizardStatus);`.
  - Add a mount effect that resets a completed wizard once on entry:
    `useEffect(() => { if (status === 'completed') dispatch(restartWizard()); }, []);`
    Run it ONLY on mount (empty dependency array) — entering the screen is the trigger, and restartWizard flips status to 'in_progress' so it will not loop. Add an eslint-disable-next-line for react-hooks/exhaustive-deps directly above the dependency array if the lint rule is enforced in this repo.
  - After restartWizard runs, the slice resets to `currentStep='ingredients'` with cleared answers, so the existing `idx`/`currentStep` clamp logic renders the ingredients step fresh. No change to the render body is needed.

In src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx (extend the existing `describe("WizardScreen", ...)`):
  - Import `completeWizard` from `@store/Reducers/WizardReducer` (the file already imports `advanceWizardStep` from there).
  - Add a test: "restarts a completed wizard at the ingredients step on re-entry". Build a store, `store.dispatch(advanceWizardStep("result"))` then `store.dispatch(completeWizard())` (so status='completed', currentStep='result'), render via `renderWizard(store)`, then assert `screen.getByTestId("wizard-step-ingredients")` is in the document and `screen.queryByTestId("wizard-step-result")` is NOT. Optionally assert `store.getState().personal.wizard.status === 'in_progress'` after render to confirm the reset dispatched.
  - Keep the three existing tests passing unchanged.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx --watchAll=false</automated>
  </verify>
  <done>Entering Wizard.screen with status='completed' dispatches restartWizard() once on mount and renders the ingredients step (not the stale result); the new screen test asserts the reset and all existing WizardScreen tests pass.</done>
</task>

</tasks>

<verification>
- `CI=true npx react-scripts test src/Modules/MealPlanning/Screens/Wizard.screen.test.tsx --watchAll=false` passes including the new reset test.
- `CI=true npx react-scripts test src/Store/Reducers/WizardReducer.test.ts --watchAll=false` still passes (restartWizard/completeWizard behavior already covered; no reducer changes in this plan).
- `npx tsc --noEmit` (or the project's typecheck) reports no new errors in the two edited source files.
- Manual re-test of UAT tests 2, 3, and 6: a "Xong" button ends the flow and returns home; reopening the wizard after finishing starts at ingredients; result-row buttons no longer overflow on a phone-width viewport.
</verification>

<success_criteria>
- Result step shows an explicit `wizard-finish` ("Xong") control that dispatches completeWizard() and navigates to "/".
- Re-entering the wizard after completion starts fresh at the ingredients step with cleared answers (restartWizard dispatched on mount).
- Result-row action buttons stack vertically full-width and fit within the card on phone-width viewports.
- Screen tests extended and green; no reducer or selector changes required.
</success_criteria>

<output>
Create `.planning/phases/04-wizard-ui-hero-entry/04-06-SUMMARY.md` when done.
</output>
