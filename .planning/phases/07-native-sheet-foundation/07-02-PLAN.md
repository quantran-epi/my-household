---
phase: 07-native-sheet-foundation
plan: 07-02
type: execute
wave: 2
depends_on: ['07-01']
requirements: [SHEET-01, SHEET-02, SHEET-04, SHEET-05, SHEET-06]
files_modified:
  - src/Components/FastOverlay/FastOverlay.tsx
  - public/index.html
  - src/Components/Sheet/Sheet.test.tsx
autonomous: true
must_haves:
  truths:
    - "User can drag the sheet down with a finger and the sheet follows the finger via translate3d using pointer events + setPointerCapture, drag state local to the instance (SHEET-01, D-06)"
    - "A flick or far-enough drag dismisses the sheet by calling onClose; a short/slow drag springs back to rest (SHEET-02, D-02)"
    - "Backdrop opacity fades as the sheet is dragged down (SHEET-01)"
    - "Every existing <Sheet> gains grabber + drag-to-dismiss + safe-area automatically with no call-site/API changes (SHEET-01, D-03)"
    - "A maskClosable={false} sheet shows the grabber and safe-area padding but never drag-dismisses; no new prop introduced (SHEET-04, D-04)"
    - "Grabber + header are always drag handles (touch-action:none); body drag only begins at scrollTop===0; overscroll-behavior:contain prevents rubber-band-into-drag (SHEET-03, D-05)"
    - "Sheet height uses dvh on supporting browsers and respects the bottom safe-area inset via viewport-fit=cover meta (SHEET-05, D-07)"
    - "Scrolling the sheet body never scrolls the page behind it; nested sheets stack correctly (SHEET-06)"
    - "Focus moves into the sheet on open and returns to the trigger on close (SHEET-01)"
    - "prefers-reduced-motion users get clamped/no animation for spring-back and dismiss; finger-follow stays direct (D-06)"
  artifacts:
    - path: "src/Components/FastOverlay/FastOverlay.tsx"
      provides: "Native bottom-sheet Sheet with grabber, pointer drag, backdrop coupling, focus trap"
      contains: "from './dragDecision'"
    - path: "public/index.html"
      provides: "viewport meta with viewport-fit=cover"
      contains: "viewport-fit=cover"
    - path: "src/Components/Sheet/Sheet.test.tsx"
      provides: "Tests for grabber presence, aria-label, maskClosable={false} grabber"
  key_links:
    - from: "src/Components/FastOverlay/FastOverlay.tsx"
      to: "src/Components/FastOverlay/dragDecision.ts"
      via: "import { shouldStartDrag, dragDecision, VELOCITY_FLICK }"
      pattern: "from '\\./dragDecision'"
    - from: "Sheet pointer handlers"
      to: "section translate3d transform"
      via: "local React state/ref drives transform during drag"
      pattern: "translate3d"
    - from: "Sheet body padding"
      to: "env(safe-area-inset-bottom)"
      via: "injected style class"
      pattern: "env\\(safe-area-inset-bottom\\)"
---

<objective>
Upgrade the `Sheet` export in `src/Components/FastOverlay/FastOverlay.tsx` (~lines 312-383) into a native-feeling iOS bottom sheet. The sheet gains a draggable grabber, finger-following pointer drag with backdrop coupling, flick/spring-back dismissal, dvh height cascade, safe-area padding, overscroll containment, and a focus trap. All behavior is driven by the pure reducer from plan 07-01 (`src/Components/FastOverlay/dragDecision.ts`).

Purpose: Make the existing Sheet feel like a native iOS sheet without changing any call-site API. Every existing `<Sheet>` usage keeps working with identical props (open, title, onClose, children, height, zIndex, maskClosable, closable, keyboard, data-testid).

Output: Modified FastOverlay.tsx Sheet implementation, viewport-fit=cover meta in public/index.html, extended Sheet.test.tsx.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07-native-sheet-foundation/07-PATTERNS.md
@.planning/phases/07-native-sheet-foundation/07-RESEARCH.md
@src/Components/FastOverlay/FastOverlay.tsx
@src/Components/FastOverlay/dragDecision.ts
@src/Components/Sheet/Sheet.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add viewport-fit=cover to viewport meta</name>
  <files>public/index.html</files>
  <read_first>public/index.html</read_first>
  <action>
    Locate the viewport `<meta>` tag whose content is currently `width=device-width, initial-scale=1, maximum-scale=1`. Append `, viewport-fit=cover` to the content so the page extends under the device safe areas and `env(safe-area-inset-bottom)` resolves to non-zero on notched iOS devices (per D-07, SHEET-05). Do not alter the other viewport directives.
  </action>
  <verify>
    <automated>grep -q 'viewport-fit=cover' public/index.html</automated>
  </verify>
  <done>The viewport meta content contains `viewport-fit=cover` alongside the existing directives.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Grabber + pointer drag + backdrop coupling</name>
  <files>src/Components/FastOverlay/FastOverlay.tsx</files>
  <read_first>src/Components/FastOverlay/FastOverlay.tsx, src/Components/FastOverlay/dragDecision.ts, .planning/phases/07-native-sheet-foundation/07-PATTERNS.md</read_first>
  <action>
    Import `shouldStartDrag`, `dragDecision`, and `VELOCITY_FLICK` from `'./dragDecision'` (the pure reducer from plan 07-01) — no Redux for any drag state.

    Add a grabber pill rendered as `<button type="button">` with an `aria-label` (Vietnamese, matching the existing "Đóng" close-button language, e.g. "Kéo để đóng"), placed ABOVE the existing header div inside the `<section role="dialog">`. Mark the grabber with a `data-drag-handle` attribute. The grabber and the header are always-active drag handles; the scroll body only initiates drag when `shouldStartDrag()` reports its `scrollTop === 0` (per D-01, SHEET-01).

    Implement pointer drag with `onPointerDown`/`onPointerMove`/`onPointerUp` on the drag-handle elements. On pointerdown, call `setPointerCapture(e.pointerId)` on the target and record start Y, start time, and current scrollTop. Track the finger delta in LOCAL state via `useState`/`useRef` (never Redux). While dragging, clamp Δy to non-negative and apply `transform: translate3d(0, ${Δy}px, 0)` to the `<section>` with `transition: none` so it follows the finger 1:1. Couple backdrop opacity to drag distance: as Δy grows toward the sheet height, reduce the outer overlay background alpha proportionally from its rest value (rgba(16,24,40,0.30)).

    On pointerup, compute velocity (Δy / elapsed ms) and call `dragDecision({ offset, sheetHeight, velocity, maskClosable })` — this is the EXACT signature defined in plan 07-01's `dragDecision.ts` (RESEARCH §"Scroll-vs-Drag Branch Enumeration"). Pass the clamped non-negative finger delta as `offset` and the dragged `<section>`'s rendered height (read from the section ref's `offsetHeight` or `getBoundingClientRect().height`) as `sheetHeight`, so the reducer's D2 distance rule `offset >= 0.40 * sheetHeight` can evaluate. Do NOT rename `offset` to `deltaY` or omit `sheetHeight` — the reducer destructures those exact keys. Honor SHEET-04: when `maskClosable === false`, `dragDecision` returns `spring-back` regardless of distance/velocity (no drag-dismiss) — do NOT add a new prop, read the existing `maskClosable`. If the decision is `'dismiss'`, call `onClose()`. If `'spring-back'`, animate the section back to rest (Δy → 0) via the CSS transition added in Task 4. Reset local drag state after release. Use `VELOCITY_FLICK` only inside the reducer (already consumed there) — do not re-implement the threshold here.

    Keep the existing aesthetic (gradient background, radius, header, close button) unchanged. Drag state must be entirely local.
  </action>
  <verify>
    <automated>grep -q "from './dragDecision'" src/Components/FastOverlay/FastOverlay.tsx && grep -q 'data-drag-handle' src/Components/FastOverlay/FastOverlay.tsx && grep -q 'setPointerCapture' src/Components/FastOverlay/FastOverlay.tsx && grep -q 'translate3d' src/Components/FastOverlay/FastOverlay.tsx</automated>
  </verify>
  <done>FastOverlay.tsx imports `shouldStartDrag`/`dragDecision`/`VELOCITY_FLICK` from `'./dragDecision'`; a grabber `<button>` with `aria-label` and `data-drag-handle` renders above the header; `setPointerCapture` is called on pointerdown; `translate3d` transform is applied during drag; backdrop opacity couples to drag distance; `maskClosable===false` never dismisses; no Redux import is added for drag state.</done>
</task>

<task type="auto">
  <name>Task 3: dvh cascade + safe-area + overscroll + touch-action via injected style</name>
  <files>src/Components/FastOverlay/FastOverlay.tsx</files>
  <read_first>src/Components/FastOverlay/FastOverlay.tsx, .planning/phases/07-native-sheet-foundation/07-RESEARCH.md</read_first>
  <action>
    React inline `style` objects cannot carry duplicate keys, so the `vh→dvh` progressive cascade must live in injected CSS. Add (or extend) an injected `<style>` block under the `.my-recipes-fast-overlay` scope with a dedicated sheet class (e.g. `.my-recipes-fast-overlay__sheet`) applied to the `<section>`:
    - `max-height: min(85vh, 720px);` followed by `max-height: min(85dvh, 720px);` so dvh wins where supported (SHEET-05). Keep `toCssSize(height, ...)` behavior intact for explicit heights — the cascade is the default fallback.
    - `touch-action: none;` on the grabber and header drag handles so vertical drags are captured rather than scrolling (D-01, SHEET-01).
    - Scroll body: `overscroll-behavior: contain;` so body scroll never chains to the page behind (SHEET-06).
    - Safe-area padding: set `padding-bottom: calc(16px + env(safe-area-inset-bottom));` on the body div (and any SheetActions footer) so content clears the home indicator (SHEET-05).

    Do NOT pass an explicit `zIndex`; keep nested stacking driven by `useResolvedOverlayZIndex` (SHEET-06). Apply the new class to the section in addition to existing inline styles.
  </action>
  <verify>
    <automated>grep -q '85dvh' src/Components/FastOverlay/FastOverlay.tsx && grep -q 'env(safe-area-inset-bottom)' src/Components/FastOverlay/FastOverlay.tsx && grep -q 'overscroll-behavior' src/Components/FastOverlay/FastOverlay.tsx && grep -q 'touch-action' src/Components/FastOverlay/FastOverlay.tsx</automated>
  </verify>
  <done>Injected style under `.my-recipes-fast-overlay` contains the `vh`→`dvh` max-height cascade, `env(safe-area-inset-bottom)` padding, `overscroll-behavior: contain`, and `touch-action: none` on drag handles; no explicit zIndex is passed.</done>
</task>

<task type="auto">
  <name>Task 4: Focus trap + preserve reduced-motion/scroll-lock/escape/z-index + spring transition</name>
  <files>src/Components/FastOverlay/FastOverlay.tsx</files>
  <read_first>src/Components/FastOverlay/FastOverlay.tsx</read_first>
  <action>
    Add a focus trap to the `<section role="dialog" aria-modal="true">`: on open, store `document.activeElement` as the trigger, move focus into the sheet (grabber or first focusable, or the section itself with tabIndex=-1). Trap Tab/Shift+Tab to cycle within focusable descendants. On close/unmount, restore focus to the stored trigger element.

    Define the spring-back/dismiss motion as a CSS `transition` on `transform` (and backdrop opacity) under the `.my-recipes-fast-overlay` sheet class so it inherits the existing `prefers-reduced-motion` clamp from `overlayMotionStyles`. During active finger-follow, transform uses `transition: none` (set in Task 2); on release, switch to the transitioned class so spring-back/dismiss animates. Preserve `useBodyScrollLock(open)`, `useEscapeClose(open && keyboard, onClose)`, and `useResolvedOverlayZIndex(open, zIndex, 1200)` exactly — do not change their call signatures. Aesthetic and existing sheet-in animation unchanged.
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false Sheet</automated>
  </verify>
  <done>Focus moves into the sheet on open and restores to the trigger on close; Tab is trapped within the dialog; spring-back/dismiss transition lives under `.my-recipes-fast-overlay` (inherits reduced-motion clamp); body scroll-lock, escape-close, and resolved z-index hooks are preserved with no explicit zIndex passed.</done>
</task>

<task type="auto">
  <name>Task 5: Extend Sheet.test.tsx for grabber + maskClosable</name>
  <files>src/Components/Sheet/Sheet.test.tsx</files>
  <read_first>src/Components/Sheet/Sheet.test.tsx</read_first>
  <action>
    Add tests to the existing Sheet suite:
    - Grabber is present when the sheet is open (query by role `button` / the grabber `aria-label`, or `data-drag-handle`).
    - Grabber exposes its `aria-label`.
    - With `maskClosable={false}`, the sheet still renders the grabber (drag-dismiss disabled is reducer-covered in 07-01; here assert the grabber affordance still exists).
    Use existing render helpers/testid conventions already in the file. Keep tests deterministic (no real pointer-physics timing).
  </action>
  <verify>
    <automated>CI=true npx react-scripts test --watchAll=false Sheet</automated>
  </verify>
  <done>`CI=true react-scripts test --watchAll=false Sheet` exits 0 with new assertions for grabber presence, grabber aria-label, and grabber-with-maskClosable={false}.</done>
</task>

</tasks>

## Artifacts this phase produces

- Grabber control: `<button type="button">` with `aria-label` and a `data-drag-handle` attribute, rendered above the header inside the dialog section.
- `data-drag-handle` attribute applied to the grabber and header (always-active drag handles).
- Injected sheet CSS class (e.g. `.my-recipes-fast-overlay__sheet`) carrying the `vh→dvh` max-height cascade, `touch-action: none`, `overscroll-behavior: contain`, and `env(safe-area-inset-bottom)` padding.
- Modified viewport `<meta>` in `public/index.html` with `viewport-fit=cover`.
- Local (non-Redux) drag state inside the Sheet component (`useState`/`useRef` for delta, start Y, start time, dragging flag).

<verification>
- `grep -q 'viewport-fit=cover' public/index.html` succeeds (Task 1).
- `grep` confirms `from './dragDecision'`, `data-drag-handle`, `setPointerCapture`, `translate3d` in FastOverlay.tsx (Task 2).
- `grep` confirms `85dvh`, `env(safe-area-inset-bottom)`, `overscroll-behavior`, `touch-action` in FastOverlay.tsx (Task 3).
- No new `from 'react-redux'` / `useDispatch` / `useSelector` import added for drag state (`grep` review).
- No explicit `zIndex` prop passed by Sheet to the overlay; `useResolvedOverlayZIndex` call preserved.
- `CI=true react-scripts test --watchAll=false Sheet` exits 0 (Tasks 4, 5).
- Manual sanity (out of automated scope): finger drag follows 1:1, flick dismisses, short drag springs back, backdrop fades, reduced-motion clamps animation, focus returns to trigger on close.
</verification>

<success_criteria>
- Sheet renders a draggable grabber and supports finger-following pointer drag with backdrop coupling.
- Flick/far drag dismisses via `onClose`; short/slow drag springs back; `maskClosable={false}` never drag-dismisses.
- dvh height cascade, safe-area bottom padding, overscroll containment, and `touch-action: none` handles are in place.
- `viewport-fit=cover` present in the viewport meta.
- Focus trap added; scroll-lock, escape-close, resolved z-index, and reduced-motion guards preserved.
- No call-site API change; all existing `<Sheet>` props behave identically.
- Sheet test suite passes.
</success_criteria>

<output>
Create `.planning/phases/07-native-sheet-foundation/07-02-SUMMARY.md` when done.
</output>

## PLANNING COMPLETE 07-02
