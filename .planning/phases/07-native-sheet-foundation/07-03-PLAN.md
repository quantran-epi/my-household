---
phase: 07-native-sheet-foundation
plan: 07-03
type: execute
wave: 3
depends_on: ['07-02']
requirements: [SHEET-03, SHEET-05, SHEET-06]
files_modified: [playwright.config.ts, tests/e2e/native-sheet.spec.ts]
autonomous: false  # webkit binary install + local dev server launch may require an unattended-unfriendly environment; see Task 1 reason

must_haves:
  truths:
    - "A WebKit iPhone (hasTouch + isMobile) Playwright project named mobile-safari runs alongside the existing chromium project (D-08)"
    - "Dragging the Sheet grabber past 40% dismisses it; dragging <40% springs it back open (D-02, D-08)"
    - "Scrolling a Sheet body below its top consumes the drag (list scrolls, Sheet stays); the same drag at top dismisses (SHEET-03)"
    - "maskClosable={false} Sheets spring back instead of dismissing on an over-threshold drag (SHEET-04)"
    - "A nested Sheet B opened from Sheet A stacks above A and dismisses independently while A stays open and scroll-locked (SHEET-06)"
    - "On the iPhone descriptor the Sheet clears the home indicator via safe-area padding and is not clipped by the URL bar via dvh (SHEET-05)"
  artifacts:
    - path: "playwright.config.ts"
      provides: "mobile-safari WebKit iPhone project with hasTouch/isMobile, chromium project preserved"
      contains: "mobile-safari"
    - path: "tests/e2e/native-sheet.spec.ts"
      provides: "Six touch-gesture e2e flows proving the upgraded Sheet"
      min_lines: 80
  key_links:
    - from: "tests/e2e/native-sheet.spec.ts"
      to: "src/Components/FastOverlay/FastOverlay.tsx"
      via: "grabber aria-label / data-drag-handle / role=dialog selectors driven by page.touchscreen"
      pattern: "touchscreen|data-drag-handle|role=.?dialog"
    - from: "playwright.config.ts"
      to: "@playwright/test devices"
      via: "devices['iPhone 13'] descriptor with hasTouch:true"
      pattern: "devices\\["
---

<objective>
Add a real touch-capable WebKit/iPhone Playwright project and a touch e2e spec that proves the Sheet upgraded in 07-02 behaves correctly under real gestures: drag-to-dismiss, spring-back, scroll-then-drag arbitration, maskClosable protection, nested stacking, and safe-area/dvh layout on iOS.

Purpose: The reducer + setPointerCapture drag logic landed in 07-02 only matters if real pointerType="touch" events flow through it. Chromium without a mobile/touch context cannot emit those, so we add an iPhone WebKit project and assert post-gesture state. This is the final wave of Phase 7 and the integration proof for SHEET-03, SHEET-05, SHEET-06.

Output: An updated playwright.config.ts with a mobile-safari project (chromium preserved) and tests/e2e/native-sheet.spec.ts covering six flows.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-native-sheet-foundation/07-RESEARCH.md

# Depends on the Sheet upgrade shipped in 07-02 (grabber, data-drag-handle, drag reducer, safe-area/dvh markup)
@.planning/phases/07-native-sheet-foundation/07-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Install WebKit and add the mobile-safari project to playwright.config.ts</name>
  <files>playwright.config.ts</files>
  <read_first>
    - playwright.config.ts (current single-project shape: testDir ./tests/e2e, projects array with chromium/E2E_BROWSER_CHANNEL, webServer `npm start` on port 3010 reuseExistingServer)
  </read_first>
  <action>
    Only chromium is installed in ~/Library/Caches/ms-playwright; WebKit is NOT present. First run `npx playwright install webkit` so the new project can launch — without it the mobile-safari project fails at browser launch. This task is gated human-action because the WebKit binary download and the local dev server may not run unattended in CI/sandboxed environments; a human confirms the install succeeds (or runs it locally) before the spec is executed.

    Then edit playwright.config.ts: import `devices` from `@playwright/test` (alongside the existing `defineConfig`/`PlaywrightTestConfig` imports). Add a second entry to the existing `projects` array named `mobile-safari` whose `use` spreads a WebKit iPhone device descriptor — use `...devices['iPhone 13']`, which sets `defaultBrowserType: 'webkit'`, `hasTouch: true`, `isMobile: true`, and a mobile viewport. Do NOT remove, rename, or alter the existing chromium project — it must stay first and unchanged. Leave testDir, webServer, and global config untouched. Per SHEET-05 this project is the iOS-specific surface; SHEET-03 and SHEET-06 flows also run against it because they need a real touch context.
  </action>
  <verify>
    <automated>npx playwright install webkit && node -e "const c=require('./playwright.config.ts'); " 2>/dev/null; grep -n "mobile-safari" playwright.config.ts && grep -n "devices\[" playwright.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright install webkit` exits 0 and a webkit-* directory appears in ~/Library/Caches/ms-playwright.
    - playwright.config.ts `projects` array contains an entry named `mobile-safari` whose `use` is built from a WebKit iPhone device descriptor with `hasTouch: true` and `isMobile: true`.
    - The original chromium (E2E_BROWSER_CHANNEL) project is still present and unmodified; `npx playwright test --list` shows both projects.
  </acceptance_criteria>
  <done>WebKit installed; mobile-safari project added; chromium project preserved; both projects list-able.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the touch e2e spec tests/e2e/native-sheet.spec.ts (six flows)</name>
  <files>tests/e2e/native-sheet.spec.ts</files>
  <read_first>
    - tests/e2e/dish-suggester-sheet-overflow.spec.ts (closest existing Sheet-focused e2e analog — mirror its import style, test/describe structure, and how it navigates to the app route / reaches the Sheet trigger)
    - src/Components/FastOverlay/FastOverlay.tsx (selectors shipped by 07-02: grabber button aria-label, the `data-drag-handle` attribute, the `role="dialog"` section, and the backdrop/mask element)
  </read_first>
  <behavior>
    - Flow 1 (SHEET-01/02 integration): touchStart on the grabber/data-drag-handle, touchMove with clientY increasing past 40% of sheet height, touchEnd → role=dialog section is detached and the backdrop is gone.
    - Flow 2 (spring-back): drag <40% then slow touchEnd → role=dialog section still attached/visible.
    - Flow 3 (SHEET-03 scroll arbitration): scroll the sheet body list down (not at top), drag down on the body → list scrolls, sheet stays; reset to top, same drag → sheet dismisses.
    - Flow 4 (SHEET-04 protection): on a maskClosable={false} sheet, drag past 40% → springs back, sheet stays open, grabber + safe-area padding still present.
    - Flow 5 (SHEET-06 nested): open sheet B from inside sheet A → B stacked above A (higher z / on top); dismiss B → A still attached and still scroll-locked (body overflow hidden).
    - Flow 6 (SHEET-05 safe-area + dvh): on the iPhone descriptor, the sheet bottom padding clears the home indicator and the sheet height (dvh cap) is not clipped under the URL bar.
  </behavior>
  <action>
    Create tests/e2e/native-sheet.spec.ts following the structure of the existing spec read above. Write six tests inside a describe block, each named for the requirement it proves (e.g. "drag past 40% dismisses (SHEET-01/02)", "scroll-then-drag keeps sheet open then dismisses at top (SHEET-03)", "maskClosable=false springs back (SHEET-04)", "nested sheet stacks and dismisses independently (SHEET-06)", "safe-area + dvh layout on iPhone (SHEET-05)").

    Drive gestures with the touch context: use `page.touchscreen` and an explicit touchStart → repeated touchMove (monotonically increasing clientY to simulate a downward drag) → touchEnd sequence so WebKit emits real pointerdown/pointermove/pointerup with pointerType "touch" — exactly what the 07-02 setPointerCapture + drag reducer consume. For the 40% threshold, read the dialog's bounding box height and move by >40% (dismiss) or <40% (spring-back).

    Assert POST-gesture state only: presence/absence of the `role="dialog"` section and the backdrop, list scrollTop for the arbitration flow, body overflow lock for the nested flow, and computed bottom padding / sheet height for the safe-area flow. Do NOT assert mid-drag transform values — they are timing-flaky.

    Harness/fixture note: these flows need a route in the app that mounts the Sheet (and a maskClosable={false} variant + a nested A→B variant). If the running app (`npm start`, port 3010) does not already expose triggers to open these Sheet variants, add a minimal dev/fixture route or a query-param-driven mount in the app and document it in the SUMMARY; the spec must navigate to that route. Prefer reusing an existing screen that already opens the Sheet if one exists from 07-02. The webServer config already starts `npm start` with reuseExistingServer.
  </action>
  <verify>
    <automated>npm run test:e2e -- --project=mobile-safari native-sheet</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/native-sheet.spec.ts contains exactly six tests, each named for the requirement (SHEET-01/02, SHEET-03, SHEET-04, SHEET-05, SHEET-06) it proves.
    - `npm run test:e2e -- --project=mobile-safari native-sheet` runs the spec against the WebKit iPhone project (real touch events) — green once 07-02 markup is present.
    - Gestures are driven via page.touchscreen touchStart→touchMove→touchEnd; assertions check post-gesture state (sheet present/absent, scrollTop, body overflow lock, bottom padding/height), not mid-drag transforms.
  </acceptance_criteria>
  <done>Six-flow touch spec exists and passes on mobile-safari; no mid-drag transform assertions.</done>
</task>

</tasks>

## Artifacts this phase produces

- `mobile-safari` Playwright project (WebKit iPhone device descriptor, hasTouch:true / isMobile:true) added to `playwright.config.ts` alongside the preserved chromium project.
- `tests/e2e/native-sheet.spec.ts` — six touch-gesture e2e flows (drag-dismiss, spring-back, scroll-then-drag arbitration, maskClosable protection, nested stacking, safe-area/dvh layout).
- WebKit browser binary installed into the local Playwright cache.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright binary download | `npx playwright install webkit` fetches a browser binary over the network |
| Local dev server (port 3010) | e2e drives the locally served app; no external/untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `npx playwright install webkit` binary fetch | mitigate | Install via pinned @playwright/test ^1.60.0 (already in package.json) so the binary matches the audited Playwright version; no new package added |
| T-07-02 | Information disclosure | e2e fixture/dev route | accept | Test-only route serves no real data; no PII; not shipped behind auth boundaries |
| T-07-SC | Tampering | npm/playwright installs | mitigate | No new npm deps added; only Playwright-managed webkit binary for the already-installed @playwright/test |
</threat_model>

<verification>
- `npx playwright install webkit` succeeds and webkit appears in ~/Library/Caches/ms-playwright.
- `npx playwright test --list` shows both the chromium project (untouched) and the new mobile-safari project.
- `npm run test:e2e -- --project=mobile-safari native-sheet` is green (all six flows) once 07-02 has shipped the grabber/data-drag-handle/role=dialog markup, safe-area padding, and dvh height cap.
- chromium project config (channel, use block) is byte-for-byte unchanged.

Note (must_haves dependency): This plan can only meaningfully verify after 07-02 ships the grabber/drag-handle/safe-area markup and the drag reducer. Until then the spec exists and the project lists, but flow assertions cannot pass.
</verification>

<success_criteria>
- mobile-safari WebKit iPhone project added; chromium preserved; webkit binary installed.
- tests/e2e/native-sheet.spec.ts covers all six flows, each named for its requirement, driven by real touch events, asserting post-gesture state.
- e2e green on mobile-safari (post-07-02).
</success_criteria>

<output>
Create `.planning/phases/07-native-sheet-foundation/07-03-SUMMARY.md` when done.
</output>

## PLANNING COMPLETE 07-03
